---
phase: 05-annotations-composite-locator
plan: 05-08
type: execute
wave: 2
depends_on: [05-07]
files_modified: []
autonomous: false
gap_closure: true
requirements: [ANNO-01, ANNO-02, ANNO-03, ANNO-04]
must_haves:
  truths:
    - "AVD Medium_Phone_API_36.1 上，选区气泡在分页与滚动两模式都正确定位于选区上方（分页闭合-shadow 坐标映射通过）"
    - "4 色高亮 / 下划线 / 复制 / 笔记 / 书签在设备上创建、编辑、持久，reopen 与 paginate↔scroll 切换后正确重绘"
    - "某 简繁/词不拆行 状态下建的高亮，切换该状态后仍锚定原文（自愈），不漂移不丢失"
    - "手指竖滑能滚动阅读视图与批注/笔记 sheet（无 pointer-capture 回归）"
    - "200+ 批注开书与滚动无可见卡顿（懒式逐 section 绘制扛得住）"
  artifacts: []
  key_links: []
---

<objective>
DEFECT 1 修复后（05-07），重跑被阻塞的 05-05 强制 Android 设备验收门（8 步）。此前气泡被原生 ActionMode 顶掉 → ANNO-01..04 的设备行为全部不可验；现补齐。经 05-07 升级后的**独立/生产 debug APK** 路径部署，覆盖分页 + 滚动两模式，采集气泡与批注 sheet 截图作证据。

Purpose: CLAUDE.md 强制门 —— 任何触及 reader 壳层/协议/排版的改动，声称完成前必须在 AVD 跑通并人工验收；桌面绿 + unit 绿不等于 Android 正确。批注引入闭合-shadow 坐标映射与 per-iframe 高亮注册，是设备-方差敏感面，必须设备复验。
Output: 8 步全绿的设备验收证据（分页 + 滚动气泡截图 + 批注 sheet 截图 + 200+ 批注性能确认）。无源码改动预期（若再现缺陷 → 再开一轮 `/gsd-plan-phase 05 --gaps`）。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-annotations-composite-locator/05-05-PLAN.md
@.planning/phases/05-annotations-composite-locator/05-UI-SPEC.md
@.planning/phases/05-annotations-composite-locator/05-RESEARCH.md
@docs/ANDROID-BUILD.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 设备门前的桌面全量 + 构建复绿</name>
  <files>(无源码改动 — 验证)</files>
  <read_first>
    - .planning/phases/05-annotations-composite-locator/05-05-PLAN.md（Task 1：设备门前置的桌面预检基线）
    - docs/ANDROID-BUILD.md（§ Device gate；MEMORY：`cargo tauri android *` 前先 export Android 环境变量）
  </read_first>
  <action>
    跑桌面预检作为设备门前置：`pnpm test`（整套 vitest，含 05-06 新增的 reading-position 用例）、`pnpm exec tsc --noEmit`、`pnpm build` 全绿；再 `cargo test --test migration`（MSVC toolchain，V7 生效）。任一红先修，别拿已知红的代码烧模拟器周期。
  </action>
  <verify>
    <automated>cd /d/Github/Pillowtome && pnpm test && pnpm exec tsc --noEmit && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test` 0 失败（含 annotation-store / anchor-resolver / css-highlight / locator-store / scroll-cfi / reading-position 各套）。
    - `pnpm exec tsc --noEmit` 干净；`pnpm build` 成功。
    - `cargo test --test migration` 绿（V7 applied）。
  </acceptance_criteria>
  <done>整套桌面 + migration 绿 —— 放行设备门。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: AVD 8 步批注验收（气泡坐标 / 持久重绘 / 自愈 / 手指滑 / 200+ 性能）</name>
  <files>(无源码改动 — 设备侧人工验收)</files>
  <read_first>
    - .planning/phases/05-annotations-composite-locator/05-05-PLAN.md（Task 2 的完整 8 步 how-to-verify —— 本任务复用其清单，不重复全文）
    - docs/ANDROID-BUILD.md（§ Device gate 升级后的独立生产 APK 步骤 `build --debug --apk` + `adb install -r`；§ Quick start 环境变量；force-stop 冷启动）
    - CLAUDE.md（Android emulator gate + Touch/scroll gate）
    - .planning/phases/05-annotations-composite-locator/05-UI-SPEC.md（Layout/Copywriting Contract：气泡/sheet/书签行为 + 屏上应见的简体中文文案）
    - .planning/phases/05-annotations-composite-locator/05-RESEARCH.md（Open Questions Q1 闭合-shadow 坐标映射；Pitfall 6 WebView 方差；Pitfall 9 懒绘性能）
    - MEMORY `pillowtome-android-env-stale-in-process` / `pillowtome-android-deploy-standalone-apk` / `pillowtome-reader-perf-stress-test`
  </read_first>
  <what-built>
    设备上的整套 Phase 5 批注栈：选区气泡（新建 + 编辑）、4 色高亮 + 下划线、笔记编辑、批注 sheet（按章分组 + 跳转 + 滑删）、书签开关、静默复合-locator 恢复 —— 分页走 foliate draw-annotation（闭合 shadow），滚动走 CSS Custom Highlight。DEFECT 1 修复后气泡不再被原生工具条顶掉。
  </what-built>
  <how-to-verify>
    先 export Android 环境变量；经 05-07 升级的**独立生产 APK** 路径部署：`pnpm tauri android build --debug --target x86_64 --apk` → `adb install -r <apk>` → force-stop 冷启动（不信任 HMR 半更新）。在**分页与滚动两模式**各走 8 步（完整措辞见 05-05 Task 2）：
    1. 长按进选择 → 气泡浮于选区上方（近顶翻下、带箭头指向锚点）。确认气泡正确定位 —— 分页闭合-shadow 坐标映射硬门。
    2. 点 4 色板 → 该色高亮绘出，日/夜/Sepia 三主题下均清晰可读；点 下划线 → 下划线绘出；点 复制 → 文本已复制。
    3. 点已有高亮 → 同一气泡在编辑语境重开（含 删除）；改色重着色；删除 移除。
    4. 笔记 → 笔记编辑 sheet 打开，输入后 完成；重开高亮 → 笔记持久。在笔记 sheet 与批注 sheet 内手指竖滑 → 能滚（触控 gate，无吞 pan）。
    5. 顶栏书签 toggle → 当前位置加书签（accent 态）；开 批注 sheet → 书签 + 高亮 + 笔记按章分组列出；点行 → 跳转并关 sheet；滑行 → 删除（笔记行两步确认）。
    6. 关书重开 → 批注在正确位置重绘（分页 + 滚动）；paginate↔scroll 切换 → 从 store 重放。
    7. 切 简繁（与 词不拆行）→ 已有高亮仍锚定原文（自愈），不漂一字不消失。
    8. 性能压测（MEMORY `pillowtome-reader-perf-stress-test`）：一本带 200+ 批注的书，开书并滚过数章 → 无可见卡顿、无开书卡顿（确认懒式逐 section 绘制、非开书全量绘）。
    采集分页 + 滚动气泡截图与批注 sheet 截图作证据。任一步失败 → 再开一轮 `/gsd-plan-phase 05 --gaps`，不在此内联打补丁。
  </how-to-verify>
  <action>
    经独立生产 debug APK 在 AVD `Medium_Phone_API_36.1`（或物理机）部署（export 环境变量 → `build --debug --apk` → `adb install -r` → force-stop 冷启动），走 <how-to-verify> 8 步 × 两模式。逐步记录 pass/fail，采集分页 + 滚动气泡 + 批注 sheet 截图。此为 CLAUDE.md 强制设备门；不得仅凭桌面绿声称 Phase 5 完成。任一失败路由到 `/gsd-plan-phase 05 --gaps` 后续，不内联打补丁。
  </action>
  <verify>
    <human-check>AVD Medium_Phone_API_36.1 上 8 步在分页与滚动两模式全过；截图已采集。</human-check>
  </verify>
  <acceptance_criteria>
    - 步骤 1-8 在分页与滚动两模式全过；步骤 1 分页气泡落在选区上（闭合-shadow 坐标映射通过），非固定偏移悬浮。
    - 步骤 7：简繁/词不拆行 切换前建的高亮切换后仍锚定同一词（自愈），不移不丢。
    - 步骤 4/5：手指竖滑（非桌面滚轮）能滚笔记 sheet、批注 sheet、阅读视图，无吞 pan。
    - 步骤 8：200+ 批注无开书卡顿、无滚动卡顿。
    - 分页 + 滚动气泡截图与批注 sheet 截图已作证据。
  </acceptance_criteria>
  <done>ANNO-01..04 每项行为在设备两模式验通（气泡坐标、持久/重绘、transform 切换自愈、手指滑、200+ 批注性能）；截图已附。</done>
  <resume-signal>输入 "approved"（附分页 + 滚动气泡截图），或描述缺陷以路由到 `/gsd-plan-phase 05 --gaps` 后续。</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 设备 WebView 版本方差 | 老/AOSP WebView < 105 缺 CSS Custom Highlight → 滚动高亮须退回 foliate Overlayer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-13 | Denial of Service | WebView < 105 缺 CSS Custom Highlight → 滚动高亮空白 | mitigate | 特性探测 + foliate Overlayer 兜底（05-03）；设备门确认高亮渲染（若 AVD WebView 老则走兜底路径） |
| T-05-14 | Tampering | 闭合-shadow 气泡坐标在高 DPI / 分栏下漂移 | mitigate | 设备门步骤 1 是两模式坐标映射显式验收；观察到漂移则动态 host-rect 校正 |
| T-05-SC | Tampering | npm/cargo installs | accept | 本计划零新依赖，仅验证 |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (plan 05-08)

- 无源码符号。产出设备验收证据（分页 + 滚动气泡截图 + 批注 sheet 截图 + 200+ 批注性能确认），作为 `/gsd-verify-work` 放行 Phase 5 的门禁，并复验 ANNO-01..04 在设备两模式的行为。
</artifacts_produced>

<verification>
- Task 1：桌面全量 + tsc + build + migration 绿。
- Task 2：AVD 8 步在分页与滚动两模式全过（气泡坐标 / 持久重绘 / mode-switch / transform 自愈 / 手指滑 / 200+ 批注性能），截图存证。
</verification>

<success_criteria>
- ANNO-01..04 每项在设备分页与滚动两模式确认工作。
- 闭合-shadow 气泡坐标映射与 CSS-Highlight 逐 section 重绘在模拟器上成立。
- 无触控 gate 回归；200+ 批注无开书卡顿。
</success_criteria>

<output>
Create `.planning/phases/05-annotations-composite-locator/05-08-SUMMARY.md` when done.
</output>
