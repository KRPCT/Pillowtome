---
phase: 02-epub-reading-core
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - vitest.config.ts
  - src-tauri/capabilities/default.json
  - src/reader/foliate-types.ts
  - src/reader/apply-reading-styles.ts
  - src/reader/apply-reading-styles.test.ts
  - src/reader/tap-zones.ts
  - src/reader/tap-zones.test.ts
  - src/reader/toc.ts
  - src/reader/toc.test.ts
  - src/reader/search-opts.ts
  - src/reader/search-opts.test.ts
autonomous: true
requirements: [READ-01, READ-02, READ-03, READ-04, READ-05, READ-07]
must_haves:
  truths:
    - "Frontend pure helpers for flow/CSS/tap-zones/TOC/search-opts have automated unit tests"
    - "SQL plugin permissions allow select + execute from the main window"
    - "Foliate ambient types expose goTo, goToTextStart, search, clearSearch, book.toc, goLeft, goRight"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Exact-pinned vitest runner for pure TS helpers"
      contains: "vitest"
    - path: "src/reader/apply-reading-styles.ts"
      provides: "flowAttr + buildReadingCss pure helpers"
      exports: ["flowAttr", "buildReadingCss", "SYSTEM_CJK_STACK", "PAGE_COLORS", "DEFAULT_PREFS"]
    - path: "src/reader/foliate-types.ts"
      provides: "Expanded FoliateViewElement ambient contract"
      contains: "goToTextStart"
    - path: "src-tauri/capabilities/default.json"
      provides: "sql:default + sql:allow-execute"
      contains: "sql:allow-execute"
  key_links:
    - from: "package.json"
      to: "vitest"
      via: "exact-pinned devDependency + test script"
      pattern: "\"vitest\": \"[0-9]"
    - from: "src-tauri/capabilities/default.json"
      to: "tauri-plugin-sql"
      via: "permission grants for frontend Database.load/execute"
      pattern: "sql:default|sql:allow-execute"
---

<objective>
Close Phase 2 Wave 0 validation gaps: add a minimal exact-pinned vitest harness for pure TypeScript reading helpers, grant SQL capabilities required by later prefs/locator writes, and expand foliate ambient types so subsequent plans compile against the real engine surface (goTo / search / TOC / goToTextStart).

Purpose: Nyquist sampling needs automated verifies before chrome/prefs/fonts land. SQL capabilities are a hard runtime blocker for D-20/D-23. Types prevent scavenger-hunt API inventing in later waves.
Output: vitest green on pure helpers; capabilities updated; shared helper + type modules ready for 02-01..04.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-epub-reading-core/02-CONTEXT.md
@.planning/phases/02-epub-reading-core/02-RESEARCH.md
@.planning/phases/02-epub-reading-core/02-UI-SPEC.md
@.planning/phases/02-epub-reading-core/02-VALIDATION.md
@src/reader/FoliateView.tsx
@src-tauri/capabilities/default.json
@package.json
</context>

<decision_notes>
Planner decisions locked for this phase set:
1. **Frontend tests:** Add exact-pinned **vitest** (not Rust-only) for pure TS helpers — CSS builder, flow attr, tap zones, TOC flatten, search opts. Engine E2E remains manual/desktop.
2. **Locator upsert (used from 02-02/03):** SCHEMA_V2 adds `CREATE UNIQUE INDEX IF NOT EXISTS idx_locator_work_id ON locator(work_id);` then upsert via `INSERT ... ON CONFLICT(work_id) DO UPDATE` (one progress row per work; annotations stay out of this table until P5).
3. **Font serve (used from 02-04):** Extend existing `pillow` protocol with reserved path prefix `fonts/<font_id>` jailed to `app_data_dir/fonts/` — do not invent a second scheme; do not ship font bytes over IPC (D-30).
</decision_notes>

<tasks>

<task type="auto">
  <name>Task 1: Exact-pin vitest + pure reading helpers with unit tests</name>
  <files>package.json, pnpm-lock.yaml, vitest.config.ts, src/reader/apply-reading-styles.ts, src/reader/apply-reading-styles.test.ts, src/reader/tap-zones.ts, src/reader/tap-zones.test.ts, src/reader/toc.ts, src/reader/toc.test.ts, src/reader/search-opts.ts, src/reader/search-opts.test.ts</files>
  <read_first>
    - .planning/phases/02-epub-reading-core/02-RESEARCH.md §Pattern 1–4, §Wave 0 Gaps, §Open Questions (vitest)
    - .planning/phases/02-epub-reading-core/02-UI-SPEC.md Typography + Color page tokens + Immersive tap zones (33/34/33)
    - package.json (exact-pin rule — no ^/~)
  </read_first>
  <action>
    Add exact-pinned devDependencies only (no runtime packages): `vitest` at an exact current 3.x version (e.g. `3.2.4` — pin whatever `pnpm add -D vitest@3.2.4` resolves without ranges). Add scripts: `"test": "vitest run"`, `"test:watch": "vitest"` is optional but do not use watch in verify. Create `vitest.config.ts` with environment `node`, include `src/**/*.test.ts`, no coverage requirement.

    Create pure helper modules (no React, no Tauri imports):

    1. `src/reader/apply-reading-styles.ts` (D-22 defaults, READ-01/02/03 foundation):
       - Export type `ReadingMode = "paginate" | "scroll"`, `ReadingTheme = "day" | "night" | "sepia"`, `ReadingPrefs` with fields: mode, theme, fontFamilyKey, fontSizePx, lineHeight, marginPx, activeFontId (string | null).
       - Export `DEFAULT_PREFS` matching UI-SPEC: paginate, day, fontFamilyKey `"system"`, fontSizePx 18, lineHeight 1.75, marginPx 24, activeFontId null.
       - Export `SYSTEM_CJK_STACK` exactly: `system-ui, "PingFang SC", "Hiragino Sans GB", "Microsoft YaHei", "Noto Sans CJK SC", sans-serif`.
       - Export `PAGE_COLORS` map for day/night/sepia page bg/fg per UI-SPEC (`#FFFEF9`/`#1C1915`, `#12100E`/`#E8E2D6`, `#F4ECD8`/`#3B2F1E`).
       - Export `flowAttr(mode: ReadingMode): "paginated" | "scrolled"` — scroll→scrolled, paginate→paginated (READ-01; D-22).
       - Export `buildReadingCss(prefs: ReadingPrefs, fontFaceCss: string, fontFamilyCss: string): string` that concatenates fontFaceCss + html/body rules using page colors from theme, font-size px, line-height, and the provided fontFamilyCss (do not hardcode Geist). Include margin is applied via renderer attribute later, not necessarily in body padding here — document in comment that margin uses `renderer.setAttribute("margin", ...)`.

    2. `src/reader/tap-zones.ts` (READ-04):
       - Export `TapZone = "left" | "center" | "right"`.
       - Export `resolveTapZone(clientX: number, width: number): TapZone` using left 33%, center 34%, right 33% (UI-SPEC).
       - Export `tapZoneAction(zone: TapZone, mode: ReadingMode): "prev" | "next" | "toggle-chrome"` — in scroll mode any zone → toggle-chrome; in paginate left→prev, right→next, center→toggle-chrome (D-33 related, READ-04).

    3. `src/reader/toc.ts` (READ-05):
       - Export `TocItem` `{ label: string; href: string; subitems?: TocItem[] }`.
       - Export `flattenToc(items: TocItem[], depth = 0): { label: string; href: string; depth: number }[]` depth-first with indent depth.

    4. `src/reader/search-opts.ts` (READ-07, D-31/D-34):
       - Export `SEARCH_DEBOUNCE_MS = 250` (within 200–300 per D-34).
       - Export `buildSearchOpts(query: string): { query: string }` — do NOT set matchWholeWords (must stay grapheme/CJK friendly per D-31). Trim query; empty string allowed for callers to skip.

    Write colocated `*.test.ts` covering: flowAttr both modes; buildReadingCss contains font-size/line-height/page colors for each theme; resolveTapZone boundaries; tapZoneAction scroll vs paginate; flattenToc nesting; buildSearchOpts does not include matchWholeWords.

    Run `pnpm install` so lockfile updates. Do NOT add floating ranges.
  </action>
  <verify>
    <automated>pnpm test && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains exact `"vitest":` version without ^ or ~; scripts.test is `vitest run`
    - vitest.config.ts exists and references vitest
    - apply-reading-styles.ts exports flowAttr, buildReadingCss, DEFAULT_PREFS, SYSTEM_CJK_STACK, PAGE_COLORS
    - flowAttr("scroll") maps to scrolled and flowAttr("paginate") maps to paginated (grep or tests)
    - buildSearchOpts source does not contain matchWholeWords
    - `pnpm test` exits 0; `pnpm build` exits 0
  </acceptance_criteria>
  <done>Pure helpers + vitest harness green; Wave 0 frontend sampling path exists.</done>
</task>

<task type="auto">
  <name>Task 2: SQL capabilities + expanded Foliate ambient types</name>
  <files>src-tauri/capabilities/default.json, src/reader/foliate-types.ts</files>
  <read_first>
    - src-tauri/capabilities/default.json
    - src-tauri/capabilities/android.json (do not strip SAF perms)
    - src/reader/FoliateView.tsx (current narrow FoliateViewElement)
    - .planning/phases/02-epub-reading-core/02-RESEARCH.md §Pattern 5 capability gap, §Pattern 3/4 APIs
  </read_first>
  <action>
    Update `src-tauri/capabilities/default.json` permissions array to include existing entries plus:
    - `sql:default`
    - `sql:allow-execute`
    Keep `core:default` and `dialog:allow-open`. Do not remove Android SAF capability file. (D-20 readiness; T-02-sql is mitigated later by parameterized queries — capability only unblocks plugin.)

    Create `src/reader/foliate-types.ts` exporting interfaces used by FoliateView and sheets (do not put implementation here):
    - `FoliateRenderer` with next/prev, setAttribute?(name, value), setStyles?(css: string | [string, string]), getAttribute?
    - `FoliateBookTocItem` { label?: string; href?: string; subitems?: FoliateBookTocItem[] }
    - `FoliateBook` { toc?: FoliateBookTocItem[]; rendition?: { layout?: string } }
    - `FoliateViewElement extends HTMLElement` with: open, close?, renderer?, book?, goTo(target: string): Promise<unknown>, goToTextStart(): Promise<unknown>, goLeft(): Promise<unknown>, goRight(): Promise<unknown>, search(opts: { query: string; index?: number }): AsyncGenerator<unknown>, clearSearch(): void
    - `RelocateDetail` with fraction?, cfi?, range?, tocItem?, section?, location?
    - Re-export nothing that imports React.

    Do not yet rewrite FoliateView to use these types (02-01 will import them) — only create the module so later plans have a single contract file.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - default.json permissions list contains both `sql:default` and `sql:allow-execute` (grep)
    - default.json still contains `dialog:allow-open` and `core:default`
    - foliate-types.ts contains string tokens: goToTextStart, clearSearch, AsyncGenerator, setStyles
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>SQL caps granted; foliate engine contract typed for Wave 1+.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebView frontend → tauri-plugin-sql | Prefs/locator SQL will execute from JS once caps land |
| Build graph → new vitest devDep | Dev-only package install |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-sql | Tampering | capabilities + future prefs SQL | mitigate | Grant least privilege sql:default + sql:allow-execute only; later plans MUST use bound `$1` params (never string-concat SQL) |
| T-02-ipc | Information Disclosure | IPC surface | accept | Wave 0 adds no book-byte commands; D-06 still holds |
| T-02-agpl | Tampering (legal) | helpers | mitigate | Clean-room pure helpers only; no Readest source |
| T-02-SC | Tampering | vitest install | mitigate | Exact pin + lockfile; no [ASSUMED]/[SUS] runtime packages (devDep only) |
</threat_model>

<verification>
- `pnpm test` green on pure helpers
- `pnpm build` green
- capabilities grep for sql:default and sql:allow-execute
</verification>

<success_criteria>
- Wave 0 gaps for frontend unit tests, SQL caps, and Foliate ambient types closed
- Downstream plans can import helpers/types without redefining contracts
- No floating version ranges introduced
</success_criteria>

<output>
Create `.planning/phases/02-epub-reading-core/02-00-SUMMARY.md` when done.
</output>
