---
phase: 02-epub-reading-core
plan: 01
type: execute
wave: 1
depends_on: ["02-00"]
files_modified:
  - src/reader/FoliateView.tsx
  - src/reader/ReaderChrome.tsx
  - src/reader/ProgressBar.tsx
  - src/index.css
  - src/App.css
  - src/components/ui/sheet.tsx
  - src/components/ui/slider.tsx
  - src/components/ui/toggle-group.tsx
  - src/components/ui/input.tsx
  - src/components/ui/scroll-area.tsx
  - src/components/ui/separator.tsx
autonomous: true
requirements: [READ-01]
must_haves:
  truths:
    - "User can toggle paginate ↔ scroll without reloading the book (READ-01)"
    - "Reader root uses data-theme and fixed full-viewport flex layout with min-height:0 view host"
    - "Primary 下一页 toolbar button is removed; chrome slots match UI-SPEC (返回/title/目录/搜索/Aa)"
    - "Flow is applied via renderer.setAttribute('flow', ...) using flowAttr helper"
  artifacts:
    - path: "src/reader/FoliateView.tsx"
      provides: "Engine controller + chrome composition root with live flow toggle"
      min_lines: 120
      contains: "setAttribute"
    - path: "src/reader/ReaderChrome.tsx"
      provides: "48px toolbar + progress percent + slot buttons per UI-SPEC"
      contains: "显示设置"
    - path: "src/reader/ProgressBar.tsx"
      provides: "2px accent progress bar under toolbar"
    - path: "src/components/ui/sheet.tsx"
      provides: "shadcn Sheet for later sheets (install now)"
  key_links:
    - from: "src/reader/FoliateView.tsx"
      to: "src/reader/apply-reading-styles.ts"
      via: "flowAttr on mode change"
      pattern: "flowAttr|setAttribute\\([\"']flow"
    - from: "src/reader/FoliateView.tsx"
      to: "src/reader/foliate-types.ts"
      via: "typed FoliateViewElement"
      pattern: "foliate-types"
---

<objective>
Stand up the reading-engine chrome foundation and real-time paginate↔scroll toggle (READ-01). Expand FoliateView into a composition root with UI-SPEC shell structure (toolbar slots, data-theme root, min-height view host), install required shadcn primitives for later sheets, and drive foliate flow exclusively through renderer.setAttribute('flow', …) using the Wave 0 flowAttr helper.

Purpose: First Lithium-parity reading milestone slice — mode switch without reload is the spine every prefs/theme/immersive plan hangs on.
Output: Working reader chrome shell + live flow toggle; shadcn sheet/slider/toggle-group/input/scroll-area/separator present for 02-02..04.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-epub-reading-core/02-CONTEXT.md
@.planning/phases/02-epub-reading-core/02-UI-SPEC.md
@.planning/phases/02-epub-reading-core/02-RESEARCH.md
@.planning/phases/02-epub-reading-core/02-00-PLAN.md
@src/reader/FoliateView.tsx
@src/reader/apply-reading-styles.ts
@src/reader/foliate-types.ts
@src/index.css
@src/App.css
@components.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install official shadcn primitives for reader sheets</name>
  <files>src/components/ui/sheet.tsx, src/components/ui/slider.tsx, src/components/ui/toggle-group.tsx, src/components/ui/input.tsx, src/components/ui/scroll-area.tsx, src/components/ui/separator.tsx, package.json, pnpm-lock.yaml</files>
  <read_first>
    - components.json (style radix-nova; registries empty)
    - .planning/phases/02-epub-reading-core/02-UI-SPEC.md §Component Inventory
    - .planning/phases/02-epub-reading-core/02-RESEARCH.md Installation block
  </read_first>
  <action>
    Generate ONLY official-registry shadcn components (UI-SPEC; no third-party registries). From repo root run (pin CLI already in deps as shadcn 4.13.0):

    `pnpm dlx shadcn@4.13.0 add sheet slider toggle-group input scroll-area separator --yes`

    Do NOT add badge unless already trivial. Ensure components.json `"registries"` stays empty / unchanged. If the generator adds runtime deps, re-pin them to exact versions (no ^/~) and refresh pnpm-lock.yaml. Verify each file lands under `src/components/ui/`. Do not redesign tokens.
  </action>
  <verify>
    <automated>test -f src/components/ui/sheet.tsx && test -f src/components/ui/slider.tsx && test -f src/components/ui/toggle-group.tsx && test -f src/components/ui/input.tsx && test -f src/components/ui/scroll-area.tsx && test -f src/components/ui/separator.tsx && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - Files exist: sheet.tsx, slider.tsx, toggle-group.tsx, input.tsx, scroll-area.tsx, separator.tsx under src/components/ui/
    - components.json does not introduce third-party registries
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>shadcn primitives installed for Settings/TOC/Search sheets.</done>
</task>

<task type="auto">
  <name>Task 2: Reader shell structure + live flow toggle (READ-01)</name>
  <files>src/reader/FoliateView.tsx, src/reader/ReaderChrome.tsx, src/reader/ProgressBar.tsx, src/index.css, src/App.css</files>
  <read_first>
    - src/reader/FoliateView.tsx (full)
    - src/reader/apply-reading-styles.ts (flowAttr, DEFAULT_PREFS)
    - src/reader/foliate-types.ts
    - src/App.css (existing .reader / .reader__view height rules)
    - .planning/phases/02-epub-reading-core/02-UI-SPEC.md Layout Contract + Toolbar + Copywriting
    - 02-CONTEXT.md D-01..D-13 carry, D-22 defaults
  </read_first>
  <action>
    Implement the UI-SPEC reader root without full settings/TOC/search logic yet (stubs OK for sheet open handlers):

    **CSS (dual-layer; D-03 themes prep):**
    - In `src/index.css` add `.reader[data-theme="day|night|sepia"]` CSS variables exactly per UI-SPEC Token Mapping Notes (`--reader-bg`, `--reader-chrome`, `--reader-accent`, `--reader-fg`, `--reader-muted`, `--reader-border`, `--page-bg`, `--page-fg`).
    - Keep home shell on shadcn `:root`/`.dark` (dual-layer rule).
    - In App.css (or index.css) enforce: `.reader { position: fixed; inset: 0; display: flex; flex-direction: column; background: var(--reader-bg); color: var(--reader-fg); }` ; `.reader__view { flex: 1 1 auto; min-height: 0; position: relative; }` ; `foliate-view { display: block; width: 100%; height: 100%; }` — preserve Phase 1 height lesson.

    **ProgressBar.tsx:** 2px track using `--reader-chrome` / fill `--reader-accent`; accepts `fraction: number` (0..1).

    **ReaderChrome.tsx:** 48px toolbar with safe-area padding; slots L→R per UI-SPEC:
    - Back button aria-label/title 返回 → calls onBack
    - Book title (prop `title`, truncate; default 示例书籍 or empty)
    - Progress caption `进度 {n}%` when fraction known
    - Icon buttons (lucide ChevronLeft, List, Search, Type or text Aa): 目录, 搜索, 显示设置 — 44×44 min hit; aria-label 简体中文
    - REMOVE primary 下一页 button from chrome (UI-SPEC).
    - Accept props: title, fraction, chromeVisible, onBack, onOpenToc, onOpenSearch, onOpenSettings
    - When chromeVisible false, toolbar+progress bar not rendered (or visually hidden) — immersive prep for 02-03; for this plan default chromeVisible=true so mode toggle is reachable via Settings stub.

    **FoliateView.tsx rewrite (keep DRM + pillow fetch; clean-room, D-06/D-10):**
    - Import FoliateViewElement types from foliate-types; import flowAttr + DEFAULT_PREFS from apply-reading-styles.
    - Local React state `mode: ReadingMode` default DEFAULT_PREFS.mode; `theme` default day for data-theme attribute on root.
    - After `view.open`, call `view.renderer?.setAttribute?.("flow", flowAttr(mode))` then `goToTextStart()` instead of bare `renderer.next()` (aligns with D-25 early; full restore in 02-03).
    - Provide a temporary Settings entry: either wire shadcn Sheet bottom with only 阅读模式 ToggleGroup (分页|滚动) applying immediately via setAttribute flow without reload (READ-01, D-22), OR a minimal inline control — prefer real ToggleGroup in bottom Sheet titled 显示设置 so 02-02 extends sections rather than rebuilds.
    - On mode change: `viewRef.current?.renderer?.setAttribute?.("flow", flowAttr(next))` live; no view.open again.
    - Keep ErrorCard soft-fail path; status loading shows 加载中….
    - Toolbar back uses 返回 copy (not only 关闭) per UI-SPEC; onClose still returns home.
    - Do NOT implement prefs SQL yet (02-02). Do NOT implement tap zones fully (02-03) but structure DOM so ReaderChrome + view host exist.
    - FXL note: if `view.book?.rendition?.layout === "pre-paginated"`, disable mode toggle (read-only UI) — soft product rule from research.

    Do not copy Readest AGPL (T-02-agpl / DEC-001).
  </action>
  <verify>
    <automated>pnpm test && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - FoliateView.tsx contains setAttribute and flowAttr (or "flow" attribute string) and goToTextStart
    - FoliateView / ReaderChrome do not render a primary button labeled 下一页
    - ReaderChrome contains 显示设置 and 目录 and 搜索 and 返回 (grep 简体中文)
    - index.css or App.css contains data-theme="day" (or [data-theme="day"]) reader tokens
    - reader__view / min-height: 0 preserved
    - `pnpm test` and `pnpm build` exit 0
  </acceptance_criteria>
  <done>Live paginate↔scroll toggle + UI-SPEC chrome foundation shipping.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebView → pillow:// | Book bytes still only via protocol |
| User gestures → foliate renderer | Mode attribute changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-ipc | Information Disclosure | FoliateView open path | mitigate | Keep fetch(pillowUrl); no invoke of book bytes (D-06) — grep gate no command returns bytes |
| T-02-zip | Denial / Tampering | open failures | mitigate | Existing check_protection + try/catch → ErrorCard soft-fail |
| T-02-agpl | Tampering (legal) | Reader chrome | mitigate | Implement from UI-SPEC + foliate MIT APIs only; no Readest source |
| T-02-path | Tampering | book id | mitigate | Continue registry-scoped ids only; no new path params |
</threat_model>

<verification>
- `pnpm test` (helpers still green)
- `pnpm build`
- Grep: setAttribute flow, no 下一页 primary chrome, 显示设置 present
</verification>

<success_criteria>
- READ-01: mode toggle applies live via flow attribute without reopening book
- Reader shell matches UI-SPEC structure (toolbar slots, data-theme, min-height host)
- shadcn primitives ready for prefs/TOC/search sheets
</success_criteria>

<output>
Create `.planning/phases/02-epub-reading-core/02-01-SUMMARY.md` when done.
</output>
