---
phase: 05-annotations-composite-locator
plan: 05-06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/reader/reading-position.ts
  - src/reader/reading-position.test.ts
  - src/reader/FoliateView.tsx
autonomous: true
gap_closure: true
requirements: []  # DEFECT 2 稳定性修复 — 不绑定任何 v1 需求（PDF 阅读在 v1 范围外，仅顺带渲染）
must_haves:
  truths:
    - "打开 PDF 时 logcat 不再刷屏 `Te.id.endsWith is not a function`；spine/outline 解析遇到数字 ref id 会跳过而非抛错"
    - "EPUB 的 TOC/搜索/CFI→spine 解析行为不变；PDF 正文照常渲染"
  artifacts:
    - path: "src/reader/reading-position.ts"
      provides: "纯函数 matchSectionByHref(sectionId, hrefPath)，非字符串 id 直接返回 false"
      contains: "matchSectionByHref"
    - path: "src/reader/reading-position.test.ts"
      provides: "数字 ref id 不抛错的单测用例"
      contains: "matchSectionByHref"
  key_links:
    - from: "src/reader/FoliateView.tsx"
      to: "src/reader/reading-position.ts"
      via: "resolveSpineIndex 的 path-match 分支改调 matchSectionByHref"
      pattern: "matchSectionByHref"
---

<objective>
修复 DEFECT 2：分页/滚动 spine 解析在遇到 PDF 的数字 section ref id（`{num,gen}`）时把它当字符串调 `.endsWith`，抛 `Te.id.endsWith is not a function` 刷屏 logcat，并可能影响 PDF 大纲跳转。把 path-match 判定抽成一个带类型守卫的纯函数并补单测。

Purpose: PDF 正文本来就能渲染，此缺陷只是日志噪音 + 潜在 TOC 跳转失效，但它掩盖真实报错、干扰设备门排查。属桌面/单测可验证，先落地让 05-08 设备复验的 logcat 干净。
Output: `reading-position.ts` 新增导出纯函数 `matchSectionByHref`；`reading-position.test.ts` 补数字-ref 用例；`FoliateView.tsx` 的 `resolveSpineIndex` 改用该函数。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-annotations-composite-locator/05-DEVICE-GATE-FINDINGS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: reading-position.ts 新增 matchSectionByHref 纯函数 + 单测</name>
  <files>src/reader/reading-position.ts, src/reader/reading-position.test.ts</files>
  <read_first>
    - src/reader/reading-position.ts（spine 辅助函数的既有归属：`spineToLinearIndex` L116、`positionToLocatorCfi` 等；在此追加导出）
    - src/reader/reading-position.test.ts（既有 spine 单测结构，追加用例）
    - src/reader/ContinuousScrollStream.tsx L74-82（`ContinuousSection.id?: string` 类型声明为字符串，但 PDF 运行时给的是数字 ref `{num,gen}` —— 这就是类型谎言的根因）
    - .planning/phases/05-annotations-composite-locator/05-DEVICE-GATE-FINDINGS.md（DEFECT 2 段：根因确认在 resolveSpineIndex 的 path-match 分支）
  </read_first>
  <behavior>
    - matchSectionByHref({num:1439,gen:0}, "chapter1.html") → false，且不抛错（PDF 数字 ref 守卫）
    - matchSectionByHref(undefined, "chapter1.html") → false
    - matchSectionByHref("OEBPS/chapter1.html", "chapter1.html") → true（sectionId.endsWith(hrefPath)）
    - matchSectionByHref("chapter1.html", "OEBPS/chapter1.html") → true（hrefPath.endsWith(sectionId)）
    - matchSectionByHref("chapter1.html", "chapter1.html") → true（全等）
    - matchSectionByHref("chapter2.html", "chapter1.html") → false
  </behavior>
  <action>
    在 `reading-position.ts` 追加导出纯函数 `matchSectionByHref(sectionId: unknown, hrefPath: string): boolean`：第一行守卫 `if (typeof sectionId !== "string") return false;`（拦掉 PDF 的数字 ref `{num,gen}`，它没有 `.endsWith`），否则返回 `sectionId === hrefPath || sectionId.endsWith(hrefPath) || hrefPath.endsWith(sectionId)`。参数取 `unknown` 而非 `ContinuousSection`，避免把组件类型耦合进纯 helper。在 `reading-position.test.ts` 按 `<behavior>` 六条追加断言（含数字-ref 不抛错的 case）。不改动 `reading-position.ts` 其它导出。
  </action>
  <verify>
    <automated>cd /d/Github/Pillowtome && pnpm test -- reading-position && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `reading-position.ts` 导出 `matchSectionByHref`，首行为 `typeof sectionId !== "string"` 守卫。
    - `reading-position.test.ts` 含 `{num:1439,gen:0}` 输入用例且断言返回 false（不抛错）。
    - `pnpm test -- reading-position` 全绿；`pnpm exec tsc --noEmit` 干净。
  </acceptance_criteria>
  <done>纯函数 matchSectionByHref 落地并被数字-ref 单测覆盖，数字 id 静默跳过不抛错。</done>
</task>

<task type="auto">
  <name>Task 2: FoliateView.resolveSpineIndex 改用 matchSectionByHref</name>
  <files>src/reader/FoliateView.tsx</files>
  <read_first>
    - src/reader/FoliateView.tsx L969-1001（`resolveSpineIndex`：L984-994 的 path-match 分支当前内联 `s.id.endsWith(...)` / `hrefPath.endsWith(s.id)`，无守卫）
    - src/reader/reading-position.ts（Task 1 新增的 `matchSectionByHref` 导出）
  </read_first>
  <action>
    在 `FoliateView.tsx` 从 `./reading-position` 引入 `matchSectionByHref`。把 `resolveSpineIndex`（L987-994）里 `continuousSections.find((s) => { if (!s.id) return false; return s.id === hrefPath || s.id.endsWith(hrefPath) || hrefPath.endsWith(s.id); })` 整段判定替换为 `continuousSections.find((s) => matchSectionByHref(s.id, hrefPath))`。**不要**动前面 `resolveNavigation` / `resolveHref` / `resolveCFI` 的候选循环（L974-983），那部分与本缺陷无关。保留 catch 里的 `console.warn` 兜底。
  </action>
  <verify>
    <automated>cd /d/Github/Pillowtome && grep -n "s.id.endsWith" src/reader/FoliateView.tsx; pnpm exec tsc --noEmit && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "s.id.endsWith" src/reader/FoliateView.tsx` 无输出（无守卫的直接调用已消除）。
    - `resolveSpineIndex` 的 path-match 分支调用 `matchSectionByHref(s.id, hrefPath)`。
    - `pnpm exec tsc --noEmit` 干净；`pnpm build` 成功。
  </acceptance_criteria>
  <done>PDF 数字 ref id 经守卫静默跳过；`Te.id.endsWith is not a function` 不再从 resolveSpineIndex 抛出。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| foliate section 元数据 → spine 解析 | section id 的运行时类型不由本代码控制（EPUB=字符串 href，PDF=数字 ref），是不可信输入 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-15 | Denial of Service | resolveSpineIndex 对非字符串 id 调 `.endsWith` 抛错 → logcat 刷屏、可能中断 TOC 解析 | mitigate | `typeof sectionId !== "string"` 守卫 + 数字-ref 单测；纯函数集中一处修，覆盖所有调用方 |
| T-05-SC | Tampering | npm/cargo installs | accept | 本计划零新依赖，仅改现有 TS |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (plan 05-06)

- `src/reader/reading-position.ts` → 新增导出纯函数 `matchSectionByHref(sectionId: unknown, hrefPath: string): boolean`（`typeof` 守卫）
- `src/reader/reading-position.test.ts` → 新增数字-ref（`{num,gen}`）不抛错 + 前后缀匹配用例
- `src/reader/FoliateView.tsx` → `resolveSpineIndex` path-match 分支改调 `matchSectionByHref`（移除无守卫的 `s.id.endsWith`）
</artifacts_produced>

<verification>
- `pnpm test -- reading-position` 覆盖 matchSectionByHref 全部分支（含数字-ref 守卫）。
- `grep -n "s.id.endsWith" src/reader/FoliateView.tsx` 无输出。
- `pnpm exec tsc --noEmit` + `pnpm build` 绿。
</verification>

<success_criteria>
- 打开 PDF 不再从 spine 解析抛 `endsWith` TypeError（05-08 设备复验时 logcat 干净）。
- EPUB path-match 行为不变（前缀/后缀/全等三种匹配均由单测锁定）。
</success_criteria>

<output>
Create `.planning/phases/05-annotations-composite-locator/05-06-SUMMARY.md` when done.
</output>
