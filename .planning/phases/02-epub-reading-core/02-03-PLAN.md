---
phase: 02-epub-reading-core
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - src/reader/ReaderTapZones.tsx
  - src/reader/TocSheet.tsx
  - src/reader/locator-store.ts
  - src/reader/FoliateView.tsx
  - src/reader/ReaderChrome.tsx
  - src-tauri/src/commands.rs
  - src-tauri/src/lib.rs
autonomous: true
requirements: [READ-04, READ-05]
must_haves:
  truths:
    - "Reader defaults to immersive chrome-hidden when status becomes reading (READ-04)"
    - "Center 34% toggles chrome; left/right 33% page-turn in paginated mode; scroll mode does not hijack scroll (READ-04)"
    - "TOC sheet lists book.toc nested items and goTo(href) on tap (READ-05)"
    - "Reading position debounced upsert on relocate (~500ms) and flush on unmount (D-23, D-24)"
    - "Restore uses goTo(cfi) or goToTextStart on invalid/missing (D-25)"
    - "Desktop Esc / arrows / PageUp/PageDown wired (D-33 partial; search shortcut in 02-04)"
  artifacts:
    - path: "src/reader/ReaderTapZones.tsx"
      provides: "Invisible L/C/R hit regions using tap-zones helpers"
      contains: "resolveTapZone|tapZoneAction"
    - path: "src/reader/TocSheet.tsx"
      provides: "目录 sheet/drawer with flattenToc"
      contains: "目录"
    - path: "src/reader/locator-store.ts"
      provides: "load/upsert locator by work_id with bound SQL"
      contains: "ON CONFLICT"
    - path: "src-tauri/src/commands.rs"
      provides: "ensure_work command for work_id mapping"
      contains: "ensure_work"
  key_links:
    - from: "src/reader/FoliateView.tsx"
      to: "relocate event"
      via: "debounced locator upsert + progress UI"
      pattern: "relocate|upsertLocator|LOCATOR_DEBOUNCE"
    - from: "src/reader/TocSheet.tsx"
      to: "view.goTo"
      via: "href navigation"
      pattern: "goTo"
---

<objective>
Ship immersive reading chrome with tap zones (READ-04), TOC jump (READ-05), composite locator progress persist/restore (D-23..D-26), and desktop keyboard basics for page turn + Esc (D-33). Map open book id → work_id via ensure_work without blocking open.

Purpose: Completes navigable immersive reading and front-loads P5 locator usage for progress only.
Output: Tap zones, TocSheet, locator-store, ensure_work, FoliateView immersive + restore/flush lifecycle.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-epub-reading-core/02-CONTEXT.md
@.planning/phases/02-epub-reading-core/02-UI-SPEC.md
@.planning/phases/02-epub-reading-core/02-RESEARCH.md
@src/reader/FoliateView.tsx
@src/reader/tap-zones.ts
@src/reader/toc.ts
@src/reader/foliate-types.ts
@src-tauri/src/commands.rs
@src-tauri/src/migrations.rs
@core/src/locator.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: ensure_work + locator-store (D-23..D-26)</name>
  <files>src-tauri/src/commands.rs, src-tauri/src/lib.rs, src/reader/locator-store.ts</files>
  <read_first>
    - src-tauri/src/commands.rs (check_protection / resolve_bytes patterns)
    - src-tauri/src/lib.rs invoke_handler
    - core/src/locator.rs (composite Locator shape)
    - 02-CONTEXT.md D-23..D-26
    - 02-RESEARCH.md Pattern 3 + work_id mapping
    - SCHEMA_V2 unique index on locator(work_id) from 02-02
  </read_first>
  <action>
    **Rust `ensure_work` (D-26):**
    - New command `ensure_work(id: String, ...) -> Result<EnsureWorkResult, String>` where result is `{ workId: string }` (camelCase serde).
    - Resolve source via SourceRegistry; read bytes in Rust (never return bytes — D-06).
    - Prefer blake3 content hash via existing core helpers if available (`EpubPublication::from_bytes` / blake3 used in core); INSERT OR IGNORE into `work(work_id, content_hash, format, created_at)`.
    - work_id strategy: stable UUID string derived from content_hash when possible (e.g. UUID v5-style or store `work_id = content_hash` hex if UUID generation awkward — document choice in code comment). Fallback: deterministic id `work-{registry_id}` when hash fails — must not block open (D-26).
    - Use tauri-plugin-sql from Rust only if already patterned; otherwise compute hash in Rust and return `{ workId, contentHash }` for frontend to INSERT via plugin-sql. Prefer: return workId + contentHash; frontend inserts with bound params if Rust SQL not wired — pick one path and keep book bytes off IPC.
    - Recommended clean path: Rust computes blake3 + work_id string, returns only `{ workId, contentHash }`; frontend `INSERT OR IGNORE INTO work ...` via plugin-sql. Still name command `ensure_work`.
    - Register in generate_handler.

    **locator-store.ts (D-23, D-24, D-25):**
    - `LOCATOR_DEBOUNCE_MS = 500`.
    - `loadLocator(workId): Promise<{ cfi, progress_fraction, text_pre, text_exact, text_post } | null>`
    - `upsertLocator(row): Promise<void>` using `INSERT INTO locator (...) VALUES (...) ON CONFLICT(work_id) DO UPDATE SET cfi=..., progress_fraction=..., text_*, updated_at=...` with bound `$1` params (T-02-sql). Relies on idx_locator_work_id from v2.
    - text_exact from range.toString() trimmed/windowed; pre/post may be empty in P2 (research A1).
    - Export helper to map relocate detail → row.

    Unit-test pure mapping if extracted; otherwise rely on build + cargo tests for ensure_work pure hash path if added.
  </action>
  <verify>
    <automated>cargo test --workspace && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - commands.rs or lib.rs registers ensure_work
    - locator-store.ts contains ON CONFLICT and LOCATOR_DEBOUNCE or 500
    - No function returns book bytes to frontend (grep commands for Vec<u8> return types remains absent on new cmds)
    - cargo test --workspace and pnpm build exit 0
  </acceptance_criteria>
  <done>work_id mapping + locator upsert/load ready.</done>
</task>

<task type="auto">
  <name>Task 2: Immersive chrome, tap zones, TOC, keyboard, restore/flush</name>
  <files>src/reader/ReaderTapZones.tsx, src/reader/TocSheet.tsx, src/reader/FoliateView.tsx, src/reader/ReaderChrome.tsx</files>
  <read_first>
    - 02-UI-SPEC.md Immersive mode + TOC sheet + gesture priority + empty TOC copy
    - src/reader/tap-zones.ts, toc.ts
    - src/reader/FoliateView.tsx (post-02-02 prefs)
    - 02-CONTEXT.md D-24, D-25, D-33
  </read_first>
  <action>
    **ReaderTapZones.tsx (READ-04):**
    - Absolute full-size transparent overlay over reader__view when appropriate.
    - On pointer up (not multi-touch): resolveTapZone(x, width) → tapZoneAction(zone, mode) → callback onAction("prev"|"next"|"toggle-chrome").
    - Paginated: left/right call view.goLeft/goRight (or renderer prev/next); center toggles chrome.
    - Scroll mode: all zones toggle chrome only — do not call preventDefault on touchmove (native scroll primary).
    - pointer-events: when a sheet is open, disable zones.

    **Immersive state (READ-04):**
    - When status becomes `reading`, set chromeVisible=false by default.
    - Center toggle shows/hides ReaderChrome + ProgressBar.
    - Optional 3s auto-hide: discretion — may skip (CONTEXT). If implemented, cancel on sheet open.
    - Sheets still overlay when opened from chrome; opening a sheet may briefly show chrome or open over immersive — prefer: opening TOC/Search/Settings sets chrome visible enough to dismiss, or opens sheet while chrome hidden (UI-SPEC: sheets still overlay).

    **TocSheet.tsx (READ-05):**
    - Title 目录.
    - Prefer left Sheet drawer when width ≥768px, bottom sheet on phone (UI-SPEC / discretion implement matchMedia).
    - Flatten view.book?.toc via flattenToc; indent by depth (md per level); min 44px rows.
    - Active item: optional match by relocate tocItem label — accent + 2px left bar if feasible; else skip active highlight if tocItem shape unclear.
    - Tap → await view.goTo(href) → close sheet.
    - Empty: 暂无目录 / 这本书没有提供目录信息.

    **FoliateView lifecycle:**
    - After open + prefs apply: ensure_work(id) → loadLocator(workId) → if cfi then goTo(cfi) else goToTextStart(); if goTo fails → goToTextStart (D-25). No modal.
    - relocate listener: update progress UI from e.detail.fraction (view-level whole-book fraction — Pitfall 4); debounce upsertLocator 500ms (D-24).
    - On unmount / onClose: force flush pending locator write (D-24).
    - Wire toolbar 目录 → TocSheet; keep Settings from 02-02.
    - Desktop keydown (D-33): ArrowLeft/PageUp → goLeft; ArrowRight/PageDown → goRight; Escape → if sheet open close it else show chrome if immersive. Do not invent vim keys. Search shortcut deferred to 02-04 if SearchSheet not present yet — if easy, stub openSearch callback prop.
    - Remove any remaining primary 下一页 control.

    ProgressBar + caption 进度 {n}% stay in sync with relocate fraction.
  </action>
  <verify>
    <automated>pnpm test && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - ReaderTapZones.tsx exists and references tapZoneAction or resolveTapZone
    - TocSheet contains 目录 and 暂无目录
    - FoliateView contains goToTextStart and ensure_work (invoke) and LOCATOR debounce or upsertLocator
    - FoliateView or ReaderChrome handles Escape or keydown ArrowLeft
    - `pnpm test` and `pnpm build` exit 0
  </acceptance_criteria>
  <done>Immersive + TOC + locator restore/persist + desktop keys shipped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebView → SQL locator/work tables | User reading position + work identity |
| WebView → ensure_work IPC | Registry id in; hash/workId out — no bytes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-sql | Tampering | locator-store / work insert | mitigate | Bound `$n` params only; work_id from hash/registry not free-form path |
| T-02-ipc | Information Disclosure | ensure_work | mitigate | Return only workId/contentHash strings; never book bytes (D-06) |
| T-02-path | Tampering | ensure_work id | mitigate | Resolve only via SourceRegistry + sanitize_id |
| T-02-zip | Denial | open/restore | mitigate | Soft-fail ErrorCard; invalid CFI → goToTextStart (D-25) |
| T-02-agpl | Tampering (legal) | TOC/immersive UI | mitigate | Clean-room from UI-SPEC + foliate APIs |
</threat_model>

<verification>
- cargo test --workspace
- pnpm test + pnpm build
- Grep immersive default, goTo, upsert locator, ensure_work
</verification>

<success_criteria>
- READ-04 immersive + tap zones implemented per UI-SPEC
- READ-05 TOC jump via goTo
- D-23..D-26 progress persist/restore path complete
- D-33 page keys + Esc implemented
</success_criteria>

<output>
Create `.planning/phases/02-epub-reading-core/02-03-SUMMARY.md` when done.
</output>
