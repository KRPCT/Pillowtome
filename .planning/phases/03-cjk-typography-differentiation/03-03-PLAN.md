---
phase: 03-cjk-typography-differentiation
plan: 03
type: execute
wave: 2
depends_on: ["03-00", "03-01"]
files_modified:
  - src-tauri/assets/fonts/noto-cjk/LICENSE
  - src-tauri/assets/fonts/noto-cjk/NOTICE
  - src-tauri/assets/fonts/noto-cjk
  - src-tauri/src/lib.rs
  - src-tauri/src/fonts.rs
  - src-tauri/src/protocol.rs
  - src/reader/fonts.ts
  - src/reader/fonts.test.ts
  - tests/fixtures/cjk/coverage-sheet.html
  - scripts/cjk-golden.mjs
  - package.json
  - pnpm-lock.yaml
autonomous: true
requirements: [CJK-05]
must_haves:
  truths:
    - "Noto Sans CJK SC + TC OFL assets ship in-app (no cloud download) with LICENSE/NOTICE (D-43, D-44, D-45, DEC-001)"
    - "Bundled faces materialize into app_data/fonts as safe ids bundled-noto-sc / bundled-noto-tc and serve via pillow fonts path (D-06 font bytes not IPC)"
    - "font-family stack order is custom? → PillowBundledCJK → system CJK stack including TC names (D-47)"
    - "Coverage sheet + golden harness can capture Blink and WebKit family screenshots for no-tofu / no ransom-note gate (D-46, DEC-002)"
    - "Incomplete custom face does not drop CJK coverage — bundled remains in fallback chain"
  artifacts:
    - path: "src-tauri/assets/fonts/noto-cjk/LICENSE"
      provides: "SIL OFL 1.1 audit trail"
      contains: "SIL OPEN FONT LICENSE"
    - path: "src/reader/fonts.ts"
      provides: "buildBundledCjkFontFaceCss + fontFamilyCssFor stack with PillowBundledCJK"
      exports: ["BUNDLED_CJK_FAMILY", "buildBundledCjkFontFaceCss", "fontFamilyCssFor"]
    - path: "src-tauri/src/fonts.rs"
      provides: "bundled materialize + safe id resolve; custom count excludes bundled-*"
    - path: "scripts/cjk-golden.mjs"
      provides: "Dual-engine golden harness entry (Blink + WebKit)"
    - path: "tests/fixtures/cjk/coverage-sheet.html"
      provides: "Glyph coverage corpus for golden + manual"
  key_links:
    - from: "src/reader/fonts.ts"
      to: "pillowFontUrl(bundled-noto-sc|bundled-noto-tc)"
      via: "@font-face src url"
      pattern: "bundled-noto-sc|bundled-noto-tc|PillowBundledCJK"
    - from: "src-tauri/src/lib.rs"
      to: "app_data/fonts"
      via: "materialize bundled Noto like sample EPUB"
      pattern: "bundled-noto|noto-cjk|materialize"
    - from: "scripts/cjk-golden.mjs"
      to: "tests/fixtures/cjk/coverage-sheet.html"
      via: "screenshot Blink + WebKit"
      pattern: "coverage-sheet|webkit|chromium|blink"
---

<objective>
Ship bundled Noto Sans CJK (SC+TC) as in-app OFL assets, materialize and serve them through the existing pillow fonts path, lock the coverage-aware fallback stack (custom → bundled → system), and add a Blink+WebKit golden-image / coverage harness so CJK-05 is testable (D-43..D-47, DEC-001/002).

Purpose: Eliminate tofu and ransom-note mixing on non-CN systems; prove engine parity for the product moat.

Output: Noto assets + LICENSE; materialize on setup; fonts.ts stack; Rust safe-id/materialize tests; golden script + fixture baselines path.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-cjk-typography-differentiation/03-CONTEXT.md
@.planning/phases/03-cjk-typography-differentiation/03-RESEARCH.md
@.planning/phases/03-cjk-typography-differentiation/03-PATTERNS.md
@.planning/phases/03-cjk-typography-differentiation/03-VALIDATION.md
@docs/decisions/DEC-001-license-cleanroom.md
@docs/decisions/DEC-002-webview-engine.md
@src/reader/fonts.ts
@src/lib/pillow.ts
@src-tauri/src/fonts.rs
@src-tauri/src/protocol.rs
@src-tauri/src/lib.rs
@tests/fixtures/cjk/coverage-sheet.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bundle Noto Sans CJK SC+TC OFL + materialize + stack order</name>
  <files>src-tauri/assets/fonts/noto-cjk, src-tauri/src/lib.rs, src-tauri/src/fonts.rs, src-tauri/src/protocol.rs, src/reader/fonts.ts, src/reader/fonts.test.ts</files>
  <read_first>
    - src-tauri/src/lib.rs (materialize_sample include_bytes pattern; protocol font branch)
    - src-tauri/src/fonts.rs (is_safe_font_id, resolve_font_path, fonts_dir, MAX_CUSTOM_FONTS, count helpers)
    - src-tauri/src/protocol.rs (parse_font_path, serve_font)
    - src/reader/fonts.ts (buildFontFaceCss, fontFamilyCssFor, BUNDLED_CJK_FAMILY stub)
    - src/lib/pillow.ts (pillowFontUrl)
    - 03-CONTEXT.md D-43..D-47
    - 03-RESEARCH.md Pattern 6 font stack + asset layout + Package Legitimacy (Noto OFL OK)
    - 03-PATTERNS.md fonts.rs / lib.rs / fonts.ts
    - DEC-001 clean-room / license discipline
  </read_first>
  <action>
    **Assets (D-43..D-45, DEC-001):**
    - Pin **Noto Sans CJK** (not Serif) language-specific variable SC + TC pair from `notofonts/noto-cjk` (exact release tag/filenames recorded in SUMMARY).
    - Place under `src-tauri/assets/fonts/noto-cjk/`:
      - SC variable OTF/TTF (e.g. NotoSansCJKsc-VF.otf — use actual pin names)
      - TC variable OTF/TTF
      - `LICENSE` full SIL OFL 1.1 text
      - `NOTICE` short attribution
    - **No** cloud/first-run download. **No** Readest font packs. Size secondary to full SC+TC coverage (D-44/D-45); prefer SC+TC variable pair over Super OTC all-langs if it still fully covers SC+TC.
    - If binaries are too large for a single git commit without LFS, use Git LFS **or** document fetch-into-assets script run at implement time that commits the binaries — still must be in-repo/in-app for release builds, never runtime CDN.

    **Materialize (Android Resource pitfall):**
    - Mirror `materialize_sample`: `include_bytes!` (or resource copy) → write to `app_data_dir/fonts/bundled-noto-sc.otf` and `bundled-noto-tc.otf` (extension must match file; ids **without dots**: `bundled-noto-sc`, `bundled-noto-tc` — pass `is_safe_font_id`).
    - Skip rewrite when on-disk size/hash matches to avoid multi-MB write every launch.
    - Soft-fail: if materialize fails, log warn; system stack still works (D-38 spirit for fonts).

    **fonts.rs:**
    - Ensure resolve works for reserved bundled ids under fonts_dir flat layout (prefer flat so existing `serve_font` unchanged).
    - Exclude `bundled-*` prefix from custom font count toward `MAX_CUSTOM_FONTS` so users still get 20 custom slots.
    - Unit tests: safe ids accept `bundled-noto-sc`; reject `../x`, `foo.bar`; resolve finds materialized test file in tempdir; count excludes bundled.

    **protocol.rs:**
    - Likely no rewrite if flat ids under fonts_dir; keep traversal reject tests green. Do not return font bytes over IPC commands.

    **fonts.ts (D-47 stack):**
    - Implement `buildBundledCjkFontFaceCss()`: two `@font-face` blocks, **same** `font-family: "PillowBundledCJK"`, `src: url("${pillowFontUrl("bundled-noto-sc")}")` and `bundled-noto-tc`, `font-display: swap`. Optional unicode-range if clean.
    - `fontFamilyCssFor(fontFamilyKey, activeFontId)`:
      - system / no custom: `"PillowBundledCJK", system-ui, "PingFang SC", "Hiragino Sans GB", "Microsoft YaHei", "Noto Sans CJK SC", "Noto Sans CJK TC", sans-serif`
      - custom active: `"PillowCustom-{id}", "PillowBundledCJK", …same system tail…`
    - Update `SYSTEM_CJK_STACK` usage: either extend the constant in apply-reading-styles to include `"Noto Sans CJK TC"` **or** keep tail local in fonts.ts — final body stack **must** include TC names (D-44/D-47). Prefer single source of truth.
    - Unit tests: stack order custom → bundled → system; system path starts with PillowBundledCJK; buildBundledCjkFontFaceCss contains both bundled ids and @font-face.

    FoliateView already concatenates `buildBundledCjkFontFaceCss()` from 03-01 — verify no regression. Do not invent a second protocol scheme.
  </action>
  <verify>
    <automated>cargo test --workspace && pnpm test && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - LICENSE under src-tauri/assets/fonts/noto-cjk contains SIL OPEN FONT LICENSE (or OFL)
    - SC and TC font binary files exist under that directory (or documented LFS path present)
    - lib.rs or fonts.rs materializes bundled-noto-sc and bundled-noto-tc into app_data fonts
    - fonts.ts fontFamilyCssFor includes PillowBundledCJK before system faces; custom id first when active
    - buildBundledCjkFontFaceCss references pillowFontUrl bundled ids
    - is_safe_font_id accepts bundled-noto-sc; cargo font tests green
    - No command returns Vec&lt;u8&gt; font bytes to JS
    - `cargo test --workspace`, `pnpm test`, `pnpm build` exit 0
  </acceptance_criteria>
  <done>Bundled Noto SC+TC served via pillow; coverage-aware stack order locked.</done>
</task>

<task type="auto">
  <name>Task 2: Coverage sheet + Blink/WebKit golden harness</name>
  <files>tests/fixtures/cjk/coverage-sheet.html, scripts/cjk-golden.mjs, package.json, pnpm-lock.yaml</files>
  <read_first>
    - tests/fixtures/cjk/coverage-sheet.html (Wave 0 scaffold)
    - tests/fixtures/cjk/kinsoku-samples.html
    - 03-RESEARCH.md Pattern golden harness; Open Questions (CI WebKit availability)
    - 03-CONTEXT.md D-46, DEC-002
    - 03-VALIDATION.md manual + golden gates
    - package.json exact-pin rules
  </read_first>
  <action>
    **Coverage sheet (CJK-05 corpus):**
    - Expand `tests/fixtures/cjk/coverage-sheet.html` to include: frequent SC glyphs, TC-only samples (e.g. 體/國/說), mixed `中文ABC数字123`, punctuation runs `「你好。」『引号』`, and a line that would ransom-note if fallback broken.
    - Use CSS `font-family: "PillowBundledCJK", …system…` note; for harness-without-Tauri, also list Noto Sans CJK SC/TC system names so local engines can still render when bundled URL unavailable.
    - Keep offline — no CDN font links.

    **Golden harness (D-46):**
    - Add exact-pinned **devDependency** Playwright (Apache-2.0 — legitimacy OK) only if needed; pin exact version, no ^/~.
    - Create `scripts/cjk-golden.mjs` (or `.ts` run via node) that:
      1. Launches **Chromium** (Blink family) and **WebKit** project browsers.
      2. Opens coverage-sheet (file URL or local static server).
      3. Writes screenshots under `tests/fixtures/cjk/golden/blink/` and `tests/fixtures/cjk/golden/webkit/` (gitkeep + README note if baselines are machine-generated).
      4. Exits non-zero on crash; optional pixel/SSIM compare against committed baselines when present (discretion on threshold — document in script header). First run may create baselines.
    - package.json script e.g. `"test:cjk-golden": "node scripts/cjk-golden.mjs"`.
    - Document in script comments: CI should at least run Chromium; WebKit required for phase gate on dev machine or GH macOS/WebKit-capable runner (RESEARCH Open Question 3). Windows WebView2 product path still covered by Chromium family for golden.

    **Not in product UI:** no in-app coverage viewer (UI-SPEC).

    **Android device gate (CLAUDE.md):** This plan’s unit/cargo green is insufficient for “font claims complete.” SUMMARY must note that phase verify requires Android emulator smoke: open sample CN EPUB after materialize; bundled face applies without protocol 404. Do not expand READER-POS while testing fonts.
  </action>
  <verify>
    <automated>pnpm test && pnpm build && node scripts/cjk-golden.mjs</automated>
  </verify>
  <acceptance_criteria>
    - coverage-sheet.html contains SC and TC sample text and mixed Latin/digits
    - scripts/cjk-golden.mjs exists and references both chromium/blink and webkit
    - package.json has test:cjk-golden (or equivalent) and any Playwright dep is exact-pinned
    - Golden script runs without throwing on Chromium at minimum; WebKit attempted (skip with explicit non-silent log only if browser not installed — prefer install via playwright install in verify when network allows)
    - `pnpm test` and `pnpm build` exit 0
  </acceptance_criteria>
  <done>CJK-05 bundling + dual-engine golden path exist for phase verification.</done>
</task>

</tasks>

## Artifacts this phase produces

| Symbol / path | Kind | Consumed by |
|---------------|------|-------------|
| Noto Sans CJK SC/TC binaries + OFL LICENSE/NOTICE | assets | materialize + legal audit |
| `bundled-noto-sc` / `bundled-noto-tc` on disk | app_data fonts | pillow://fonts/* |
| `buildBundledCjkFontFaceCss()` real faces | TS | FoliateView CSS |
| `fontFamilyCssFor` D-47 order | TS | body font-family |
| `scripts/cjk-golden.mjs` | CI/dev | phase gate D-46 |
| `tests/fixtures/cjk/golden/{blink,webkit}/` | baselines | visual regression |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bundled font files → app_data/fonts | Trusted pin at build; still path-jailed at serve |
| WebView → pillow://fonts/bundled-* | Font bytes to render docs |
| Playwright/dev script → local filesystem | Dev-only screenshots |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-path | Tampering | font id resolve / protocol | mitigate | is_safe_font_id; canonicalize under app_data/fonts only; reserved flat ids (T-03-path) |
| T-03-font | Denial | bundled + custom fonts | mitigate | Pin known Noto sizes; keep MAX_CUSTOM_FONTS/MAX_FONT_BYTES for imports; exclude bundled from custom count |
| T-03-ipc | Information Disclosure | font commands | mitigate | No invoke returns font bytes; serve via pillow only (D-06) |
| T-03-agpl | Tampering (legal) | font assets | mitigate | Noto OFL only; LICENSE+NOTICE co-located; no Readest packs (DEC-001, T-03-agpl) |
| T-03-SC | Tampering | Playwright devDep | mitigate | Exact pin + lockfile; Apache-2.0 legitimacy OK; devDependency only |
</threat_model>

<verification>
- `cargo test --workspace` (font safe ids / materialize helpers)
- `pnpm test` + `pnpm build`
- `node scripts/cjk-golden.mjs` (Blink required; WebKit when available)
- Phase gate (manual/device): Android emulator open book with bundled face; desktop Blink + WebKit coverage sheet visual
</verification>

<success_criteria>
- CJK-05: bundled SC+TC Noto, coverage-aware stack, golden harness both engine families
- D-43..D-47 and DEC-001/002 honored
- No cloud fonts; no READER-POS scope creep
</success_criteria>

<output>
Create `.planning/phases/03-cjk-typography-differentiation/03-03-SUMMARY.md` when done.
</output>
