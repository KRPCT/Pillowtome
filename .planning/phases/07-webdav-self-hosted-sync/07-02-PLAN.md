---
phase: 07-webdav-self-hosted-sync
plan: 02
type: execute
wave: 2
depends_on: ["07-00", "07-01"]
files_modified:
  - src-tauri/src/sync/reconcile.rs
  - src-tauri/src/sync/mod.rs
  - src-tauri/src/sync/commands.rs
  - src-tauri/src/lib.rs
  - src-tauri/Cargo.toml
  - src-tauri/tests/sync_reconcile.rs
  - src-tauri/tests/sync_e2e.rs
  - Cargo.lock
autonomous: true
requirements: [SYNC-02, SYNC-03, SYNC-05]
must_haves:
  truths:
    - "Remote write conflicts are impossible by construction — each device writes only its own state/<device_id>.json; If-Match is purely a backstop for a same-device dual-open (Pattern 1)"
    - "A half-written remote state file is never read: writes land at a tmp-<uuid> name and are published by atomic MOVE; readers only ever open the final name (Pitfall 5)"
    - "Records merged in from peers are NEVER written to the local change_log — the ledger records local ops only (no clock inflation, no re-push loops)"
    - "No record present on only one side is ever dropped — merge is a set union for annotations and library; same-id/same-revision/different-content lands as a 冲突副本 under a new UUID"
    - "Undo (D-92) restores the exact pre-jump local locator position, stashed as the merge_progress replaced_local byproduct before any overwrite"
  artifacts:
    - path: "src-tauri/src/sync/reconcile.rs"
      provides: "Push path (rebuild DeviceStateFile from SQLite → tmp PUT → conditional MOVE), pull path (PROPFIND ETag map → GET changed → validate → merge → write SQLite), devices-registry upkeep, peer-ETag cache, sync_file_state download-discovery rows for 07-03"
      contains: "tmp-"
    - path: "src-tauri/src/sync/commands.rs"
      provides: "IPC commands part 2: sync_book_opened / sync_book_closed / sync_revert_jump / sync_now / sync_status + unified sync-status event (SOLE emitter — owns the in-memory downloads/uploads percent maps that 07-03 reports into; password never in any payload)"
      exports: ["sync_book_opened", "sync_book_closed", "sync_revert_jump", "sync_now", "sync_status"]
    - path: "src-tauri/tests/sync_reconcile.rs"
      provides: "Off-device coverage: state-builder shape (register/tombstones/file_sync), conditional tmp+MOVE push with 412 retry, pull merge apply incl. undo stash, change_log untouched by pull"
    - path: "src-tauri/tests/sync_e2e.rs"
      provides: "Two-device end-to-end: dual in-memory SqlitePool + wiremock WebDAV fake — union/no-loss, tombstone anti-resurrection, clock-skew determinism, jump undo + revert"
  key_links:
    - from: "src-tauri/src/sync/reconcile.rs"
      to: "SQLite (locator/annotation/library_item/sync_state)"
      via: "the plugin's shared sqlx pool — single SQLite binding (Pitfall 6)"
      pattern: "DbPool::Sqlite"
    - from: "src-tauri/src/sync/reconcile.rs"
      to: "core/src/sync/merge.rs"
      via: "pure merge fns from 07-00 (furthest-progress / OR-Set remove-wins / catalog union)"
      pattern: "merge_progress"
    - from: "src-tauri/src/sync/reconcile.rs"
      to: "WebDAV server"
      via: "agent PUT tmp-<uuid> then MOVE with If-None-Match: * / If-Match: <etag> (412 → re-pull-merge-retry once)"
      pattern: "If-Match"
    - from: "src-tauri/src/sync/commands.rs"
      to: "change_log"
      via: "LOCAL revert row only, monotonic clock inside one atomic INSERT (annotation-store pattern)"
      pattern: "INSERT INTO change_log"
    - from: "src-tauri/src/sync/commands.rs"
      to: "frontend"
      via: "app.emit sync-status unified payload {configured, syncing, lastError, downloads, uploads} on every status transition and transfer progress tick (D-93)"
      pattern: "sync-status"
---

<objective>
Land the **state plane reconcile engine**: the push path that rebuilds this device's self-describing state file from SQLite and publishes it atomically with optimistic-concurrency backstop, the pull path that fetches changed peer state files and merges them into SQLite via the 07-00 pure merge functions, the D-92 undo plumbing, and the IPC commands part 2 that wire it to the reading lifecycle (开书拉 / 合书推 / 手动按钮, D-90/D-91). This is the direct payoff of the phase being "对账 rather than rewrite" — the change_log spine (V1) and annotation/sync_meta (V7) are consumed, never rebuilt.

Purpose: Make reading progress (SYNC-02), annotations (SYNC-03), and the full library catalog (D-102) flow between devices with merge semantics that cannot silently lose data (SYNC-05), and make the furthest-position jump reversible (D-92).
Output: `src-tauri/src/sync/reconcile.rs` + part-2 commands + two off-device test files (reconcile unit/integration, dual-device e2e). No frontend files touched (reader/library wiring is 07-04); no core changes (merge/model are 07-00's).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-webdav-self-hosted-sync/07-CONTEXT.md
@.planning/phases/07-webdav-self-hosted-sync/07-RESEARCH.md
@.planning/phases/07-webdav-self-hosted-sync/07-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: reconcile.rs push-side builder — rebuild this device's DeviceStateFile from SQLite</name>
  <files>src-tauri/src/sync/reconcile.rs, src-tauri/tests/sync_reconcile.rs</files>
  <read_first>
    - src-tauri/src/migrations.rs (authoritative table shapes: `locator` V1 lines 31-39 — one latest row per work, NO device_id column; `annotation` V7 lines 149-167 incl. revision/content_hash/deleted; `library_item` V4 lines 104-118; `sync_meta` V7 id='device' row; `change_log` V1 lines 41-49 — READ nothing from it here, push rebuilds from tables per RESEARCH Pattern 1)
    - src-tauri/src/sync/mod.rs, src-tauri/src/sync/transport.rs, src-tauri/src/sync/credentials.rs (07-01 output: engine assembly, reqwest_dav client construction, `SyncConfigRow` reading sync_config; this plan extends them)
    - core/src/sync/model.rs + core/src/sync/merge.rs (07-00 output: `DeviceStateFile`/`ProgressRec`/`AnnotationRec`/`LibraryRec`/`DeviceRecord` serde types and `merge_progress`/`merge_annotation`/`merge_library` — RESEARCH Pattern 1/2 shapes are the contract)
    - .planning/phases/07-webdav-self-hosted-sync/07-RESEARCH.md (Pattern 1 remote file format — the authoritative JSON; Assumptions Log A4; Responsibility Map row "SQLite 合并写入")
    - src/reader/annotation-store.ts lines 1-20 (hash_algo双算法警告: annotation content_hash is WebCrypto SHA-256 tagged "sha256"; work.content_hash is blake3 — tag annotation recs with hash_algo "sha256" verbatim)
  </read_first>
  <action>
    Create `src-tauri/src/sync/reconcile.rs` (engine internals, `pub(crate)` + test-visible). All SQL goes through the **shared plugin pool** — single SQLite binding (Pitfall 6). Pool resolution helper (extend `src-tauri/src/sync/mod.rs`):

    ```rust
    // A4 — verified against vendored tauri-plugin-sql 2.4.0 source:
    // lib.rs setup() calls `app.manage(DbInstances)` and
    // `pub struct DbInstances(pub RwLock<HashMap<String, DbPool>>)`,
    // `pub enum DbPool { Sqlite(Pool<Sqlite>), .. }` (wrapper.rs).
    pub(crate) async fn sqlite_pool(app: &tauri::AppHandle) -> Result<sqlx::SqlitePool, SyncError> {
        let instances = app.state::<tauri_plugin_sql::DbInstances>();
        let lock = instances.0.read().await;
        match lock.get("sqlite:pillow.db") {
            Some(tauri_plugin_sql::DbPool::Sqlite(pool)) => Ok(pool.clone()),
            _ => Err(SyncError::soft("数据库尚未就绪")),
        }
    }
    ```

    **Runtime A4 verification (FIRST, before relying on the pool):** the off-device tests below prove the SQL with in-memory pools but cannot prove AppHandle wiring; on the first desktop run of Task 4, trigger `sync_status` and confirm the log line `[sync] shared pool acquired` (add it on first successful resolution). If DbInstances proves unreachable at runtime, apply the documented A4 fallback (RESEARCH Assumptions Log): route merge writes through the frontend plugin-sql path — still the single binding, one extra IPC hop — and record the deviation in the SUMMARY.

    Implement in `reconcile.rs`:
    - `pub(crate) async fn build_device_state(pool: &SqlitePool, cfg: &SyncConfigRow, now_ms: i64) -> Result<DeviceStateFile, SyncError>`:
      - `SELECT device_id, logical_clock FROM sync_meta WHERE id='device'` → `device_id`, `clock`.
      - **Progress register** (RESEARCH Pattern 1: register not log): `SELECT work_id, cfi, progress_fraction, text_pre, text_exact, text_post, updated_at FROM locator` → one `ProgressRec` per work_id (UNIQUE index idx_locator_work_id guarantees ≤1 row/work).
      - **Annotations FULL set incl. tombstones**: `SELECT annotation_id, work_id, type, cfi, color, text_pre, text_exact, text_post, progress_fraction, note, created_at, updated_at, revision, content_hash, deleted FROM annotation` (NO `WHERE deleted=0` — tombstones must ride the state file so deletes never resurrect). Each rec carries `hash_algo: "sha256"` per-record (locked warning — never assume one algorithm).
      - **Library full catalog incl. tombstoned rows** (research Q2 ADOPTED — planner decision, do not re-open): `SELECT li.work_id, li.title, li.author, li.imported_at, li.deleted, li.file_sync_enabled, w.content_hash, w.format FROM library_item li JOIN work w ON w.work_id = li.work_id` → one `LibraryRec` per work. For rows with `file_sync_enabled=1`, attach file metadata from `sync_file_state` (`SELECT remote_path, size, hash FROM sync_file_state WHERE work_id=$1 AND direction='upload' AND remote_path IS NOT NULL` — 07-00 DDL vocabulary, NOT `'up'`): emit `file_sync: { enabled: true, remote_path, size, hash }` when a completed upload row exists, else `{ enabled: true }` (upload pending — 07-03's file plane fills it; peer renders per UI-SPEC §3).
      - Assemble `DeviceStateFile { format: 1, device_id, device_name: cfg.device_name, clock, updated_at: now_ms, progress, annotations, library }` (RESEARCH Pattern 1 JSON verbatim).
    - `pub(crate) fn build_device_record(existing: Option<DeviceRecord>, device_id: &str, device_name: &str, now_ms: i64) -> DeviceRecord` — `{device_id, device_name, first_seen: existing.first_seen.unwrap_or(now), last_seen: now}` for `devices/<device_id>.json`.
    - Serialization note: `core` has NO serde_json runtime dep (contract: core gets no new runtime deps), so JSON (de)serialization lives here in src-tauri. Ensure `src-tauri/Cargo.toml` has direct deps `serde_json = "=1.0.150"` and `uuid = { version = "=1.23.4", features = ["v4"] }` (idempotent if 07-00 already added them; both versions are already in the committed Cargo.lock — no new resolution, D-13; commit the updated lock). If 07-01 declared `serde_json` as a **dev**-dependency, this entry UPGRADES it to a runtime dependency — same pin `=1.0.150`, exactly one manifest entry, never a second.
    - Tests `src-tauri/tests/sync_reconcile.rs` (off-device, `sqlx::SqlitePool` against `sqlite::memory:` — same binding family as the plugin, dev-dep already pinned `sqlx =0.8.6`; apply SCHEMA_V1..V8 from `pillowtome_lib::migrations`): seed work + locator + annotations (one live, one `deleted=1`) + library rows (one `file_sync_enabled=1` with a `sync_file_state` row `direction='upload'` carrying a KNOWN remote_path/size/hash triple, one disabled, one `deleted=1` tombstone) → assert: `format==1`; progress register has exactly the latest locator row per work; the tombstoned annotation is present with `deleted==1` and `hash_algo=="sha256"`; the enabled book's `file_sync` block deserializes to EXACTLY the seeded remote_path string, size value, and blake3 hash (value-level asserts — a vocabulary slip like `direction='up'` fails this test); the disabled book emits `enabled:false`; no field named `password`/`secret` appears anywhere in the serialized JSON (V7 threat control); `build_device_record` preserves `first_seen` when an existing record is passed.
  </action>
  <verify>
    <automated>cd /d/Github/Pillowtome && cargo test -p pillowtome --test sync_reconcile build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "build_device_state" src-tauri/src/sync/reconcile.rs` ≥ 1 and `grep -c "pub struct DbInstances\|DbPool::Sqlite" src-tauri/src/sync/*.rs` ≥ 1 (shared-pool access, not a second binding).
    - `grep -c "WHERE deleted=0" src-tauri/src/sync/reconcile.rs` == 0 (tombstones must be read).
    - `grep -ci "password" src-tauri/src/sync/reconcile.rs` == 0 (no credential material in the state builder).
    - Serialized fixture JSON contains `"format":1` and `"hash_algo":"sha256"` (asserted in test).
    - `cargo test -p pillowtome --test sync_reconcile build` green. (MSVC toolchain required — GNU gcc exits 1 silently per STATE.md line 104.)
  </acceptance_criteria>
  <done>The push payload is rebuilt from local tables (never from raw change_log replay), carries progress register + full annotations with tombstones + full catalog with file_sync flags, and its shape is locked by off-device tests.</done>
</task>

<task type="auto">
  <name>Task 2: Conditional atomic push — tmp PUT → conditional MOVE, 412 retry, sync_state persistence</name>
  <files>src-tauri/src/sync/reconcile.rs, src-tauri/tests/sync_reconcile.rs</files>
  <read_first>
    - .planning/phases/07-webdav-self-hosted-sync/07-RESEARCH.md (Pattern 1 conditional semantics; Pattern 3 — reqwest_dav high-level methods take no custom headers, so If-Match/MOVE go through the public `client.agent`; Pitfall 5 tmp+MOVE; Pitfall 8 `Overwrite: T` + ETag-as-opaque-token; A1 degrade note; Pitfall 3 503/429 backoff + 服务器限流，请稍后重试)
    - src-tauri/src/sync/transport.rs (07-01: client construction, agent access, the locked `SyncError::user_message()` Chinese taxonomy — call it, never re-invent error strings)
    - SCHEMA_V8 `sync_state` row shape in src-tauri/src/migrations.rs (landed by 07-00: single row id='state', remote_etag/last_sync_at/last_error/syncing; the peer-ETag cache reuses this table's TEXT PRIMARY KEY with id='peer:<device_id>')
    - docs.rs reqwest_dav 0.3.3 `types::list_cmd::ListFile` (`pub tag: Option<String>` is the ETag; `ListEntity::File` variant) — verified for this plan
  </read_first>
  <action>
    Extend `reconcile.rs`:
    - `pub(crate) async fn push_state_file(client: &reqwest_dav::Client, state_url: &str, tmp_url: &str, body: &[u8], last_etag: Option<&str>) -> Result<String, SyncError>`:
      1. `client.agent.put(tmp_url).header("Content-Type", "application/json").body(body)` — tmp name is `state/<device_id>.json.tmp-<uuid v4>` (uuid dep from Task 1). A crashed push orphans only a tmp file; readers never match `tmp-*` (Pitfall 5).
      2. Publish: `client.agent.request(Method::from_bytes(b"MOVE")?, tmp_url)` → `.header("Destination", state_url)`, `.header("Overwrite", "T")` (Pitfall 8), plus the optimistic-concurrency backstop: `If-None-Match: *` when `last_etag` is None (first-ever write) else `If-Match: "<last_etag>"`. 412 → `SyncError::RemoteChanged`. (Guarded case = same-device dual-open only — cross-device conflicts are impossible by construction, Pattern 1. Per A1, servers that don't honor MOVE conditionals degrade to harmless overwrite of one's own file; the validation matrix notes it.)
      3. New ETag: `client.list(state_url, reqwest_dav::Depth::Zero)` → `ListEntity::File(f) => f.tag` — treat as an opaque equality token, never parse (Pitfall 8). Missing ETag → store empty + proceed (A1 degrade).
    - `pub(crate) async fn reconcile_push(pool, client, cfg) -> Result<(), SyncError>`: read `remote_etag` from `sync_state` (id='state'); build state (Task 1) → serialize → `push_state_file`; on `RemoteChanged` run the pull path (Task 3) once, rebuild, retry ONCE — no loop. On success: `UPDATE sync_state SET remote_etag=$1, last_sync_at=$2, last_error=NULL, syncing=0 WHERE id='state'`. On failure: `last_error` = 07-01's locked `SyncError::user_message()` taxonomy verbatim — 无法连接到服务器，请检查地址 / 认证失败，请检查用户名和应用密码 / 证书校验失败，可开启「信任自签名证书」 / 没有目录写入权限，请检查路径 / 服务器限流，请稍后重试 (503 and 429 both map to the 限流 copy after transport's exponential backoff, A5). Never invent parallel error strings here; never block on backoff longer than the 60s request budget — surface the failure instead (D-93 toast path).
    - Devices registry: `upsert_device_record(client, cfg, pool)` — GET `devices/<device_id>.json` if present (preserve `first_seen`), then tmp+MOVE PUT of `build_device_record(...)`; unconditional (own exclusive file, idempotent). Called at the end of every successful push AND pull.
    - Tests (same file, wiremock `=0.6.5` dev-dep from 07-00): fake DAV server asserts (a) first push: PUT to a `.tmp-` path then MOVE carrying `If-None-Match: *` + `Destination` + `Overwrite: T`; (b) second push: MOVE carries `If-Match: "\"etag-1\""`; (c) a 412 on MOVE triggers exactly one re-pull+merge+retry and the retried MOVE carries the fresh etag; (d) the final path never serves a partial body (fake 404s the final name until MOVE lands) — assert GET-before-MOVE 404s. ETag capture from the post-MOVE PROPFIND is stubbed with a multistatus carrying `<D:getetag>`.
  </action>
  <verify>
    <automated>cd /d/Github/Pillowtome && cargo test -p pillowtome --test sync_reconcile push</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'tmp-' src-tauri/src/sync/reconcile.rs` ≥ 1 and `grep -c 'If-None-Match' src-tauri/src/sync/reconcile.rs` ≥ 1 and `grep -c 'If-Match' src-tauri/src/sync/reconcile.rs` ≥ 1 and `grep -c '"MOVE"' src-tauri/src/sync/reconcile.rs` ≥ 1.
    - wiremock expectations prove the conditional headers actually arrive (Pitfall 2 — a conditional path never exercised in test is a fail).
    - `UPDATE sync_state SET remote_etag` present; `last_error` written on failure with Chinese copy.
    - `cargo test -p pillowtome --test sync_reconcile push` green (MSVC toolchain).
  </acceptance_criteria>
  <done>Own state file is published atomically (tmp+MOVE) with If-None-Match/If-Match backstop and one-shot 412 recovery; remote_etag/last_sync_at/last_error persist in sync_state; devices registry entry is maintained.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pull path — PROPFIND ETag map → GET changed → validate → merge into SQLite (never via change_log)</name>
  <files>src-tauri/src/sync/reconcile.rs, src-tauri/tests/sync_reconcile.rs</files>
  <behavior>
    - PROPFIND `state/` Depth:1 yields a device_id→ETag map; own file, folders, non-`.json`, and `tmp-*` names are skipped.
    - Only devices whose ETag differs from the cached `sync_state` peer row are GET; unchanged peers cost zero GETs (Nutstore request frugality, Pitfall 3).
    - A malformed/oversized/foreign-format device file is skipped with a recorded warning; it NEVER aborts the whole pull and NEVER writes partial merges.
    - Every merge application is a set union — records existing on only one side are never dropped; remote-merged rows NEVER enter the local change_log.
    - A further remote progress replaces the local locator row ONLY after the pre-jump local row is stashed in the session undo map (Pitfall 7, D-92).
  </behavior>
  <read_first>
    - .planning/phases/07-webdav-self-hosted-sync/07-RESEARCH.md (Pattern 1 request economics; Pattern 2 merge rules + 非破坏兜底 冲突副本; Pitfall 3 750-item truncation defense; Pitfall 6 hash_algo per-record; Pitfall 7 undo stash; Security Domain V5 remote-JSON validation)
    - core/src/sync/merge.rs + core/src/sync/model.rs (07-00 REAL exports — call, do not reimplement: per-record `merge_progress(local: Option<&ProgressRec>, remote: &ProgressRec, local_device: &str, remote_device: &str)`, `merge_annotation(local: Option<&AnnotationRec>, remote: &AnnotationRec, local_device: &str, remote_device: &str, conflict_copy_id: &str)`, `merge_library(local: Option<&LibraryRec>, remote: &LibraryRec, local_device: &str, remote_device: &str)` → `MergeOutcome<T> { winner: MergeWinner<T>, replaced_local: Option<T> }`; the reconcile-facing map drivers `merge_progress_map` / `merge_annotation_map` (adds `id_alloc: &mut dyn FnMut() -> String`) / `merge_library_map`, each taking `local: &BTreeMap<String, T>`, `remotes: &[(String, BTreeMap<String, T>)]`, `local_device: &str` — the progress driver returns `(merged, Vec<(work_id, replaced_local)>)`, which IS the undo-stash source; `DeviceStateFile::validate()` (ModelError: UnsupportedFormat / InvalidFraction / EmptyDeviceId) covers the format+fraction checks. Local rows adapt to the SAME Rec types — there are no ProgressRow/AnnotationRow/LibraryRow types)
    - src-tauri/src/migrations.rs (locator/annotation/library_item/work/sync_state columns — merge writes target these exact shapes; annotation has no source_id; library_item.source_id is NOT NULL so placeholders need a sentinel)
    - src/reader/locator-store.ts lines 86-129 (the ON CONFLICT(work_id) DO UPDATE upsert shape the locator merge write mirrors)
  </read_first>
  <action>
    Extend `reconcile.rs`:
    - `pub(crate) async fn pull_state_files(pool: &SqlitePool, client: &reqwest_dav::Client, cfg: &SyncConfigRow, undo: &UndoMap, scope_work: Option<&str>) -> Result<PullReport, SyncError>`:
      1. `client.list(&state_dir, reqwest_dav::Depth::One)` → build device_id→etag map from `ListEntity::File` entries whose href matches `<state_dir>/<device_id>.json`; skip own device_id, folders, `tmp-*`. **750-truncation defense** (Pitfall 3): if exactly 750 entries come back, proceed with the returned set AND push a warning into `PullReport.warnings` (surfaced via sync_status last_error-free warning channel) — per-device files keep this count tiny by design; never silently drop devices.
      2. Peer-ETag cache: `sync_state` rows with `id='peer:<device_id>'` (schema id is TEXT PRIMARY KEY — peer rows use only id+remote_etag). GET (`client.get`) only ETag-changed files; upsert the peer row after a successful merge of that file.
      3. Validate before merge (V5): body cap 16 MiB; `serde_json::from_slice::<DeviceStateFile>` (its built-in recursion limit guards nesting depth) → `DeviceStateFile::validate()` from 07-00's model (`UnsupportedFormat` / `InvalidFraction` / `EmptyDeviceId` → skip device with warning); then assert filename device_id == payload device_id. Any failure → skip THAT device, record warning, continue (soft-fail, 中文 warning strings).
      4. Apply via 07-00's map drivers (hash_algo always read per-record — annotation sha256 vs work blake3, never cross-compare, Pitfall 6). Load the local side into the SAME Rec types: `BTreeMap<work_id, ProgressRec>` from `locator`; `BTreeMap<annotation_id, AnnotationRec>` from `annotation` (ALL rows incl. tombstones); `BTreeMap<work_id, LibraryRec>` from the `library_item`⋈`work` join (ALL rows incl. tombstones). Then fold ONE remote device at a time (`remotes = &[(device_id, maps)]` single-entry slices — so each replaced local is attributable to exactly that device for the undo payload):
         - **Progress**: `merge_progress_map(&local, remotes, &own_device_id)` → merged map + `Vec<(work_id, replaced_local)>`. For each displaced entry, FIRST stash `JumpStash { work_id, from_row, to_fraction, from_device_name, stashed_at }` into `undo` keyed by work_id (`to_fraction` = the merged winner's fraction; `from_device_name` = that remote file's `device_name`); then write back only entries that differ from the pre-merge local map via `INSERT INTO locator ... ON CONFLICT(work_id) DO UPDATE` (locator-store shape). When `scope_work` is Some, still merge everything (request economics identical) but only report that work's jump.
         - **Annotations**: `merge_annotation_map(&local, remotes, &own_device_id, &mut id_alloc)` with `id_alloc` producing uuid v4 strings (uuid generation stays out of the pure core) → merged map (union incl. tombstone remove-wins; same-id/same-revision/different-content lands under the new id with `note` prefixed `冲突副本`). Write back differing entries via `INSERT ... ON CONFLICT(annotation_id) DO UPDATE` — including `deleted=1` tombstones.
         - **Library**: `merge_library_map(&local, remotes, &own_device_id)` → merged map. Write-back per key: remote-only work with `deleted==0` → `INSERT OR IGNORE INTO work` (content_hash/format from rec) + placeholder `library_item` (`item_id` = fresh uuid, `source_id='sync-remote'` sentinel — column is NOT NULL and the file is not local yet, D-99 placeholder card reads this row; `file_sync_enabled` from rec; title/author/imported_at from rec). Remote-only with `deleted==1` → no-op (nothing to tombstone locally). Both-present → apply merge winner for `deleted` and `file_sync_enabled` ONLY — local title/author/cover and the remaining V4 columns (`item_id`/`source_id`/`cover_file`/`last_opened_at`/`last_read_at`, invisible to merge) are preserved untouched (D-100 single truth). Remote book files are NEVER deleted by sync (Q2).
         - **Discovery rows (contract with 07-03)**: for every merged library rec whose `file_sync.enabled` is true AND carries remote metadata (`remote_path` present), UPSERT a `sync_file_state` DISCOVERY row — `work_id`, `direction='download'`, `transfer_uuid=NULL`, `chunks_done='[]'`, `size`/`hash`/`remote_path` from the rec, `started_at`/`updated_at` = now — so `sync_download_book` locates the remote file with no extra network round-trip. On conflict (work_id PK) update ONLY `size`/`hash`/`remote_path`/`updated_at` — NEVER clobber `transfer_uuid`/`chunks_done` of an in-flight download.
         - **Ledger hygiene**: none of the above writes touches `change_log` (anti-pattern — clock inflation / re-push loops). Add a code comment saying so at the top of the apply block. (Discovery rows are file-plane bookkeeping, not ledger ops.)
      5. `upsert_device_record` (Task 2) refreshes own `devices/<device_id>.json` last_seen.
    - `PullReport { pulled_devices, merged_progress, merged_annotations, merged_library, jumps: Vec<JumpStash>, warnings: Vec<String> }`.
    - `pub(crate) type UndoMap = std::sync::Arc<tokio::sync::Mutex<std::collections::HashMap<String, JumpStash>>>` in reconcile.rs; the managed Tauri state wrapper `SyncUndoMap` lives in sync/mod.rs (Task 4).
    - Tests: (a) remote further progress replaces locator and stash holds the exact old row; (b) remote tombstone deletes (deleted=1) a local-live annotation and a later pull of an older live copy does NOT resurrect it; (c) one-side-only records survive both directions (union); (d) invalid JSON / `UnsupportedFormat` / mismatched device_id each skip only that device; (e) `SELECT COUNT(*) FROM change_log` identical before/after a pull; (f) ETag-unchanged peer issues zero GET (wiremock request journal); (g) a peer library rec with `file_sync.enabled` + metadata lands a `sync_file_state` row `direction='download'` carrying the rec's remote_path/size/hash with NULL transfer_uuid — and a second pull does NOT clobber a manually-set transfer_uuid/chunks_done on that row.
  </action>
  <verify>
    <automated>cd /d/Github/Pillowtome && cargo test -p pillowtome --test sync_reconcile pull</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Depth::One" src-tauri/src/sync/reconcile.rs` ≥ 1 and `grep -c "tmp-" src-tauri/src/sync/reconcile.rs` ≥ 1 (tmp names skipped on read and used on write).
    - `grep -c "INSERT INTO change_log" src-tauri/src/sync/reconcile.rs` == 0 (pull path never writes the ledger).
    - `grep -c "peer:" src-tauri/src/sync/reconcile.rs` ≥ 1 (per-peer ETag cache rows).
    - `grep -c "冲突副本" src-tauri/src/sync/reconcile.rs` ≥ 1 and `grep -c "sync-remote" src-tauri/src/sync/reconcile.rs` ≥ 1.
    - `grep -c "'download'" src-tauri/src/sync/reconcile.rs` ≥ 1 (discovery rows) and `grep -c "merge_progress_map\|merge_annotation_map\|merge_library_map" src-tauri/src/sync/reconcile.rs` ≥ 1 (07-00 map drivers, not reimplemented merge).
    - change_log count invariance asserted in test (e); tests (a)-(g) green under MSVC.
  </acceptance_criteria>
  <done>Pull merges changed peer state into SQLite via 07-00's map drivers with union/no-loss semantics, tombstone anti-resurrection, undo stashing before any overwrite, request-frugal ETag caching, download-discovery rows for 07-03, and zero change_log writes.</done>
</task>

<task type="auto">
  <name>Task 4: Undo plumbing (D-92 backend) + IPC commands part 2 + sync-status event</name>
  <files>src-tauri/src/sync/commands.rs, src-tauri/src/sync/mod.rs, src-tauri/src/lib.rs, src-tauri/tests/sync_reconcile.rs</files>
  <read_first>
    - src-tauri/src/commands.rs lines 1-120 (house command style: `#[tauri::command]`, `#[derive(Serialize)] #[serde(rename_all = "camelCase")]` payload structs, 简体中文 soft-fail messages; AppHandle/State usage)
    - src-tauri/src/lib.rs lines 8-13 + 164-175 (module decl + `invoke_handler` registration vec — extend, matching existing shape)
    - src-tauri/src/sync/commands.rs + sync/mod.rs (07-01 output: part-1 commands sync_get_config/sync_test_and_save/sync_disconnect, engine assembly, SyncError; part 2 extends the same files)
    - .planning/phases/07-webdav-self-hosted-sync/07-UI-SPEC.md §5 (trace pill 已从其他设备同步 + dialog 「{设备名称}」上读到了 {n}%，已自动跳到最远位置。 — the pill is fed by this command's return; pill lifetime = session until tap/manual jump/合书) and §C/Copywriting (status states; 同步失败：{原因}; 服务器限流，请稍后重试)
    - src/reader/annotation-store.ts appendChangeLog pattern (monotonic `logical_clock = COALESCE((SELECT MAX(logical_clock) ... WHERE device_id=$d),0)+1` computed INSIDE one atomic INSERT — the revert row reuses it with entity='locator')
    - src-tauri/capabilities/default.json + android.json (verify-only: no new grant needed — see action)
  </read_first>
  <action>
    1. **Session state + orphan reset**: in `sync/mod.rs` add `pub struct SyncUndoMap(pub reconcile::UndoMap)` and `pub struct SyncProgressMaps(pub tokio::sync::Mutex<TransferMaps>)` with `pub struct TransferMaps { pub downloads: std::collections::HashMap<String, f64>, pub uploads: std::collections::HashMap<String, f64> }` (work_id → percent) + `Default` impls; register both via `app.manage(...)` in lib.rs next to the 07-01 sync engine state. Session-scoped in-memory only (Pitfall 7 — never persisted; pill lifetime per UI-SPEC §5). At engine init, reset any crash-orphaned flag: `UPDATE sync_state SET syncing=0 WHERE id='state'` — a row found set at startup is definitionally orphaned (single-process engine), so manual sync can never be permanently refused (the live re-entry guard below is in-memory).
    2. **Commands** (extend `src-tauri/src/sync/commands.rs`; all async, all resolve the pool via `sqlite_pool(&app)` from Task 1, all soft-fail to Chinese copy, NONE accepts or returns password; payload structs `#[serde(rename_all = "camelCase")]`):
       - `sync_book_opened(work_id: String) -> BookOpenedSyncResult { jumped: bool, device_name: Option<String>, progress_fraction: Option<f64>, replaced_local: Option<ReplacedLocal> }` with `pub struct ReplacedLocal { pub cfi: String, pub progress_fraction: f64 }` (wire: `jumped`/`deviceName`/`progressFraction`/`replacedLocal{cfi,progressFraction}`) — 开书拉 (D-90): if unconfigured return `jumped:false` silently; else run `pull_state_files(..., scope_work: Some(&work_id))` then READ the undo stash for work_id WITHOUT consuming it (consumed only by revert — the user may revert later this session) and map it to the trace-pill payload: `device_name` = the winning peer's name, `progress_fraction` = the new furthest fraction, `replaced_local` = the displaced local position (`cfi` null → `""`, `progress_fraction` null → `0.0`; D-08 makes a fraction effectively always present). This feeds the UI-SPEC §5 dialog 「{设备名称}」上读到了 {n}%，已自动跳到最远位置。
       - `sync_book_closed(work_id: String) -> ()` — 合书推 (D-90): run `reconcile_push`; drop the undo stash entry for work_id (pill lifetime ends at close, UI-SPEC §5). Fire-and-forget semantics: failures persist to sync_state + event, never error the UI.
       - `sync_revert_jump(work_id: String) -> Option<ReplacedLocal>` — D-92 撤回原位: take (consume) the stash; write the stashed pre-jump locator row back via the normal upsert (`ON CONFLICT(work_id) DO UPDATE`, `updated_at = now`); append exactly one LOCAL change_log row (entity='locator', op='upsert', payload = locator JSON, monotonic clock inside the single INSERT — this IS a local user op, the anti-pattern ban does not apply); return `Some(ReplacedLocal)` carrying the RESTORED position (same camelCase struct) so the UI jumps to the target straight from the response. No stash → `null` (soft-fail; a second tap is a no-op).
       - `sync_now() -> SyncStatusPayload` — manual 立即同步 (D-90 兜底): re-entry guard = in-memory `AtomicBool` in the engine state (authoritative in-process; the init reset above already cleared any crash-orphaned `syncing=1`), persisting `syncing=1`/`0` in sync_state around the run for cross-visibility; emit `syncing`; run pull → push (pull first so the push carries the merged state); persist last_sync_at/last_error; emit terminal state.
       - `sync_status() -> SyncStatusPayload { configured, server_url, username, syncing, last_sync_at, last_error }` (wire: configured/serverUrl/username/syncing/lastSyncAt/lastError) — reads sync_config + sync_state + the in-memory flag; 07-04's store initializes from it with EMPTY transfer arrays until the first event arrives. Password NEVER leaves the keychain (RESEARCH Pattern 4 — the payload struct must not even have the field).
    3. **Event (SOLE emitter)**: the ONLY site calling `app.emit("sync-status", …)` is `pub(crate) fn emit_sync_status(app: &tauri::AppHandle)` in commands.rs (`use tauri::Emitter`). Payload `SyncStatusEvent { configured: bool, syncing: bool, last_error: Option<String>, downloads: Vec<TransferProgress>, uploads: Vec<TransferProgress> }` with `TransferProgress { work_id: String, percent: f64 }` — camelCase on the wire (`configured`/`syncing`/`lastError`/`downloads`/`uploads`/`workId`/`percent`); downloads/uploads are snapshotted from `SyncProgressMaps`. Emitted on EVERY status transition (idle/syncing/error incl. failure paths, D-93 status dot) AND on every transfer progress tick: 07-03's fileplane reports INTO the engine via `pub(crate) async fn report_transfer_progress(app: &tauri::AppHandle, kind: TransferKind, work_id: &str, percent: f64)` (`TransferKind { Download, Upload }`, defined here) — it updates the map (percent ≥ 100 removes the entry) then re-emits the unified event; 07-03 NEVER emits its own event shape. Verified grant: `core:default` already in default.json includes `core:event:default` → `allow-listen`/`allow-emit` (gen/schemas/acl-manifests.json) — **no capability change is required**, and app-defined commands need no per-command ACL entries (existing commands.rs commands are unlisted today). Leave both capabilities files untouched; state this in the SUMMARY so 07-01's grant surface stays minimal.
    4. **Registration**: add the five commands to the `invoke_handler` vec in lib.rs.
    5. **A4 runtime probe**: first successful `sqlite_pool` resolution logs `[sync] shared pool acquired`; verify on a desktop run (`pnpm tauri dev`, invoke sync_status from the frontend console or the settings sheet) — if unreachable, apply the RESEARCH A4 fallback (route merge writes through the frontend plugin-sql path, still single binding) and record the deviation in the SUMMARY.
    6. Tests: revert path against in-memory pool — seed locator 0.9 (post-jump) + stash 0.4 → run the revert helper → assert locator row equals the stashed composite (cfi/text_*/fraction) with fresh updated_at, change_log +1 row with entity='locator' and clock = prior max+1, the returned `Some(ReplacedLocal)` carries the restored cfi/fraction, and a second revert returns `None`.
  </action>
  <verify>
    <automated>cd /d/Github/Pillowtome && cargo test -p pillowtome --test sync_reconcile undo && cargo build -p pillowtome</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "sync_book_opened\|sync_book_closed\|sync_revert_jump\|sync_now\|sync_status" src-tauri/src/lib.rs` ≥ 5 (all five registered).
    - `grep -c '"sync-status"' src-tauri/src/sync/commands.rs` ≥ 1 and `grep -c "downloads" src-tauri/src/sync/commands.rs` ≥ 1 and `grep -c "uploads" src-tauri/src/sync/commands.rs` ≥ 1 (unified event payload, sole emitter).
    - `grep -c "ReplacedLocal" src-tauri/src/sync/commands.rs` ≥ 2 (struct def + both command returns) and `grep -c 'rename_all = "camelCase"' src-tauri/src/sync/commands.rs` ≥ 1.
    - `grep -c "report_transfer_progress" src-tauri/src/sync/commands.rs` ≥ 1 (07-03's only progress channel into the engine).
    - `grep -c "SET syncing=0" src-tauri/src/sync/mod.rs` ≥ 1 (crash-orphan reset at engine init).
    - `grep -ci "password" src-tauri/src/sync/commands.rs` == 0 (no credential field in any payload).
    - `grep -c "INSERT INTO change_log" src-tauri/src/sync/commands.rs` ≥ 1 (LOCAL revert row only).
    - capabilities/default.json + android.json byte-identical to pre-plan (no grant needed — verified against acl-manifests).
    - Revert test green (`Some(ReplacedLocal)` with restored cfi/fraction, then `None`); `cargo build -p pillowtome` green (MSVC).
  </acceptance_criteria>
  <done>The five part-2 commands exist and are registered; book-open pull returns the trace-pill payload (deviceName/progressFraction/replacedLocal, stash read-not-consumed); revert restores the exact pre-jump position, returns it for the UI jump, and appends a local ledger row; the unified sync-status event (sole emitter, engine-owned transfer progress maps) fires on every transition and progress tick; a crash-orphaned syncing flag can never lock out manual sync; no capability or frontend change in this plan.</done>
</task>

<task type="auto">
  <name>Task 5: e2e — dual in-memory devices over a wiremock WebDAV fake (union, anti-resurrection, skew determinism, undo)</name>
  <files>src-tauri/tests/sync_e2e.rs</files>
  <read_first>
    - src-tauri/tests/sync_reconcile.rs (this plan's Task 1-4 harness: in-memory pool helper, wiremock DAV fake — factor shared fakes into this file's own module or duplicate minimal helpers; keep tests self-contained like tests/migration.rs)
    - src-tauri/tests/migration.rs lines 1-30 (off-device pattern: apply SCHEMA_Vn from pillowtome_lib::migrations against sqlite::memory:)
    - .planning/phases/07-webdav-self-hosted-sync/07-VALIDATION.md (SYNC-03 e2e row: dual in-memory SqlitePool + wiremock; SYNC-05 conditional_put row — covered by Task 2 + this file)
    - .planning/phases/07-webdav-self-hosted-sync/07-RESEARCH.md Pattern 2 关键规则 (the exact total order: progress_fraction → updated_at → device_id lexicographic; revision → content_hash → updated_at → device_id; tombstone wins ties)
  </read_first>
  <action>
    Create `src-tauri/tests/sync_e2e.rs`:
    - **Harness**: two `sqlx::SqlitePool` instances (`sqlite::memory:`) named A and B, each migrated SCHEMA_V1..V8, each with its own sync_meta device row (device_id 'device-a' / 'device-b') and sync_config/sync_state rows pointing at the same wiremock server (`allow_http=1`, remote_path `pillowtome/`). The wiremock fake implements the DAV verbs the engine uses: PROPFIND (multistatus XML with per-href `<D:getetag>`), GET, PUT (stores body, bumps etag), MOVE (atomic rename + honors If-None-Match/If-Match → 412), all behind method matchers with custom verb strings ("PROPFIND"/"MOVE"). The transport client comes from 07-01's builder pointed at `mock_server.uri()`.
    - **Scenario 1 — union, nothing lost (SYNC-02/03/05)**: A seeds 2 annotations + locator 0.42 + 2 library books → push → B pulls → assert B holds both annotations verbatim, locator 0.42, both books (placeholder `source_id='sync-remote'`); B's `change_log` row count is unchanged by the pull; B's own pre-existing annotation on another work still exists (union).
    - **Scenario 2 — tombstone anti-resurrection**: B sets deleted=1 on one of A's annotations (revision bump) → push → A pulls → A's row is `deleted=1`; then re-pull an older state file where it was still live (stale peer) → stays `deleted=1` (remove-wins, no resurrection).
    - **Scenario 3 — skewed clocks determinism**: same logical update issued on A with updated_at = T+3h and on B with T-3h (equal progress_fraction, equal revision): run merge both orders (A-pulls-B, B-pulls-A) and assert the SAME winner twice (total order falls to device_id lexicographic) — determinism over wall-clock trust (PITFALLS #8).
    - **Scenario 4 — jump + undo + revert (D-92)**: B locator 0.90, A locator 0.40 for the same work → A pulls (scope) → A's locator == 0.90 AND A's undo stash holds the exact 0.40 composite row → A reverts → locator back to 0.40, one LOCAL change_log row appended (entity='locator'), stash cleared.
    - **Scenario 5 — catalog union incl. tombstone flag + discovery row**: B-only book (file_sync enabled, with upload metadata) arrives in A as placeholder with remote_path/size/hash AND a `sync_file_state` discovery row `direction='download'` carrying that remote_path/size/hash with NULL transfer_uuid (what 07-03's `sync_download_book` reads — no extra network); A-only book remains in A's catalog after a full push→pull roundtrip (never dropped); a remote `deleted=1` catalog row tombstones A's local copy (`deleted=1`, row retained — Q2 tombstone, never physical DELETE) and A's push then carries the tombstone.
  </action>
  <verify>
    <automated>cd /d/Github/Pillowtome && cargo test -p pillowtome --test sync_e2e && cargo test -p pillowtome sync && cargo test -p pillowtome-core sync</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "sqlite::memory:" src-tauri/tests/sync_e2e.rs` ≥ 1 and `grep -c "MockServer" src-tauri/tests/sync_e2e.rs` ≥ 1.
    - All five scenarios assert by name (union / resurrection / determinism / revert / catalog); the anti-resurrection and determinism scenarios run the merge in BOTH orders.
    - `cargo test -p pillowtome --test sync_e2e` green; full `cargo test -p pillowtome sync` (reconcile + e2e + 07-01's conditional_put/transport tests) and `cargo test -p pillowtome-core sync` (07-00 merge fns — regression, untouched here) green under MSVC.
    - Feedback latency: the reconcile+e2e subset runs well under the 60s Nyquist budget (in-memory SQLite + wiremock, no real network).
  </acceptance_criteria>
  <done>Two simulated devices reconcile end-to-end over a fake DAV server: union/no-loss, tombstone anti-resurrection, skew-deterministic merges, and exact-position undo — all off-device, all green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebDAV server → reconcile (pull) | Remote state files + PROPFIND multistatus are untrusted input: the server (or a proxy) may be malicious, corrupted, or compromised — self-hosted trust model documented, E2EE out of scope (D-105 accepted) |
| reconcile → SQLite (shared pool) | Merge writes enter the single local DB; a bad merge poisons local reading state |
| WebView → sync commands (IPC) | New command surface; payloads are small structs only (D-06) |
| reconcile → WebDAV server (push) | Outbound catalog/progress/annotations are plaintext on the user's own server (accepted, D-105); credentials ride Basic/Digest per D-96 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Info Disclosure | password exfiltration via sync payloads/logs/toasts | mitigate | RESEARCH Pattern 4: password lives only in the OS keychain (07-01); no payload struct in this plan has a credential field (`grep -ci password` acceptance gates on both files); last_error strings carry classified Chinese copy, never URLs-with-credentials or Authorization headers (V7) |
| T-07-02-02 | Tampering | hostile/corrupt remote device state file | mitigate | V5 validation before merge: 16 MiB body cap, `format==1` gate, filename↔payload device_id equality, progress_fraction range, revision ≥ 1; any failure skips that device with a Chinese warning, never aborts the pull, never partially applies |
| T-07-02-03 | Tampering | path injection via remote hrefs (`../../`) | mitigate | All remote paths built single-point in core remote.rs (07-00) jailed under the configured root; pull only processes hrefs matching `<root>/state/<device_id>.json`, skips `tmp-*` and anything else |
| T-07-02-04 | DoS | giant multistatus / truncated listings / rate limiting | mitigate | 750-item truncation defense (warn-and-continue, Pitfall 3); 60s request timeout (transport); 503/429 → exponential backoff + toast 服务器限流，请稍后重试 (A5); per-device single file keeps request count minimal |
| T-07-02-05 | Tampering | half-written remote state read by peers | mitigate | tmp-`<uuid>` PUT + atomic MOVE publish (Pitfall 5); readers only open final names; optimistic concurrency (If-None-Match/If-Match) backstops same-device dual-open with 412 → re-pull-merge-retry once |
| T-07-02-06 | Tampering/Repudiation | merge loop / clock inflation via ledger | mitigate | Remote-merged rows NEVER written to change_log (acceptance gate: zero INSERTs in reconcile.rs); only the LOCAL D-92 revert appends, with the monotonic clock computed inside one atomic INSERT (annotation-store pattern) |
| T-07-02-07 | Info Disclosure | book bytes over IPC | mitigate | State plane touches SQL rows only — no book file is read by this plan; bytes stay on pillow:// (D-06); IPC payloads are small structs |
| T-07-02-08 | Elevation of Privilege | new sync_* IPC command surface | mitigate | Commands accept only small structs (work_id strings); no new plugin permissions — `core:default` already covers event listen/emit (acl-manifests verified), app commands need no ACL entries; capabilities files byte-identical |
| T-07-02-09 | Tampering | undo stash forgery/lifetime | mitigate | Stash is session-scoped in-memory Rust state, never serialized, never accepted from IPC input — only written by the progress merge's replaced_local byproduct; revert is idempotent-safe (second call returns null) |
| T-07-02-10 | MITM | TLS policy weakened transitively | mitigate | D-95 switches stay independent (`allow_http` / `trust_self_signed` are separate sync_config columns, both default 0) and are applied ONLY at agent construction in 07-01's transport (`danger_accept_invalid_certs(cfg.trust_self_signed)`); reconcile never rebuilds or mutates the client, so the merge engine cannot widen the TLS policy |

</threat_model>

<artifacts_produced>
## Artifacts this plan produces (07-02)

- **`src-tauri/src/sync/reconcile.rs`** (new): `build_device_state`, `build_device_record`, `push_state_file`, `reconcile_push`, `pull_state_files`, `upsert_device_record`, `sqlite_pool` (mod.rs), `UndoMap`/`JumpStash`/`PullReport` types. The whole state plane: push (SQLite → DeviceStateFile → tmp PUT → conditional MOVE), pull (PROPFIND ETag map → GET changed → V5-validate → merge via 07-00's map drivers → SQLite), devices-registry upkeep, and `sync_file_state` DISCOVERY rows (`direction='download'`) so 07-03's `sync_download_book` locates remote files with no extra network.
- **`src-tauri/src/sync/commands.rs`** (extended): `sync_book_opened`, `sync_book_closed`, `sync_revert_jump`, `sync_now`, `sync_status`; payload types `BookOpenedSyncResult`, `ReplacedLocal`, `SyncStatusPayload`, `SyncStatusEvent`, `TransferProgress`, `TransferKind` — all camelCase serde, all password-free. SOLE emitter of the unified `"sync-status"` event (`emit_sync_status` + `report_transfer_progress`, the channel 07-03's fileplane reports into).
- **`src-tauri/src/sync/mod.rs`** (extended): `SyncUndoMap` (D-92 undo byproduct store) + `SyncProgressMaps`/`TransferMaps` (in-memory downloads/uploads percent maps) managed session state; crash-orphan `syncing=0` reset at engine init.
- **`src-tauri/src/lib.rs`** (extended): five new commands in `invoke_handler`, `SyncUndoMap` + `SyncProgressMaps` managed.
- **`src-tauri/Cargo.toml` + `Cargo.lock`**: direct pins `serde_json = "=1.0.150"`, `uuid = "=1.23.4"` (both already in the lock — no new packages, D-13; idempotent if 07-00 added them; if 07-01 declared serde_json as a dev-dependency, this UPGRADES it to a runtime dependency — same pin, one manifest entry).
- **`src-tauri/tests/sync_reconcile.rs`** (new): builder shape (incl. value-level file_sync metadata asserts), conditional tmp+MOVE push with 412 retry, pull merge apply, discovery rows, undo stash + revert — off-device (in-memory SQLite + wiremock).
- **`src-tauri/tests/sync_e2e.rs`** (new): dual-device e2e over wiremock DAV fake (5 scenarios).
- **Remote artifacts at runtime**: `state/<device_id>.json` (format:1 self-describing), `devices/<device_id>.json` registry entries — tmp+MOVE published; peer ETag cache rows `sync_state.id='peer:<device_id>'`.
- **NOT produced (boundaries honored)**: no frontend files (07-04 wires the pill/buttons), no core changes (merge/model are 07-00), no file-plane transfer code (07-03 — reconcile reads upload metadata and writes download DISCOVERY rows only; 07-03 reports transfer progress INTO this plan's engine maps and never emits its own event), no capabilities changes (none needed), no change_log writes from pull.
</artifacts_produced>

<verification>
- `cargo test -p pillowtome --test sync_reconcile` green (build/push/pull/undo groups).
- `cargo test -p pillowtome --test sync_e2e` green (5 scenarios).
- `cargo test -p pillowtome sync` green (this plan + 07-01 transport/conditional_put surface).
- `cargo test -p pillowtome-core sync` green (07-00 merge fns untouched, regression).
- `cargo build -p pillowtome` green; all cargo runs use the MSVC toolchain (STATE.md: GNU gcc exits 1 silently).
- A4 runtime probe: `[sync] shared pool acquired` observed on a desktop run, else the documented frontend plugin-sql fallback applied and recorded in the SUMMARY.
- No frontend files touched (tsc/pnpm test not applicable to this plan's diff); capabilities files byte-identical.
- AVD production-APK gate: nothing in this plan is device-visible (backend engine + IPC surface only) — the device acceptance for the trace pill/status dot/undo dialog belongs to 07-04's UI wiring; the A4 pool probe above is the only runtime smoke this plan needs.
</verification>

<success_criteria>
- Push publishes `state/<device_id>.json` atomically (tmp+MOVE) with If-None-Match/If-Match backstop and one-shot 412 recovery; sync_state persists remote_etag/last_sync_at/last_error.
- Pull merges changed peer state via the 07-00 pure merge fns: progress furthest-wins with pre-jump stash, annotations by-id union with tombstone remove-wins, library union with placeholder derivation — zero data loss, zero change_log pollution.
- D-92 backend works: book-open pull returns the trace-pill payload; revert restores the exact pre-jump local position as a local ledgered op.
- SYNC-02/SYNC-03 e2e: two devices converge to the union; SYNC-05: determinism under clock skew, anti-resurrection, conditional-write safety — all off-device green.
</success_criteria>

<output>
Create `.planning/phases/07-webdav-self-hosted-sync/07-02-SUMMARY.md` when done.
</output>
