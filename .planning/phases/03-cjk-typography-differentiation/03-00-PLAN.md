---
phase: 03-cjk-typography-differentiation
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src-tauri/src/migrations.rs
  - src-tauri/tests/migration.rs
  - src/reader/apply-reading-styles.ts
  - src/reader/apply-reading-styles.test.ts
  - src/reader/reading-prefs.ts
  - src/reader/cjk-feature-detect.ts
  - src/reader/cjk-feature-detect.test.ts
  - src/reader/cjk-kinsoku.ts
  - src/reader/cjk-kinsoku.test.ts
  - src/reader/cjk-autospace-shim.ts
  - src/reader/cjk-autospace-shim.test.ts
  - src/reader/fonts.ts
  - src/components/ui/switch.tsx
  - src/components/ui/popover.tsx
  - package.json
  - pnpm-lock.yaml
  - tests/fixtures/cjk/coverage-sheet.html
  - tests/fixtures/cjk/kinsoku-samples.html
autonomous: true
requirements: [CJK-01, CJK-02, CJK-03, CJK-04]
must_haves:
  truths:
    - "ReadingPrefs carries three CJK booleans defaulting true (标点挤压 / 盘古之白 / 禁则) per D-32"
    - "SCHEMA_V3 appends cjk_punct_trim / cjk_autospace / cjk_kinsoku INTEGER NOT NULL DEFAULT 1 on reading_prefs (D-34)"
    - "detectCjkCssCaps is pure and injectable; never infers support from OS/API level (D-35, D-12)"
    - "Autospace shim unit tests prove concatenated textContent equals original and disposer restores DOM (D-37)"
    - "Shared zh kinsoku prohibited start/end tables exist for fixtures/tests (CJK-03; no JA UI)"
    - "Official shadcn switch + popover primitives exist for Wave 1 settings UI"
  artifacts:
    - path: "src-tauri/src/migrations.rs"
      provides: "SCHEMA_V3 ALTER columns for CJK toggles"
      contains: "SCHEMA_V3"
    - path: "src/reader/cjk-feature-detect.ts"
      provides: "Runtime CSS.supports probes + CjkCssCaps"
      exports: ["detectCjkCssCaps", "CjkCssCaps"]
    - path: "src/reader/cjk-autospace-shim.ts"
      provides: "installAutospaceShim disposer API (stub or minimal reversible path)"
      exports: ["installAutospaceShim"]
    - path: "src/reader/cjk-kinsoku.ts"
      provides: "ZH_PROHIBITED_LINE_START / ZH_PROHIBITED_LINE_END"
    - path: "src/components/ui/switch.tsx"
      provides: "shadcn Switch for CJK toggles"
    - path: "tests/fixtures/cjk/coverage-sheet.html"
      provides: "Glyph coverage sheet fixture for golden harness"
  key_links:
    - from: "src/reader/reading-prefs.ts"
      to: "reading_prefs CJK columns"
      via: "rowToPrefs + saveReadingPrefs bound params"
      pattern: "cjk_punct_trim|cjkPunctTrim"
    - from: "src/reader/apply-reading-styles.ts"
      to: "DEFAULT_PREFS"
      via: "cjkPunctTrim/cjkAutospace/cjkKinsoku true"
      pattern: "cjkPunctTrim:\\s*true"
---

<objective>
Close Phase 3 Wave 0 Nyquist gaps: SCHEMA_V3 global CJK toggle columns (D-34), ReadingPrefs three defaults-ON fields (D-32), pure CJK helper modules (feature-detect D-35, kinsoku tables CJK-03, autospace shim API with D-37 textContent invariance), official shadcn switch+popover, and CJK fixture scaffolds for later golden-image work.

Purpose: Downstream CSS/UI/font plans need typed prefs, automated unit sampling, and UI primitives before render/CSS and settings land. No parallel render path — only contracts and pure helpers.

Output: Migration v3 green; prefs map soft-fails to CJK defaults ON; pure helpers + tests green; switch/popover installed; coverage-sheet fixtures present.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cjk-typography-differentiation/03-CONTEXT.md
@.planning/phases/03-cjk-typography-differentiation/03-RESEARCH.md
@.planning/phases/03-cjk-typography-differentiation/03-PATTERNS.md
@.planning/phases/03-cjk-typography-differentiation/03-UI-SPEC.md
@.planning/phases/03-cjk-typography-differentiation/03-VALIDATION.md
@src/reader/apply-reading-styles.ts
@src/reader/reading-prefs.ts
@src-tauri/src/migrations.rs
@src-tauri/tests/migration.rs
@components.json
</context>

<decision_notes>
Planner locks for this phase set (cite in later plans too):
1. **SCHEMA_V3 columns:** `cjk_punct_trim`, `cjk_autospace`, `cjk_kinsoku` INTEGER NOT NULL DEFAULT 1 (D-32/D-34).
2. **ReadingPrefs keys:** `cjkPunctTrim`, `cjkAutospace`, `cjkKinsoku` booleans default true.
3. **Shim API:** `installAutospaceShim(doc: Document): () => void` — Wave 0 lands API + textContent/disposer tests; full Highlight/span strategy completes in 03-01 (D-36/D-37).
4. **Kinsoku:** one shared zh table (Hans/Hant); tables for tests/fixtures only; runtime enforcer is CSS `line-break: strict` in 03-01 (no JA UI).
5. **fonts.ts stub:** export `BUNDLED_CJK_FAMILY = "PillowBundledCJK"` and `buildBundledCjkFontFaceCss(): string` returning empty string until 03-03.
6. **Do not** implement Settings「中文排版」UI here (03-02); do not materialize Noto binaries here (03-03); do not expand READER-POS.
</decision_notes>

<tasks>

<task type="auto">
  <name>Task 1: SCHEMA_V3 + ReadingPrefs CJK fields + soft-fail prefs map</name>
  <files>src-tauri/src/migrations.rs, src-tauri/tests/migration.rs, src/reader/apply-reading-styles.ts, src/reader/apply-reading-styles.test.ts, src/reader/reading-prefs.ts</files>
  <read_first>
    - src-tauri/src/migrations.rs (SCHEMA_V2 append-only pattern; migrations() vec)
    - src-tauri/tests/migration.rs (fresh_db_v2, has_column, seed assertions, migration_set length)
    - src/reader/apply-reading-styles.ts (ReadingPrefs, DEFAULT_PREFS — D-41 lineHeight 1.75 must stay)
    - src/reader/reading-prefs.ts (rowToPrefs, loadReadingPrefs soft-fail, saveReadingPrefs bound params)
    - 03-CONTEXT.md D-32, D-34, D-41
    - 03-RESEARCH.md Pattern 1 SCHEMA_V3 SQL + ReadingPrefs
    - 03-PATTERNS.md migrations.rs / reading-prefs.ts / apply-reading-styles.ts sections
  </read_first>
  <action>
    **Rust SCHEMA_V3 (D-34, append-only — never rewrite V1/V2):**
    - Add `pub const SCHEMA_V3` with three ALTERs only:
      - `ALTER TABLE reading_prefs ADD COLUMN cjk_punct_trim INTEGER NOT NULL DEFAULT 1;`
      - `ALTER TABLE reading_prefs ADD COLUMN cjk_autospace INTEGER NOT NULL DEFAULT 1;`
      - `ALTER TABLE reading_prefs ADD COLUMN cjk_kinsoku INTEGER NOT NULL DEFAULT 1;`
    - Register Migration version 3, description `cjk_typography_prefs`, kind Up, after v2 in `migrations()`.
    - Extend `src-tauri/tests/migration.rs`: `fresh_db_v3` applies V1→V2→V3; assert three columns exist; existing `global` seed row has defaults `1` for each; `migrations().len() == 3`; description/sql identity checks for SCHEMA_V3.

    **TypeScript ReadingPrefs (D-32, D-41):**
    - Extend `ReadingPrefs` with `cjkPunctTrim: boolean`, `cjkAutospace: boolean`, `cjkKinsoku: boolean`.
    - `DEFAULT_PREFS` sets all three `true`; **do not** change `lineHeight: 1.75` (D-41).
    - Update `apply-reading-styles.test.ts` DEFAULT_PREFS exact-object expectation to include the three true flags and lineHeight 1.75.

    **reading-prefs.ts map (soft-fail, bound params — T-03-sql):**
    - Extend `ReadingPrefsRow` with `cjk_punct_trim`, `cjk_autospace`, `cjk_kinsoku` (number).
    - `rowToPrefs`: treat missing/undefined as true; map `!== 0` → true (defaults ON).
    - `loadReadingPrefs` SELECT list includes new columns; catch still returns `{ ...DEFAULT_PREFS }`.
    - `saveReadingPrefs` INSERT…ON CONFLICT updates the three INTEGER columns via bound `$n` params only (0/1 from booleans). Never localStorage. Keep `PREFS_SAVE_DEBOUNCE_MS = 400`.
  </action>
  <verify>
    <automated>cargo test --workspace && pnpm test && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - migrations.rs contains SCHEMA_V3 and cjk_punct_trim / cjk_autospace / cjk_kinsoku with DEFAULT 1
    - migrations() length is 3 (test or source)
    - DEFAULT_PREFS includes cjkPunctTrim/cjkAutospace/cjkKinsoku all true and lineHeight 1.75
    - reading-prefs.ts maps cjk_punct_trim and uses bound params (no string-concat SQL)
    - `cargo test --workspace` exits 0; `pnpm test` exits 0; `pnpm build` exits 0
  </acceptance_criteria>
  <done>SCHEMA_V3 + prefs types/defaults/map ready for CSS and Settings consumers.</done>
</task>

<task type="auto">
  <name>Task 2: Pure CJK helpers + shadcn switch/popover + fixture scaffolds</name>
  <files>src/reader/cjk-feature-detect.ts, src/reader/cjk-feature-detect.test.ts, src/reader/cjk-kinsoku.ts, src/reader/cjk-kinsoku.test.ts, src/reader/cjk-autospace-shim.ts, src/reader/cjk-autospace-shim.test.ts, src/reader/fonts.ts, src/components/ui/switch.tsx, src/components/ui/popover.tsx, package.json, pnpm-lock.yaml, tests/fixtures/cjk/coverage-sheet.html, tests/fixtures/cjk/kinsoku-samples.html</files>
  <read_first>
    - 03-RESEARCH.md Patterns 2, 4, 5; Package Legitimacy Audit (Playwright later; switch/popover official only)
    - 03-PATTERNS.md cjk-feature-detect / cjk-autospace-shim / cjk-kinsoku / fonts.ts / switch+popover
    - 03-UI-SPEC.md Component Inventory (switch, popover); registries stay empty
    - src/reader/fonts.ts (buildFontFaceCss, fontFamilyCssFor, SYSTEM_CJK_STACK import)
    - components.json (radix-nova; registries empty)
    - 03-CONTEXT.md D-35, D-36, D-37, D-38
  </read_first>
  <action>
    **cjk-feature-detect.ts (D-35, DEC-002):**
    - Export `CjkCssCaps` with `textSpacingTrim`, `textAutospace`, `lineBreakStrict` booleans.
    - Export pure `detectCjkCssCaps(cssSupports?: (query: string) => boolean): CjkCssCaps` probing exactly:
      - `text-spacing-trim: normal`
      - `text-autospace: normal`
      - `line-break: strict`
    - Default `cssSupports` uses global `CSS.supports` when available; else all false (tests/SSR soft-fail). Never read OS/API level (D-12).
    - Unit tests with `vi.fn` mock: independent true/false per probe.

    **cjk-kinsoku.ts (CJK-03 tables only — no DOM rewriter):**
    - Export `ZH_PROHIBITED_LINE_START` and `ZH_PROHIBITED_LINE_END` as `as const` string arrays from CLREQ-derived shared zh sets (include 。，、；：？！ and closing brackets/quotes for start; opening 《「『【（ and opening quotes for end — expand reasonably from RESEARCH Pattern 5).
    - Shared Hans/Hant only; no JA-only tables/UI.
    - Snapshot/unit tests: non-empty; known chars present (e.g. `。` start-prohibited, `「` end-prohibited); no runtime layout engine.

    **cjk-autospace-shim.ts (D-36/D-37 Wave 0 contract):**
    - Export `installAutospaceShim(doc: Document): () => void`.
    - Wave 0 minimum: implement reversible path that either no-ops safely OR installs reversible wrappers/highlights without inserting U+0020/U+2009; disposer must restore DOM.
    - Hard bans in comments + tests: no permanent character insertion; concatenated textContent of body must equal pre-install snapshot after install and after dispose.
    - Full Custom Highlight → span → silent degrade priority lands in 03-01; Wave 0 must already lock the textContent invariance contract.

    **fonts.ts stub (D-47 prep for 03-03):**
    - Export `BUNDLED_CJK_FAMILY = "PillowBundledCJK"`.
    - Export `buildBundledCjkFontFaceCss(): string` returning `""` for now (real @font-face in 03-03).
    - Do not yet change `fontFamilyCssFor` stack order (03-03 owns stack).

    **shadcn (official registry only):**
    - From repo root: `pnpm dlx shadcn@4.13.0 add switch popover --yes` (or project-pinned shadcn version if different — match package.json).
    - `components.json` `"registries"` stays empty. Re-pin any new runtime deps to exact versions (no ^/~) and refresh lockfile.
    - Files must land under `src/components/ui/switch.tsx` and `popover.tsx`.

    **Fixtures (D-46 prep):**
    - Create `tests/fixtures/cjk/coverage-sheet.html`: static HTML with SC + TC sample glyphs, mixed Latin/digits, punctuation runs, and a short ransom-note stress line; body uses a placeholder font-family note for later bundled face. No network font URLs.
    - Create `tests/fixtures/cjk/kinsoku-samples.html`: short paragraphs stressing line-start/end prohibited punctuation for visual/golden later.
    - No Playwright pin required in Wave 0 (03-03 owns harness).
  </action>
  <verify>
    <automated>pnpm test && pnpm build && test -f src/components/ui/switch.tsx && test -f src/components/ui/popover.tsx && test -f tests/fixtures/cjk/coverage-sheet.html</automated>
  </verify>
  <acceptance_criteria>
    - cjk-feature-detect.ts exports detectCjkCssCaps and probes the three CSS queries above
    - cjk-kinsoku.ts exports ZH_PROHIBITED_LINE_START and ZH_PROHIBITED_LINE_END
    - cjk-autospace-shim tests assert textContent invariance and disposer restore
    - fonts.ts exports BUNDLED_CJK_FAMILY and buildBundledCjkFontFaceCss
    - switch.tsx and popover.tsx exist; components.json has no third-party registries
    - coverage-sheet.html and kinsoku-samples.html exist under tests/fixtures/cjk/
    - `pnpm test` and `pnpm build` exit 0; no floating version ranges added
  </acceptance_criteria>
  <done>Wave 0 pure helpers, UI primitives, and fixtures ready for 03-01..03.</done>
</task>

</tasks>

## Artifacts this phase produces

| Symbol / path | Kind | Consumed by |
|---------------|------|-------------|
| `SCHEMA_V3`, migration v3 | Rust const + Migration | App SQL plugin; 03-02 prefs persistence |
| `ReadingPrefs.cjkPunctTrim/cjkAutospace/cjkKinsoku` | TS fields | 03-01 CSS, 03-02 Settings |
| `DEFAULT_PREFS` CJK true | TS const | soft-fail load; UI defaults |
| `detectCjkCssCaps`, `CjkCssCaps` | pure module | 03-01 buildReadingCss gating |
| `installAutospaceShim` | pure/DOM module | 03-01 FoliateView / ContinuousScrollStream |
| `ZH_PROHIBITED_LINE_*` | tables | tests + golden fixtures |
| `BUNDLED_CJK_FAMILY`, `buildBundledCjkFontFaceCss` stub | fonts.ts | 03-01 wire; 03-03 fill |
| `src/components/ui/switch.tsx`, `popover.tsx` | UI | 03-02 |
| `tests/fixtures/cjk/*` | fixtures | 03-03 golden |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebView frontend → tauri-plugin-sql | New CJK boolean columns written via existing prefs path |
| Build graph → shadcn/switch/popover deps | Official registry component install |
| Test fixtures → repo | Static HTML only; no remote assets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-sql | Tampering | reading-prefs save/load | mitigate | Bound `$n` params only; columns are 0/1 integers; soft-fail load (T-03-sql) |
| T-03-css | Elevation of Privilege | future CSS builder | mitigate | Prefs remain booleans/numbers; Wave 0 does not concatenate user strings into CSS |
| T-03-dom | Tampering | cjk-autospace-shim | mitigate | textContent invariance + disposer tests; ban space-character insertion (D-37) |
| T-03-agpl | Tampering (legal) | helpers / shadcn | mitigate | Clean-room pure helpers; official shadcn only; no Readest (DEC-001) |
| T-03-SC | Tampering | npm install | mitigate | Exact pins + lockfile; official registry only; no [ASSUMED]/[SUS] packages |
</threat_model>

<verification>
- `cargo test --workspace` (migration v3)
- `pnpm test` (prefs defaults, feature-detect, kinsoku, autospace invariance)
- `pnpm build`
- File existence: switch, popover, coverage-sheet
</verification>

<success_criteria>
- Wave 0 Nyquist gaps closed for SCHEMA_V3, CJK prefs defaults ON, pure helpers, shadcn primitives, fixtures
- D-32, D-34, D-35 (detect module), D-37 (test contract), D-41 honored
- No Settings UI, no Noto binaries, no READER-POS work
</success_criteria>

<output>
Create `.planning/phases/03-cjk-typography-differentiation/03-00-SUMMARY.md` when done.
</output>
