---
phase: 07-webdav-self-hosted-sync
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src-tauri/Cargo.toml
  - Cargo.lock
  - src-tauri/src/migrations.rs
  - src-tauri/tests/migration.rs
  - src/reader/locator-store.ts
  - src/reader/locator-store.test.ts
  - core/src/lib.rs
  - core/src/sync/mod.rs
  - core/src/sync/model.rs
  - core/src/sync/remote.rs
  - core/src/sync/merge.rs
autonomous: true
requirements: [SYNC-02, SYNC-03, SYNC-05]
must_haves:
  truths:
    - "Cargo.lock resolves exactly one reqwest (0.13.x — no 0.12 dragged in) with reqwest_dav 0.3.3 / keyring 4.1.5 / wiremock 0.6.5 exact-pinned; pillowtome-core gains no new runtime dependency"
    - "SCHEMA_V8 applies cleanly forward from V7 — sync_config / sync_state / sync_file_state tables and library_item.deleted + library_item.file_sync_enabled exist; no password column anywhere in the schema"
    - "Every locator upsert appends exactly one change_log row (entity='locator', op='upsert') whose logical_clock is COALESCE(MAX(logical_clock))+1 computed inside the same atomic INSERT"
    - "Merge never drops a record that exists on only one side — merged output is the set union of both inputs (SYNC-05)"
    - "A tombstone wins at equal revision (OR-Set remove-wins) — deleted annotations and books never resurrect"
    - "Hash comparisons never cross algorithms — content_hash is compared only when both records carry the same hash_algo (sha256 annotations vs blake3 works)"
  artifacts:
    - path: "src-tauri/Cargo.toml"
      provides: "Exact-pinned sync deps: reqwest_dav =0.3.3, keyring =4.1.5 (+android-native-keyring-store), dev wiremock =0.6.5"
      contains: "reqwest_dav = \"=0.3.3\""
    - path: "src-tauri/src/migrations.rs"
      provides: "SCHEMA_V8 migration: library_item.deleted/file_sync_enabled + sync_config/sync_state/sync_file_state, registered at version 8 (append-only; SCHEMA_V1..V7 untouched)"
      contains: "sync_webdav_state"
    - path: "src/reader/locator-store.ts"
      provides: "Progress upsert now appends one change_log row (entity='locator') with the same atomic monotonic-clock INSERT as annotation-store"
      exports: ["upsertLocator", "loadLocator", "ensureWorkRow", "relocateToLocatorRow"]
    - path: "core/src/sync/model.rs"
      provides: "Serde types for the remote device-state file per RESEARCH Pattern 1 (format:1, self-describing, BTreeMap-keyed)"
      exports: ["DeviceStateFile", "DeviceRecord", "Manifest", "ProgressRec", "AnnotationRec", "LibraryRec", "FileSyncRec"]
    - path: "core/src/sync/remote.rs"
      provides: "Pure remote-path hygiene: segment sanitize, per-segment percent-encode, root jail, human-readable book names with [hash8] collision suffix"
      exports: ["REMOTE_FORMAT", "DEFAULT_ROOT", "normalize_root", "sanitize_segment", "join_remote", "book_remote_path", "state_file_path", "state_tmp_file_path", "device_file_path", "manifest_path"]
    - path: "core/src/sync/merge.rs"
      provides: "Pure merge functions + MergeOutcome; set-union map drivers used by reconcile and by the no-data-loss property matrix"
      exports: ["merge_progress", "merge_annotation", "merge_library", "MergeOutcome", "MergeWinner"]
  key_links:
    - from: "src/reader/locator-store.ts"
      to: "change_log"
      via: "one INSERT per upsert with COALESCE(MAX(logical_clock))+1 inside the statement"
      pattern: "INSERT INTO change_log"
    - from: "src/reader/locator-store.ts"
      to: "src/reader/annotation-store.ts"
      via: "ensureDevice() import for the shared device_id bootstrap (no duplicate bootstrap logic)"
      pattern: "ensureDevice"
    - from: "src-tauri/src/migrations.rs"
      to: "sync tables"
      via: "SCHEMA_V8 registered as Migration version 8 at the END of the migrations() vec"
      pattern: "sync_webdav_state"
    - from: "core/src/sync/merge.rs"
      to: "core/src/sync/model.rs"
      via: "pure functions over the shared record types (ProgressRec / AnnotationRec / LibraryRec)"
      pattern: "MergeOutcome"
---

<objective>
Land the Wave 0 foundation for WebDAV sync so every later plan (connect/keychain, state plane, file plane, scheduler/UI) builds on settled ground: (1) exact-pinned transport + keychain dependencies with a single reqwest 0.13 in the lockfile (D-13); (2) SCHEMA_V8 — the append-only sync tables plus the `library_item.deleted` catalog tombstone (research Q2 ADOPTED: deletions must not resurrect via set-union) and `file_sync_enabled` (D-98); (3) close the locator change_log gap — today only annotation-store writes the ledger, so progress would be invisible to the reconcile spine (SYNC-02 blocker called out in 07-RESEARCH § Architecture Patterns 现状缺口); (4) the pure sync core in `pillowtome-core`: remote-file serde model, remote-path hygiene, and the deterministic merge engine with exhaustive off-device fixtures (SYNC-03/SYNC-05).

Purpose: Phase 7 is a reconcile, not a rewrite — this plan lands everything that must exist BEFORE any network code: the schema the reconcile writes into, the ledger rows progress sync consumes, and the merge semantics proven never to lose data.
Output: pinned deps + committed Cargo.lock; V8 migration + extended off-device migration test; locator change_log append + extended vitest; `core/src/sync/{mod,model,remote,merge}.rs` with in-module fixtures. No network, no IPC, no UI, no Android spike (those are 07-01+; AVD gate not triggered by this plan — nothing here is device-visible).

Planner decisions already made (state, do not re-open): research Q1 ADOPTED (generic RFC-4918 servers get streaming whole-file PUT + retry; only Nextcloud gets chunk v2 — an explicit correction to D-101, lands in the 07-03 file-plane plan); research Q2 ADOPTED (`library_item.deleted` tombstone — lands HERE in V8); Q3 per UI-SPEC §3 (downloading card = progress state, tap disabled — 07-03/07-04); chunk threshold 10MB / chunk size 10MB (sync_file_state table landed here is sized for it; planner lands the state machine in 07-03); remote format version `manifest.json` `format:1` (REMOTE_FORMAT=1 in remote.rs); device name user-editable in settings (sync_config.device_name column); scheduler = open-book pull + close-book push + manual button only, NO background timers (D-90/D-91 — nothing scheduled in this plan).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-webdav-self-hosted-sync/07-CONTEXT.md
@.planning/phases/07-webdav-self-hosted-sync/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin sync dependencies — reqwest_dav / keyring / wiremock, single reqwest 0.13 in lock</name>
  <files>src-tauri/Cargo.toml, Cargo.lock</files>
  <read_first>
    - src-tauri/Cargo.toml ([dependencies] at lines 18-26 — exact-pin style with `=` and Cargo.lock-committed comment at line 19; [dev-dependencies] at 37-43 already carry sqlx =0.8.6 + tokio =1.52.3 for the off-device migration test)
    - core/Cargo.toml (runtime deps at 8-20: uuid/blake3/zip/thiserror/serde/quick-xml — this file MUST NOT change; core gets no new runtime deps)
    - Cargo.lock (verify the single `name = "reqwest"` entry is 0.13.4 — grep around line 3208; tao is 0.35.3, tauri-plugin-sql 2.4.0)
    - .planning/phases/07-webdav-self-hosted-sync/07-RESEARCH.md § Standard Stack + Package Legitimacy Audit (pin rationale: reqwest_dav 0.3.3 sits on reqwest 0.13 — pinning the older 0.2.1 would drag in reqwest 0.12 as a second version)
  </read_first>
  <action>
    Edit `src-tauri/Cargo.toml` ONLY (core/Cargo.toml stays byte-identical):

    - In `[dependencies]`, after the `tauri-plugin-opener` line, add with a short style-matching comment (exact pins, D-13; Android store feature is target-gated — desktop uses the default v1 store: Windows Credential Manager / macOS Keychain / Linux Secret Service):
      ```toml
      # WebDAV transport (SYNC-01..05): high-level PROPFIND/GET/PUT/MKCOL; conditional
      # headers / chunking / Range go through its public `agent` (07-RESEARCH Pattern 3).
      reqwest_dav = "=0.3.3"
      # OS keychain for the WebDAV password (SYNC-01 — password never in SQLite/IPC/logs).
      keyring = { version = "=4.1.5", features = ["android-native-keyring-store"] }
      ```
    - In `[dev-dependencies]`, after the `tokio` line, add:
      ```toml
      # Off-device WebDAV transport test server (Wave 1 transport tests; 07-RESEARCH Don't Hand-Roll).
      wiremock = "=0.6.5"
      ```
    - Regenerate the lock: run `cargo check --workspace` from the repo root (this resolves the new crates, type-checks both crates, and rewrites Cargo.lock). Cargo.lock is committed — D-13.
    - Verify reqwest unicity: reqwest_dav 0.3.3 depends on reqwest 0.13, which must unify with the existing 0.13.4 entry. If a second reqwest (0.12.x) appears in Cargo.lock, STOP — do not proceed; report the dependency conflict (the likely cause is a wrong reqwest_dav version).
    - Do NOT run `cargo update` (it would float transitive pins); do NOT touch `[target.'cfg(target_os = "android")'.dependencies]` or the workspace Cargo.toml.
  </action>
  <verify>
    <automated>cd /d/Github/Pillowtome && cargo check --workspace</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'reqwest_dav = "=0.3.3"' src-tauri/Cargo.toml` = 1; `grep -c '"=4.1.5"' src-tauri/Cargo.toml` = 1 and the keyring line carries `features = ["android-native-keyring-store"]`; `grep -c 'wiremock = "=0.6.5"' src-tauri/Cargo.toml` = 1.
    - `grep -c '^name = "reqwest"$' Cargo.lock` = 1 and `awk '/^name = "reqwest"$/{getline; print; exit}' Cargo.lock` prints `version = "0.13.x"` (no 0.12 anywhere as a second reqwest entry).
    - `awk '/^name = "reqwest_dav"$/{getline; print; exit}' Cargo.lock` = `version = "0.3.3"`; same awk pattern yields `version = "4.1.5"` for `keyring` and `version = "0.6.5"` for `wiremock`.
    - `git diff --stat -- core/Cargo.toml` is empty (no new core runtime deps).
    - `cargo check --workspace` exits 0 (MSVC toolchain required on this machine — GNU gcc exits 1 silently per MEMORY).
  </acceptance_criteria>
  <done>Deps exact-pinned, workspace type-checks, lockfile regenerated with a single reqwest 0.13 and committed; core untouched.</done>
</task>

<task type="auto">
  <name>Task 2: SCHEMA_V8 migration — sync_webdav_state (library tombstone + file-sync flag + three sync tables, append-only)</name>
  <files>src-tauri/src/migrations.rs, src-tauri/tests/migration.rs</files>
  <read_first>
    - src-tauri/src/migrations.rs (SCHEMA_V4 at lines 103-118 — CONFIRM the real `library_item` column set: item_id, work_id UNIQUE, source_id, title, author, cover_file, imported_at, last_opened_at, last_read_at; the two V8 ALTERs target this table. SCHEMA_V7 at 148-174; the `migrations()` registration vec at 184-229 with version 7 last)
    - src-tauri/tests/migration.rs (off-device pattern: `fresh_db_v7()` chain at 70-77 applying raw SQL against `sqlite::memory:`; `has_column` helper at 89-96; V7 table/column assertions at 308-391; `migration_set_is_v1_through_v7_up` at 393-447 asserting len 7)
    - .planning/phases/07-webdav-self-hosted-sync/07-RESEARCH.md (Pattern 1 — why the catalog needs a tombstone; Q2 — adopted for `library_item.deleted`; Security Domain — no password column anywhere)
  </read_first>
  <action>
    Add `pub const SCHEMA_V8: &str` after SCHEMA_V7 and register it as `Migration { version: 8, description: "sync_webdav_state", sql: SCHEMA_V8, kind: MigrationKind::Up }` at the END of the `migrations()` vec. Do NOT alter SCHEMA_V1..V7 or any existing registration — the diff of `src-tauri/src/migrations.rs` must contain only added lines (extend the `migrations()` doc comment with one v8 line).

    SCHEMA_V8 DDL (exactly this shape; short `--` comments matching the file's existing style are fine):
    ```sql
    ALTER TABLE library_item ADD COLUMN deleted INTEGER NOT NULL DEFAULT 0;
    ALTER TABLE library_item ADD COLUMN file_sync_enabled INTEGER NOT NULL DEFAULT 0;

    CREATE TABLE sync_config (
        id                TEXT    PRIMARY KEY,   -- single row id='config'
        server_url        TEXT    NOT NULL,
        username          TEXT    NOT NULL,
        remote_path       TEXT    NOT NULL DEFAULT 'pillowtome/',
        allow_http        INTEGER NOT NULL DEFAULT 0,
        trust_self_signed INTEGER NOT NULL DEFAULT 0,
        device_name       TEXT,
        updated_at        INTEGER NOT NULL
    );

    CREATE TABLE sync_state (
        id           TEXT    PRIMARY KEY,        -- single row id='state'
        remote_etag  TEXT,                        -- opaque token: equality only, never parsed
        last_sync_at INTEGER,
        last_error   TEXT,
        syncing      INTEGER NOT NULL DEFAULT 0
    );

    CREATE TABLE sync_file_state (
        work_id       TEXT    PRIMARY KEY,
        direction     TEXT    NOT NULL,           -- 'upload' | 'download'
        transfer_uuid TEXT,
        chunks_done   TEXT    NOT NULL DEFAULT '[]', -- JSON array of confirmed chunk numbers
        size          INTEGER,
        hash          TEXT,                        -- blake3 hex of the book file
        remote_path   TEXT,
        started_at    INTEGER NOT NULL,
        updated_at    INTEGER NOT NULL
    );
    ```
    Rationale anchors: `library_item.deleted` = research Q2 ADOPTED (catalog tombstone — merge set-union must not resurrect deleted books; remote book files are NEVER deleted by sync); `file_sync_enabled` = D-98 per-book opt-in flag that travels on the state plane; `sync_config` holds NO password (keychain only, SYNC-01); the two TLS switches are independent columns (D-95); `sync_file_state` is the chunk-resume state the 10MB/10MB file plane (07-03) persists.

    Extend `src-tauri/tests/migration.rs`:
    - Add `SCHEMA_V8` to the `use pillowtome_lib::migrations::{...}` import (line 9-11) and a `fresh_db_v8()` that applies SCHEMA_V8 on top of `fresh_db_v7()`.
    - New test `schema_v8_adds_library_tombstone_and_file_sync_flag`: on `fresh_db_v8()`, assert `has_column(conn, "library_item", "deleted")` and `has_column(conn, "library_item", "file_sync_enabled")`; seed a work + library_item (reuse the V4 test's insert shape) and assert both new columns read back 0 by default; assert the V4 columns (title, source_id, cover_file, last_opened_at, last_read_at) still exist (forward-from-V7 append-only proof).
    - New test `schema_v8_creates_sync_tables`: on `fresh_db_v8()`, assert tables `sync_config`, `sync_state`, `sync_file_state` exist; assert the key columns (`server_url`, `username`, `remote_path`, `allow_http`, `trust_self_signed`, `device_name` on sync_config; `remote_etag`, `last_sync_at`, `last_error`, `syncing` on sync_state; `direction`, `transfer_uuid`, `chunks_done`, `size`, `hash`, `remote_path`, `started_at` on sync_file_state); insert a minimal sync_config row (id, server_url, username, updated_at) and assert `remote_path` defaults to `pillowtome/`; insert a minimal sync_file_state row (work_id, direction, started_at, updated_at) and assert `chunks_done` defaults to `[]`; insert sync_state id='state' and assert `syncing` defaults to 0. Assert no column named like `password` exists on sync_config (PRAGMA table_info scan — credentials never in SQLite, SYNC-01).
    - Update `migration_set_is_v1_through_v7_up` → rename to `migration_set_is_v1_through_v8_up`: len 8; `set[7].version == 8`, `description == "sync_webdav_state"`, `sql == SCHEMA_V8`, contains `CREATE TABLE sync_config`, `sync_state`, `sync_file_state`, `ALTER TABLE library_item ADD COLUMN deleted`, `file_sync_enabled`; keep all V1..V7 slot assertions unchanged.
    - Update the stale file-header doc comment ("schema v1 + v2 + v3") to say the suite covers v1..v8.
  </action>
  <verify>
    <automated>cd /d/Github/Pillowtome/src-tauri && cargo test --test migration</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "SCHEMA_V8" src-tauri/src/migrations.rs` ≥ 2 (const decl + registration); version 8 description is `sync_webdav_state`; `migrations()` returns 8 entries.
    - SCHEMA_V8 contains `ALTER TABLE library_item ADD COLUMN deleted INTEGER NOT NULL DEFAULT 0`, `ALTER TABLE library_item ADD COLUMN file_sync_enabled INTEGER NOT NULL DEFAULT 0`, `CREATE TABLE sync_config`, `CREATE TABLE sync_state`, `CREATE TABLE sync_file_state`; `grep -ci 'password' src-tauri/src/migrations.rs` = 0.
    - `git diff -U0 -- src-tauri/src/migrations.rs | grep -c '^-[^-]'` = 0 (append-only: no line of V1..V7 or of the existing vec touched).
    - `cargo test --test migration` passes including the new V8 assertions (MSVC toolchain required — GNU gcc exits 1 silently per MEMORY).
  </acceptance_criteria>
  <done>V8 applies cleanly on a fresh DB and forward from V7; migration set is 8 entries; no credential column exists; migration test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: locator-store change_log append — close the progress-ledger gap (SYNC-02 unblock)</name>
  <files>src/reader/locator-store.ts, src/reader/locator-store.test.ts</files>
  <behavior>
    - upsertLocator(row) still upserts the locator row on UNIQUE(work_id) exactly as today, THEN appends exactly one `change_log` row (entity='locator', op='upsert') with payload `{work_id, cfi, progress_fraction, text_pre, text_exact, text_post, updated_at}`.
    - The appended row's logical_clock = COALESCE(MAX(logical_clock) for this device_id, 0)+1 computed INSIDE the INSERT (single atomic statement — same pattern as annotation-store).
    - device_id comes from the EXISTING exported `ensureDevice()` of annotation-store (imported, not duplicated).
    - The empty-upsert guard (no cfi AND null progress_fraction) still returns early — no locator write, no ledger row.
    - device_id unavailable → locator write already done stays; ledger append is skipped with a console.warn (mirror annotation-store's guard). Any SQL failure keeps today's contract: console.warn + rethrow (upsertLocator rethrows; FoliateView callers already catch).
    - touchLastRead (D-65) still fires after the ledger append, unchanged.
  </behavior>
  <read_first>
    - src/reader/locator-store.ts (upsertLocator at lines 86-129: empty-upsert guard 95-98, locator INSERT…ON CONFLICT 102-122, `void touchLastRead(...)` at 124, catch-warn-rethrow at 125-128)
    - src/reader/annotation-store.ts (`ensureDevice()` exported at 84-103 — INSERT OR IGNORE sync_meta + cached read; private `appendChangeLog` at 111-140 — the atomic `COALESCE((SELECT MAX(logical_clock) …) + 1` INSERT at 131-139 with entity literal embedded; the ledger target shape)
    - src/reader/annotation-store.test.ts (lines 3-13: the `vi.hoisted` execSpy/selectSpy + `vi.mock("@tauri-apps/plugin-sql")` pattern; beforeEach reset at 46-51)
    - src/reader/locator-store.test.ts (112 lines, currently PURE-function tests only — no plugin-sql mock exists yet; it must be ADDED at the top)
    - src-tauri/src/migrations.rs (change_log columns at 41-49: id, device_id, logical_clock, entity, op, payload, created_at)
  </read_first>
  <action>
    Edit `src/reader/locator-store.ts`:
    - Add `import { ensureDevice } from "./annotation-store";` (acyclic: annotation-store imports nothing from locator-store).
    - Add a private `appendLocatorChangeLog(db, deviceId, row, updatedAt)` mirroring annotation-store's `appendChangeLog`: one `INSERT INTO change_log (id, device_id, logical_clock, entity, op, payload, created_at) VALUES ($1, $2, COALESCE((SELECT MAX(logical_clock) FROM change_log WHERE device_id = $2), 0) + 1, 'locator', $3, $4, $5)` with binds `[crypto.randomUUID(), deviceId, "upsert", payload, updatedAt]`, where `payload = JSON.stringify({ work_id, cfi, progress_fraction, text_pre, text_exact, text_post, updated_at })` (exactly these seven keys — the locator row shape; no hash fields, locator has none).
    - In `upsertLocator`, inside the existing try AFTER the locator `db.execute` and BEFORE `touchLastRead`: `const deviceId = await ensureDevice();` — if null, `console.warn("[locator-store] change_log skipped: no device_id")` and skip the append; otherwise `await appendLocatorChangeLog(db, deviceId, row, updatedAt)`. Keep the existing catch (warn + rethrow) untouched. Update the file header comment to note the entity='locator' ledger append (SYNC-02).

    Edit `src/reader/locator-store.test.ts` — add at the very top the `vi.hoisted` + `vi.mock("@tauri-apps/plugin-sql")` block copied from annotation-store.test.ts lines 3-13 (existing pure-function tests are unaffected — they never touch Database), plus a `beforeEach` clearing both spies. New `describe("upsertLocator change_log (SYNC-02)")` cases:
    1. Upsert appends the ledger row: after `await upsertLocator({work_id:"w1", cfi:"epubcfi(/6/4)", progress_fraction:0.5, …})`, assert `execSpy.mock.calls` contains a call whose SQL includes `INSERT INTO change_log` AND the literal `'locator'`, and whose SQL includes `COALESCE((SELECT MAX(logical_clock)`. IMPORTANT: never assert exact call counts — `touchLastRead` (library-store) issues extra SQL on the same mocked Database; always filter calls by SQL content (the `sqlOf` helper pattern from annotation-store.test.ts).
    2. Payload shape: locate the change_log call, JSON.parse its payload bind (4th bind), assert keys exactly `{work_id, cfi, progress_fraction, text_pre, text_exact, text_post, updated_at}` with work_id "w1".
    3. Empty upsert (`cfi: null, progress_fraction: null`) → early return → no `INSERT INTO locator` and no `INSERT INTO change_log` among exec calls.
    4. SQL failure path: `execSpy.mockRejectedValueOnce(new Error("db down"))` → `await expect(upsertLocator(…)).rejects.toThrow("db down")` (existing rethrow contract preserved).
    5. device_id failure path: make the FIRST execute (ensureDevice's INSERT OR IGNORE) reject once, subsequent calls succeed → locator INSERT still happened, no `INSERT INTO change_log` was issued (ledger skipped, not fatal).
    - All locator upsert + ledger SQL uses `$n` binds only — no template interpolation of values into SQL strings.
  </action>
  <verify>
    <automated>cd /d/Github/Pillowtome && pnpm test src/reader/locator-store.test.ts && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'INSERT INTO change_log' src/reader/locator-store.ts` ≥ 1 and the statement contains the `'locator'` entity literal and `COALESCE((SELECT MAX(logical_clock)`.
    - `grep -n 'ensureDevice' src/reader/locator-store.ts` shows the import from `./annotation-store`; no sync_meta bootstrap logic is duplicated into locator-store.
    - Payload construction contains all seven keys: work_id, cfi, progress_fraction, text_pre, text_exact, text_post, updated_at.
    - `grep -c 'DELETE FROM' src/reader/locator-store.ts` = 0; `grep -c '\${' src/reader/locator-store.ts` on SQL template literals = 0 for value interpolation (binds only).
    - `pnpm test src/reader/locator-store.test.ts` green (old pure-function cases + 5 new ledger cases); `pnpm exec tsc --noEmit` clean.
  </acceptance_criteria>
  <done>Every locator upsert appends one monotonic-clock change_log row (entity='locator'); empty upserts still skip; failure contracts unchanged; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: core/src/sync/{mod,model,remote,merge}.rs — remote model, path hygiene, deterministic merge engine + exhaustive fixtures</name>
  <files>core/src/sync/mod.rs, core/src/sync/model.rs, core/src/sync/remote.rs, core/src/sync/merge.rs, core/src/lib.rs</files>
  <behavior>
    - model.rs: serde types mirror the RESEARCH Pattern 1 JSON shape exactly; deserialize tolerates missing sections (`#[serde(default)]` maps); `DeviceStateFile::validate()` rejects format≠1 and out-of-range progress_fraction (V5 input validation — remote JSON is untrusted).
    - remote.rs: any path derived from user/server data is jailed under the normalized root — `..`, absolute paths, drive letters, separators inside segments, `/\:*?"<>|` and control chars are rejected at the single point; every built path is per-segment percent-encoded and has no trailing slash.
    - merge.rs: progress furthest-wins by (progress_fraction → updated_at → device_id) — a documented total order, deterministic under argument swap; annotation by-id union with (revision → tombstone remove-wins → content_hash same-algo → updated_at → device_id) and a non-destructive conflict copy; library by-work union with tombstone anti-resurrection and file_sync flag union; nothing existing on only one side is ever dropped.
    - MergeOutcome carries `replaced_local` — the pre-jump local row — whenever a remote winner replaces an existing local row (D-92 undo payload).
    - Hash comparisons happen ONLY when both records carry the same `hash_algo` (annotation sha256 vs work blake3 — never cross-compare).
  </behavior>
  <read_first>
    - core/src/lib.rs (seam-module doc + `pub mod` list at 18-22 — add `pub mod sync;` and one doc bullet, matching the seam style)
    - core/Cargo.toml (what you may use: serde =1.0.228 derive, blake3 =1.8.5, thiserror =2.0.18, uuid =1.23.4; serde_json =1.0.150 is DEV-only — fine for tests. NO new runtime deps; note percent-encoding is NOT a direct dep — hand-roll the small RFC-3986 encoder inside remote.rs instead of adding one)
    - core/src/locator.rs (in-module `#[cfg(test)] mod tests` convention at line 50 — all sync fixtures live in-module, not in core/tests/)
    - .planning/phases/07-webdav-self-hosted-sync/07-RESEARCH.md (Pattern 1 JSON shape at lines 215-241 — the authoritative serde target; Pattern 2 merge rules; Pitfall 5 tmp-name + MOVE; Pitfall 6 hash_algo; Pitfall 8 ETag opacity / path quirks)
    - .planning/phases/07-webdav-self-hosted-sync/07-CONTEXT.md (D-104 root default/custom; D-105 human-readable names with short-hash collision suffix)
  </read_first>
  <action>
    **core/src/sync/mod.rs** — crate-doc for the sync core (pure, off-device tested; IO lives in src-tauri's sync plane) and `pub mod merge; pub mod model; pub mod remote;`. In `core/src/lib.rs` add `pub mod sync;` after `pub mod source;` plus one seam doc bullet (`sync` — WebDAV sync pure core: remote model, path hygiene, merge; plan 07-00).

    **core/src/sync/model.rs** — serde types for the remote layout (use `std::collections::BTreeMap` for deterministic iteration; derive `Debug, Clone, PartialEq, Serialize, Deserialize`):
    - `Manifest { format: u32, app: String }` + `pub const REMOTE_FORMAT: u32 = 1;` + `manifest_json()` returning the canonical compact string `{"format":1,"app":"pillowtome"}`.
    - `DeviceRecord { device_id, device_name, first_seen, last_seen }` (devices/<device_id>.json).
    - `ProgressRec { cfi: Option<String>, progress_fraction: Option<f64>, text_pre/text_exact/text_post: Option<String>, updated_at: i64 }`.
    - `AnnotationRec { work_id, annotation_type (serde rename "type"), cfi, color, text_pre, text_exact, text_post, progress_fraction, note, created_at, updated_at, revision, content_hash, hash_algo, deleted }` — mirrors the annotation table incl. tombstone; `hash_algo` rides per record (Pitfall 6).
    - `FileSyncRec { enabled: bool, remote_path: Option<String>, size: Option<i64>, hash: Option<String> }` and `LibraryRec { title, author: Option<String>, format, content_hash, imported_at, deleted, file_sync: Option<FileSyncRec> }` (keyed by work_id in the map — do NOT embed work_id inside; local-only V4 columns item_id/source_id/cover_file/last_opened_at/last_read_at are intentionally absent — the reconcile layer preserves them, merge never sees them).
    - `DeviceStateFile { format: u32, device_id, device_name, clock: i64, updated_at: i64, #[serde(default)] progress: BTreeMap<String, ProgressRec>, #[serde(default)] annotations: BTreeMap<String, AnnotationRec>, #[serde(default)] library: BTreeMap<String, LibraryRec> }` + `validate() -> Result<(), ModelError>` (thiserror): format == REMOTE_FORMAT, device_id non-empty, every progress_fraction in 0.0..=1.0. ModelError variants: `UnsupportedFormat(u32)`, `InvalidFraction(f64)`, `EmptyDeviceId`.

    **core/src/sync/remote.rs** — pure path hygiene (single point for ALL remote paths; the jail):
    - `pub const DEFAULT_ROOT: &str = "pillowtome";`
    - `normalize_root(root: &str) -> Result<String, RemoteError>`: trim; empty → DEFAULT_ROOT; strip trailing `/`; reject `..` segments, leading `/`, `\`, drive-letter `:`, control chars, and any segment failing `sanitize_segment`. (The DB default literal `pillowtome/` from V8 normalizes to `pillowtome` here — stored form keeps D-104's human-readable trailing slash, wire form never has one.)
    - `sanitize_segment(seg: &str) -> Result<&str, RemoteError>`: reject empty, `.`, `..`, any of `/\:*?"<>|`, and control chars (`char::is_control`).
    - private `percent_encode_segment(&str) -> String` — hand-rolled RFC 3986: keep `A-Z a-z 0-9 -._~`, percent-encode every other UTF-8 byte as `%XX` uppercase (covers CJK, space, `#`, `%`, `&`, `+`; NO new dependency).
    - `join_remote(root: &str, segments: &[&str]) -> Result<String, RemoteError>` — normalize_root + sanitize each segment + percent-encode each segment + join `/` (never a trailing slash).
    - Book naming (D-105): `sanitize_book_component(&str) -> String` REPLACES forbidden/control chars with `_` and trims trailing dots/spaces (book titles may legally contain `:` etc. — rejection would lose books; fallback `"untitled"` when empty). `book_file_name(author, title, ext, work_hash: &str, collision: bool) -> String` → `作者 - 书名.epub`, or `作者 - 书名 [hash8].epub` when collision — hash8 = first 8 chars of the blake3 hex work_hash (work_id IS the blake3 content hash, D-…02 lock). `book_remote_path(root, author, title, ext, work_hash, collision) -> Result<String, RemoteError>` → `<root>/books/<name>` via join_remote.
    - `state_file_path(root, device_id)`, `state_tmp_file_path(root, device_id, nonce)` (`state/<device_id>.json.tmp-<nonce>` — Pitfall 5 temp-then-MOVE; nonce supplied by caller, fn stays pure), `device_file_path(root, device_id)` (`devices/<device_id>.json`), `manifest_path(root)`.
    - `RemoteError` (thiserror): `InvalidRoot(String)`, `InvalidSegment(String)`.
    - Fixtures (in-module): injection battery — `..`, `a/b`, `a\b`, absolute `/etc`, `C:\x`, `con?trol`, `\u{0007}`, trailing-slash roots, empty root → `pillowtome`; every built path starts with the normalized root and contains no `..` and no trailing slash; CJK + space + `#` + `%` percent-encoded per segment; `作者 - 书名.epub` happy path; collision adds ` [hash8]`; `manifest_path("pillowtome/")` = `pillowtome/manifest.json`.

    **core/src/sync/merge.rs** — pure merge over model.rs types:
    ```rust
    pub enum MergeWinner<T> { Local(T), Remote(T), Both { kept: T, conflict_copy: T } }
    pub struct MergeOutcome<T> { pub winner: MergeWinner<T>, pub replaced_local: Option<T> }
    ```
    - `merge_progress(local: Option<&ProgressRec>, remote: &ProgressRec, local_device: &str, remote_device: &str) -> MergeOutcome<ProgressRec>` — total order: progress_fraction (None sorts BELOW any Some; None==None ties) → updated_at (later wins) → device_id (lexicographically GREATER wins). Winner Remote ⇒ `replaced_local = Some(local.clone())` when local existed (D-92 undo payload); winner Local ⇒ None. None local ⇒ Remote, None.
    - `merge_annotation(local: Option<&AnnotationRec>, remote: &AnnotationRec, local_device: &str, remote_device: &str, conflict_copy_id: &str) -> MergeOutcome<AnnotationRec>` — None local ⇒ Remote (union keeps remote-only records, tombstones included). Some: (1) higher `revision` wins outright (revision lineage is authoritative — no copy); (2) equal revision, exactly one `deleted == 1` ⇒ the TOMBSTONE wins (OR-Set remove-wins; our tombstone rows retain all content columns, so removal is non-destructive); (3) equal revision, same deletedness: if `hash_algo` equal AND `content_hash` equal ⇒ Local idempotent no-op; otherwise (hash differs, or hash_algo differs — NEVER compare across algorithms) ⇒ `Both { kept, conflict_copy }`: kept = chain winner by (updated_at later → device_id greater); conflict_copy = the losing side re-keyed to `conflict_copy_id` with `note` prefixed `冲突副本` (`冲突副本：原笔记` when a note exists, bare `冲突副本` otherwise), keeping the loser's revision/content_hash/timestamps. `replaced_local` = Some(local) whenever the local row is displaced from its id (Remote winner), else None.
    - `merge_library(local: Option<&LibraryRec>, remote: &LibraryRec, local_device: &str, remote_device: &str) -> MergeOutcome<LibraryRec>` — None ⇒ Remote. Both tombstones ⇒ Local idempotent. Exactly one tombstone ⇒ tombstone wins (Q2 anti-resurrection). Both live ⇒ deterministic kept by (imported_at later → device_id greater); `file_sync` result = union: `enabled` if either side enabled, `remote_path/size/hash` taken from an enabled side (kept side preferred). `replaced_local` per the same rule.
    - Set-level drivers (the actual reconcile API + property-test target): `merge_progress_map`, `merge_annotation_map`, `merge_library_map`, each taking `local: &BTreeMap<String, T>`, `remotes: &[(String, BTreeMap<String, T>)]` (device_id → records), `local_device: &str` (annotation map also takes `id_alloc: &mut dyn FnMut() -> String` for conflict-copy ids — uuid generation stays OUT of the pure core; tests pass a counter closure). Iterate the UNION of keys: local-only keys pass through verbatim; remote-only keys merge in via the per-record fns; conflict copies ADD new keys. progress map returns `(BTreeMap<String, ProgressRec>, Vec<(String, ProgressRec)>)` — merged map + (work_id, replaced local) undo stash. Annotation/library maps return the merged map.
    - Fixtures (in-module `#[cfg(test)]`, serde_json dev-dep available):
      - progress: remote further ⇒ Remote + replaced_local=Some; local further ⇒ Local + None; equal fraction ⇒ updated_at decides; full tie ⇒ device_id decides AND swapping argument order yields the same winning record (determinism); None-fraction rows order below any Some.
      - annotation: remote-only record survives (union); higher revision wins; equal-revision tombstone vs live ⇒ tombstone (anti-resurrection); equal revision + same hash ⇒ idempotent Local; equal revision + different hash ⇒ Both, copy id = the supplied conflict id, note starts with `冲突副本`; DUAL hash_algo fixture (local sha256-tagged vs remote blake3-tagged, same id/revision) ⇒ no cross-algorithm compare happens ⇒ conflict-copy fallback, not a hash-equality verdict.
      - library: remote-only work survives; tombstone vs live ⇒ tombstone; both live ⇒ file_sync enabled union; deterministic kept under argument swap.
      - Property-style matrix (no rand dep — hand-roll a tiny LCG with a fixed seed): two devices perform interleaved create/edit/delete ops over a fixed pool of annotation ids (~200 ops, revision bumps per edit, deletes tombstone), then `merge_annotation_map` runs (a) A-local/B-remote and (b) B-local/A-remote. Assert in BOTH directions: union completeness — every annotation_id ever created on either side is present in the merged output under its own id or as a conflict copy (SYNC-05 no-data-loss); determinism — same inputs merged twice give byte-identical maps; idempotence — merging the output again against the same remotes changes nothing.
  </action>
  <verify>
    <automated>cd /d/Github/Pillowtome && cargo test -p pillowtome-core sync</automated>
  </verify>
  <acceptance_criteria>
    - `ls core/src/sync/` = mod.rs, model.rs, remote.rs, merge.rs; `grep -n 'pub mod sync' core/src/lib.rs` present.
    - `grep -c 'pub fn merge_progress\|pub fn merge_annotation\|pub fn merge_library' core/src/sync/merge.rs` = 3 (plus map drivers); `grep -c '冲突副本' core/src/sync/merge.rs` ≥ 1; `MergeOutcome` has exactly `winner` + `replaced_local` fields.
    - `grep -n 'unsupported_format\|UnsupportedFormat' core/src/sync/model.rs` present; remote.rs rejects `/\:*?"<>|` and control chars and contains a hand-rolled `percent_encode_segment` (no percent-encoding crate added).
    - `git diff --stat -- core/Cargo.toml` empty (no new runtime deps).
    - `cargo test -p pillowtome-core sync` green — including the dual-device interleavings, equal-revision tombstone, dual hash_algo, conflict-copy, and fixed-seed union/determinism/idempotence cases (MSVC toolchain note applies to all cargo runs on this machine).
  </acceptance_criteria>
  <done>The pure sync core compiles into pillowtome-core with zero new deps; merge fixtures prove set-union (no data loss), tombstone remove-wins, per-record hash_algo, and deterministic total orders; path hygiene jails everything under the configured root.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebDAV server → core model (future pulls) | Remote state JSON is untrusted input — deserialized and validated before any merge (V5) |
| Local store → remote paths | Book titles/authors/configured root cross into server-side paths — injection into sibling keys or the server root must be impossible (V4) |
| SQLite sync tables | sync_config holds server URL/username but NEVER the password (SYNC-01) |
| Cargo supply chain | New crates enter the tree — exact pins + committed lock only (D-13) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-00-01 | Tampering | Remote path construction from title/author/root | mitigate | remote.rs is the single point: segment whitelist rejects `/\:*?"<>|` + control chars + `..` + absolute/drive forms; per-segment percent-encode; every path jailed under the normalized configured root, never a trailing slash (07-RESEARCH § Security Domain V4/V5, Pitfall 8) |
| T-07-00-02 | Info Disclosure | Credential storage | mitigate | SCHEMA_V8 sync_config has no password column (test asserts); keyring =4.1.5 pinned for the OS keychain (SYNC-01); this plan stores/transmits no credential yet; no password in logs/IPC/toasts ever (RESEARCH Pattern 4) |
| T-07-00-03 | Tampering | Merge silently dropping records | mitigate | Set-union drivers + fixed-seed property matrix asserting union completeness, determinism, idempotence in both merge directions; tombstone remove-wins; conflict copy `冲突副本` instead of silent drop (SYNC-05, PITFALLS #7) |
| T-07-00-04 | Tampering | hash_algo confusion (sha256 vs blake3) | mitigate | content_hash compared only when both records carry identical `hash_algo`; dual-algorithm fixture proves no cross-compare (Pitfall 6) |
| T-07-00-05 | DoS / Tampering | Malformed remote state JSON | mitigate | DeviceStateFile::validate() — format==1, device_id non-empty, fraction in 0..=1; BTreeMap serde with `#[serde(default)]` sections; numbers range-checked before merge (V5) |
| T-07-00-06 | Supply-chain Tampering | reqwest_dav / keyring / wiremock addition | mitigate | Exact `=` pins per RESEARCH Package Legitimacy Audit (all crates [OK]); Cargo.lock regenerated + committed; acceptance asserts single reqwest 0.13 (no 0.12 duplicate) |
| T-07-00-07 | Elevation | New IPC attack surface | accept | This plan adds NO IPC commands and touches NO capabilities file (default.json / android.json unchanged); the `sync_*` minimal-grant arrives with commands.rs in 07-01. No fs/shell permission change |
| T-07-00-08 | Tampering | Second SQLite binding / ledger fork | mitigate | V8 rides the existing tauri-plugin-sql migration chain (single binding, Pitfall 6); locator ledger rows reuse the V1 change_log with the same atomic COALESCE clock (T-05-02 pattern); merge stays pure — later reconcile writes reuse `tauri_plugin_sql::DbInstances` pool, with research-A4 fallback (route merge writes through the frontend plugin-sql path) noted for the 07-02 plan if pool access proves impossible |
| T-07-00-09 | Info Disclosure / MITM | TLS posture | accept | Not exercisable yet (no network code in this plan); V8 lands the two INDEPENDENT switches allow_http / trust_self_signed defaulting to 0 (strict) — D-95; `danger_accept_invalid_certs` wiring is 07-01 transport work |
</threat_model>

<artifacts_produced>
## Artifacts this plan produces (07-00)

- **Dependencies:** `reqwest_dav = "=0.3.3"` + `keyring = { version = "=4.1.5", features = ["android-native-keyring-store"] }` in src-tauri `[dependencies]`; `wiremock = "=0.6.5"` in `[dev-dependencies]`; regenerated Cargo.lock with a SINGLE reqwest 0.13.x (committed, D-13). core/Cargo.toml untouched.
- **Migration:** `SCHEMA_V8` const in `src-tauri/src/migrations.rs`, registered as version **8** (`sync_webdav_state`). DDL: `library_item.deleted` + `library_item.file_sync_enabled` ALTERs; `sync_config` (single row id='config', NO password column; remote_path default `pillowtome/`; independent allow_http / trust_self_signed); `sync_state` (id='state'; remote_etag opaque equality token; syncing flag); `sync_file_state` (per-work chunk-resume: direction, transfer_uuid, chunks_done JSON, size/hash/remote_path).
- **Ledger:** `src/reader/locator-store.ts` upsertLocator appends one `change_log` row per upsert — entity `'locator'`, op `'upsert'`, payload `{work_id, cfi, progress_fraction, text_pre, text_exact, text_post, updated_at}`, clock = COALESCE(MAX(logical_clock))+1 inside the INSERT; device_id via annotation-store's exported `ensureDevice()`.
- **Rust module `core/src/sync/model.rs` exports:** `Manifest`, `REMOTE_FORMAT`, `manifest_json`, `DeviceRecord`, `ProgressRec`, `AnnotationRec`, `FileSyncRec`, `LibraryRec`, `DeviceStateFile` (+ `validate`), `ModelError`.
- **Rust module `core/src/sync/remote.rs` exports:** `DEFAULT_ROOT`, `REMOTE_FORMAT` re-export, `RemoteError`, `normalize_root`, `sanitize_segment`, `join_remote`, `book_file_name`, `book_remote_path`, `state_file_path`, `state_tmp_file_path`, `device_file_path`, `manifest_path`. Book naming: `作者 - 书名.ext`, collision → `作者 - 书名 [hash8].ext` (D-105).
- **Rust module `core/src/sync/merge.rs` exports:** `MergeWinner` (`Local | Remote | Both{kept, conflict_copy}`), `MergeOutcome { winner, replaced_local }`, `merge_progress`, `merge_annotation`, `merge_library`, `merge_progress_map`, `merge_annotation_map`, `merge_library_map`.
- **Merge semantics (locked here, consumed by 07-02 reconcile):** progress furthest-wins (fraction → updated_at → device_id, total order); annotation by-id union (revision → tombstone remove-wins → same-algo hash → updated_at → device_id), conflict copy under caller-supplied UUID with `冲突副本` note prefix; library by-work union, tombstone anti-resurrection, file_sync union; `replaced_local` = D-92 undo payload; nothing on only one side is ever dropped.
- **Shared-contract notes for downstream plans:** remote layout `pillowtome/{manifest.json, books/, state/<device_id>.json, devices/<device_id>.json}`; state pushes write `state/<device_id>.json.tmp-<uuid>` then MOVE (Pitfall 5) and use conditional PUT (`If-None-Match: *` first, `If-Match: <etag>` after, 412 → re-pull-merge-retry); ETags are opaque equality tokens, never parsed.
</artifacts_produced>

<verification>
- `cd /d/Github/Pillowtome && cargo check --workspace` green with the new pins; Cargo.lock holds exactly one reqwest (0.13.x).
- `cd /d/Github/Pillowtome/src-tauri && cargo test --test migration` green (V1..V8 chain, new sync tables/columns/defaults, append-only diff).
- `cd /d/Github/Pillowtome && pnpm test src/reader/locator-store.test.ts && pnpm exec tsc --noEmit` green.
- `cd /d/Github/Pillowtome && cargo test -p pillowtome-core sync` green (path-hygiene battery + merge fixtures + fixed-seed property matrix).
- Full-suite spot check before commit: `cd /d/Github/Pillowtome && cargo test --workspace && pnpm test` green. (All cargo runs need the MSVC toolchain — GNU gcc exits 1 silently per MEMORY.)
</verification>

<success_criteria>
- Sync deps exact-pinned and locked without a second reqwest; pillowtome-core gains zero runtime dependencies.
- SCHEMA_V8 applies cleanly forward from V7 (append-only) and gives sync its config/state/file-resume tables plus the catalog tombstone and per-book file-sync flag — with no credential column.
- Progress writes now leave a change_log trail identical in discipline to annotations (SYNC-02 unblocked).
- The merge engine is a pure, exhaustively fixture-tested core that provably never loses a single-side record, honors tombstone remove-wins and per-record hash_algo, and produces deterministic winners plus the D-92 undo payload.
- All remote paths are born jailed under the configured root with human-readable CJK book names.
</success_criteria>

<output>
Create `.planning/phases/07-webdav-self-hosted-sync/07-00-SUMMARY.md` when done.
</output>
