---
phase: 04-local-library
plan: 01
type: execute
wave: 1
depends_on: ["04-00"]
files_modified:
  - src-tauri/src/commands.rs
  - src-tauri/src/lib.rs
  - src/library/ImportButton.tsx
  - src/library/FolderScanButton.tsx
  - src/library/import-pipeline.ts
  - src/library/import-pipeline.test.ts
  - src/App.tsx
  - src-tauri/capabilities/default.json
autonomous: true
requirements: [LIB-01]
must_haves:
  truths:
    - "User can import EPUB file(s) and they appear as library_item rows (D-50)"
    - "User can scan a folder recursively for .epub (desktop folder / Android SAF tree) (D-50, D-53)"
    - "Duplicate content_hash skips insert and surfaces 简体中文 已有 feedback (D-51)"
    - "Import registers BookSource handle without bulk-copying EPUB into app data (D-52)"
    - "DRM/corrupt items soft-fail; scan continues; aggregate summary (D-53)"
  artifacts:
    - path: "src/library/import-pipeline.ts"
      provides: "Dedup + catalog upsert orchestration"
    - path: "src/library/FolderScanButton.tsx"
      provides: "Folder scan entry UI"
  key_links:
    - from: "import-pipeline"
      to: "library_item + SourceRegistry + ensure_work"
      via: "hash → skip or insert"
      pattern: "content_hash|library_item"
---

<objective>
Implement dual ingest pipeline: file import + recursive folder scan into library_item with content_hash dedup, protection gate, metadata/cover extract, and 简体中文 aggregate feedback (LIB-01, D-50..D-53).

Purpose: Real books enter the shelf, not only in-memory SourceRegistry list.

Output: Working import/scan commands + UI entry points; registry + SQL catalog coherent.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-local-library/04-CONTEXT.md
@.planning/phases/04-local-library/04-RESEARCH.md
@.planning/phases/04-local-library/04-00-PLAN.md
@src/library/ImportButton.tsx
@src-tauri/src/commands.rs
@docs/decisions/DEC-004-android-saf-mechanism.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend ingest — hash, protect, catalog upsert, scan</name>
  <files>src-tauri/src/commands.rs, src-tauri/src/lib.rs, src-tauri/capabilities/default.json</files>
  <read_first>
    - src-tauri/src/commands.rs (import, imported_books, ensure_work, check_protection)
    - src-tauri/src/storage.rs
    - 04-00 artifacts (SCHEMA_V4, extract, covers)
    - D-50..D-54
  </read_first>
  <action>
    Implement ingest API (names discretionary but stable for frontend):
    - Given a BookSource / path / content URI: run protection check; on clean, hash; if work/library exists for hash → return `skipped_duplicate`; else register registry, ensure_work, extract meta/cover, INSERT library_item
    - `scan_folder` / walk: recursive .epub only; collect results `{ imported, skipped_duplicate, failed[] }` with 简体中文 messages for failures
    - Never return book bytes; never MANAGE_EXTERNAL_STORAGE
    - Capabilities: only existing dialog/SAF permissions needed
  </action>
  <verify>
    <automated>cargo test --workspace</automated>
  </verify>
  <acceptance_criteria>
    - Duplicate hash path returns skip without second library_item
    - DRM path does not insert library_item
    - cargo test --workspace exits 0
  </acceptance_criteria>
  <done>Rust ingest pipeline ready for UI.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend dual entry + pipeline orchestration</name>
  <files>src/library/ImportButton.tsx, src/library/FolderScanButton.tsx, src/library/import-pipeline.ts, src/library/import-pipeline.test.ts, src/App.tsx</files>
  <read_first>
    - src/library/ImportButton.tsx
    - src/App.tsx
    - 04-CONTEXT D-50, D-61 empty state (empty UI may land fully in 04-02)
  </read_first>
  <action>
    Wire 导入书籍 to new catalog-aware import (refresh library list from library-store not only imported_books).
    Add 扫描文件夹 button (desktop open directory; Android SAF tree — match existing is_android gating).
    Pure helpers for summarizing scan results 简体中文; unit test aggregate message builder.
    Soft-fail invoke errors with console + user-visible caption.
  </action>
  <verify>
    <automated>pnpm test && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - UI strings include 导入书籍 and 扫描文件夹
    - import-pipeline tests cover duplicate summary text
    - pnpm test and pnpm build exit 0
  </acceptance_criteria>
  <done>LIB-01 dual entry usable end-to-end (device gate later).</done>
</task>

</tasks>

<threat_model>
| Threat | Mitigation |
|--------|------------|
| T-04-path | BookSource + SAF only; no raw path in UI |
| T-04-dos | Scan soft-fail per file; optional size/count caps discretionary |
| T-04-ipc | Metadata structs only |
</threat_model>

<verification>
cargo test --workspace; pnpm test; pnpm build
</verification>

<success_criteria>
LIB-01 import + scan + dedup
</success_criteria>

<output>
Create `.planning/phases/04-local-library/04-01-SUMMARY.md` when done.
</output>
