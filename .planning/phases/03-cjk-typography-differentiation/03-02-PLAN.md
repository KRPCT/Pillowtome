---
phase: 03-cjk-typography-differentiation
plan: 02
type: execute
wave: 1
depends_on: ["03-00"]
files_modified:
  - src/reader/SettingsSheet.tsx
  - src/App.css
autonomous: true
requirements: [CJK-01, CJK-02, CJK-03]
must_haves:
  truths:
    - "Aa 显示设置 sheet includes section 中文排版 after 主题 and before 字体 (D-31)"
    - "Three independent switches 标点挤压 / 盘古之白 / 禁则 default ON and live-apply via onPrefsChange (D-32, D-22)"
    - "Each row has info affordance with plain-language 简体中文 body from UI-SPEC (D-30, D-33)"
    - "Switches stay enabled even when engine cannot honor features — no upgrade wall or capability chip (D-38)"
    - "Sheet body keeps min-h-0 overflow-y-auto touch pan-y (Android scroll gate)"
  artifacts:
    - path: "src/reader/SettingsSheet.tsx"
      provides: "中文排版 section with 3 Switch rows + info Popovers"
      contains: "中文排版"
    - path: "src/App.css"
      provides: "reader-cjk-row layout styles (48px min-height)"
      contains: "reader-cjk-row"
  key_links:
    - from: "src/reader/SettingsSheet.tsx"
      to: "ReadingPrefs CJK fields"
      via: "onPrefsChange({ cjkPunctTrim | cjkAutospace | cjkKinsoku })"
      pattern: "cjkPunctTrim|cjkAutospace|cjkKinsoku"
    - from: "src/reader/SettingsSheet.tsx"
      to: "src/components/ui/switch.tsx"
      via: "Switch checked + onCheckedChange"
      pattern: "from \\\"@/components/ui/switch\\\"|from \\\"../components/ui/switch\\\"|Switch"
    - from: "src/reader/SettingsSheet.tsx"
      to: "src/components/ui/popover.tsx"
      via: "Info popover per toggle"
      pattern: "Popover|关于标点挤压|关于盘古之白|关于禁则"
---

<objective>
Extend the existing Aa bottom SettingsSheet with a dedicated「中文排版」section: three independent live toggles (default ON) and plain-language 简体中文 info affordances (CJK-01..03 UX; D-30..D-34, D-38). No master toggle, no per-book overrides, no WebView upgrade UI.

Purpose: Readers can discover and control the moat features without jargon-only chrome; prefs already persist via SCHEMA_V3 + FoliateView debounce from Wave 0/1.

Output: Settings section + row CSS matching 03-UI-SPEC; live onPrefsChange for the three booleans.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-cjk-typography-differentiation/03-CONTEXT.md
@.planning/phases/03-cjk-typography-differentiation/03-UI-SPEC.md
@.planning/phases/03-cjk-typography-differentiation/03-PATTERNS.md
@.planning/phases/03-cjk-typography-differentiation/03-RESEARCH.md
@.planning/phases/02-epub-reading-core/02-UI-SPEC.md
@src/reader/SettingsSheet.tsx
@src/reader/apply-reading-styles.ts
@src/App.css
@src/components/ui/switch.tsx
@src/components/ui/popover.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: 「中文排版」section — three switches + info popovers</name>
  <files>src/reader/SettingsSheet.tsx</files>
  <read_first>
    - src/reader/SettingsSheet.tsx (full — section order, onPrefsChange, sheet body scroll classes)
    - 03-UI-SPEC.md Layout Contract (section order), 中文排版 rows, Copywriting Contract, a11y
    - 03-CONTEXT.md D-30..D-34, D-38
    - 03-PATTERNS.md SettingsSheet.tsx section
    - src/components/ui/switch.tsx and popover.tsx APIs
    - src/reader/apply-reading-styles.ts ReadingPrefs field names
  </read_first>
  <action>
    Insert a new section **after 主题 and before 字体** (D-31; UI-SPEC order 1..7).

    **Section structure:**
    - `section.reader-settings-section` with `h3.reader-settings-section__title` text exactly `中文排版` (16px/600 via existing class).
    - Exactly **three** rows (no master “优化中文排版” — deferred). Row layout: label (flex 1) → info button → Switch.
    - Labels exact copy (D-30): `标点挤压`, `盘古之白`, `禁则`.
    - Map to prefs: `cjkPunctTrim`, `cjkAutospace`, `cjkKinsoku`.
    - Switch `checked={prefs.cjkPunctTrim}` (etc.); `onCheckedChange` → `onPrefsChange({ cjkPunctTrim: checked })` partial only — live apply + existing parent debounce save (D-22). No separate 应用 button.
    - Defaults come from prefs/DEFAULT_PREFS (all true, D-32). Do not force-disable switches based on feature-detect (D-38). No “当前设备不支持” chip. No upgrade dialog copy.

    **Info affordance (D-33):**
    - Icon button per row: lucide `CircleHelp` or `Info`; min 44×44 hit; `aria-label` exactly `关于标点挤压` / `关于盘古之白` / `关于禁则`.
    - Prefer shadcn **Popover** anchored to the button (UI-SPEC). Title = toggle label; body exact UI-SPEC strings:
      - 标点挤压: `收窄中文标点旁多余空白，让「你好。」这类句子更紧凑、更像印刷书。`
      - 盘古之白: `在汉字与英文、数字之间自动留出细小间距，例如「读取 PDF」更易扫读。`
      - 禁则: `避免行首出现句号、逗号，或行尾出现左引号、左括号等不合适的断行。`
    - Closing info must not toggle the switch. Optional dialog fallback only if Popover is clearly broken — prefer Popover first.
    - Switch `aria-label` matches visible label.

    **Sheet chrome:**
    - Extend sr-only `SheetDescription` to: `调整阅读模式、主题、中文排版与字体选项`.
    - Preserve `reader-sheet__body` classes: `min-h-0 flex-1 overflow-y-auto overscroll-contain` + touch pan-y (Android gate). Do not add full-screen transparent pointer-eating overlays.
    - Optional extract `CjkTypographySection` component in same file or sibling — not required if inline stays clear.
    - Do not redesign theme/mode/font sections. Do not add per-book UI (D-34 / D-21). Clean-room; no Readest.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - SettingsSheet.tsx contains exact strings: 中文排版, 标点挤压, 盘古之白, 禁则
    - Contains info a11y labels 关于标点挤压, 关于盘古之白, 关于禁则
    - Contains the three UI-SPEC helper body sentences (or unique substrings 更像印刷书 / 读取 PDF / 左引号)
    - onPrefsChange wired for cjkPunctTrim, cjkAutospace, cjkKinsoku
    - Imports/uses Switch and Popover (or Dialog fallback with same copy)
    - No copy containing 请升级 or 功能不可用 for CJK
    - SheetDescription mentions 中文排版
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>中文排版 section live in Aa sheet with toggles + info copy.</done>
</task>

<task type="auto">
  <name>Task 2: CJK row CSS + switch accent token wiring</name>
  <files>src/App.css, src/reader/SettingsSheet.tsx</files>
  <read_first>
    - src/App.css (.reader-settings-section*, .reader-font-list__row, .reader-sheet__body)
    - src/index.css (.reader[data-theme] tokens --reader-accent etc.)
    - 03-UI-SPEC.md Spacing (row min-height 48px), Color (switch checked = accent), Token Mapping Notes
    - 03-PATTERNS.md App.css section
  </read_first>
  <action>
    Add app-owned row classes (UI-SPEC Token Mapping Notes) without new color tokens:

    - `.reader-cjk-row` — display flex; align-items center; min-height 48px; gap 8px (sm).
    - `.reader-cjk-row__label` — flex 1; font-size 14px; truncate if needed.
    - `.reader-cjk-row__info` — icon button wrapper; min 44×44 hit; muted idle color via existing `--reader-muted` / chrome text on hover.
    - `.reader-cjk-row__switch` — switch wrapper ensuring 44px hit if needed.

    Wire SettingsSheet rows to these classes. Switch checked state must use reader accent under `.reader[data-theme]` (UI-SPEC accent list item 6) — either via data-slot/class overrides in App.css targeting Switch inside `.reader` / `.reader-settings-sheet`, or className props. OFF = muted track; no accent wash.

    Popover surface: reuse `--reader-bg` / `--reader-chrome` / `--reader-border` / `--reader-fg` where practical; max-width ~280–320px; padding 16px. No new badge/NEW marketing chrome.

    Keep section spacing `gap-8` between major sections. Do not break existing font list or slider layout.
  </action>
  <verify>
    <automated>pnpm build && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - App.css defines reader-cjk-row (and label/info/switch children)
    - SettingsSheet uses reader-cjk-row classes
    - No new third-party CSS frameworks; no third-party shadcn registries
    - `pnpm build` and `pnpm test` exit 0
  </acceptance_criteria>
  <done>CJK settings rows match paper-feel UI-SPEC layout and accent rules.</done>
</task>

</tasks>

## Artifacts this phase produces

| Symbol / path | Kind | Consumed by |
|---------------|------|-------------|
| SettingsSheet「中文排版」section | UI | end users; prefs → 03-01 CSS |
| Info Popover copy (3 features) | 简体中文 UX | D-33 |
| `.reader-cjk-row*` | CSS | Settings layout |
| Live `onPrefsChange` for 3 booleans | wiring | FoliateView save + CSS reapply |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User toggle gestures → prefs object | Boolean only; parent persists via SQL |
| Popover content | Static copy; no HTML from books |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-sql | Tampering | prefs save path | mitigate | UI only sets booleans; existing bound-param save in reading-prefs (no new SQL in this plan) |
| T-03-css | Elevation of Privilege | settings | accept | Settings does not build CSS strings from free text |
| T-03-agpl | Tampering (legal) | SettingsSheet | mitigate | Clean-room UI from UI-SPEC; no Readest |
</threat_model>

<verification>
- `pnpm build` + `pnpm test`
- Grep: 中文排版, three labels, three about-labels, three helper phrases, cjkPunctTrim/cjkAutospace/cjkKinsoku, reader-cjk-row
- Human visual (end-of-phase): Aa sheet section order and switch/info behavior on desktop; Android scroll of longer sheet
</verification>

<success_criteria>
- D-30..D-34, D-38 UX surface complete for CJK-01..03 toggles
- Live apply without master toggle or upgrade wall
- Android sheet scroll gate preserved
</success_criteria>

<output>
Create `.planning/phases/03-cjk-typography-differentiation/03-02-SUMMARY.md` when done.
</output>
