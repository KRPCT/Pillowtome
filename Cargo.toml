[workspace]
members = ["core", "src-tauri"]
resolver = "2"

# Shared release profile keeps both crates optimizing identically across
# desktop and the Android cross-compile.
[profile.release]
codegen-units = 1
lto = true
opt-level = "s"
panic = "abort"
strip = true
