---
phase: 04-local-library
plan: 02
type: execute
wave: 2
depends_on: ["04-00", "04-01"]
files_modified:
  - src/App.tsx
  - src/App.css
  - src/library/LibraryGrid.tsx
  - src/library/LibraryCard.tsx
  - src/library/LibraryToolbar.tsx
  - src/library/library-sort.ts
  - src/library/library-sort.test.ts
  - src/library/library-store.ts
autonomous: true
requirements: [LIB-02, LIB-03, LIB-04]
must_haves:
  truths:
    - "Library default view is a responsive cover grid (D-55)"
    - "Each card shows cover (or paper placeholder), title, author under cover (D-56, D-57, D-58)"
    - "Sort: 标题/作者/最近阅读/进度; default 最近阅读 (D-59)"
    - "Filter chips: 全部/在读/未读/已读完 (D-60)"
    - "Empty state dual CTA 导入书籍 + 扫描文件夹 (D-61)"
    - "Thin progress bar on card from locator progress (D-62)"
  artifacts:
    - path: "src/library/LibraryGrid.tsx"
      provides: "Cover grid shelf"
    - path: "src/library/library-sort.ts"
      provides: "Pure sort/filter"
  key_links:
    - from: "LibraryCard"
      to: "library_item + progress_fraction"
      via: "props"
      pattern: "progress|cover|title"
    - from: "App open"
      to: "source_id / FoliateView id"
      via: "onOpen(item)"
      pattern: "setOpenId|source_id"
---

<objective>
Replace the thin imported list with a paper-feel cover grid, toolbar sort/filter chips, empty dual CTA, and progress affordances (LIB-02..04, D-55..D-62).

Purpose: Shelf becomes the primary home shell.

Output: LibraryGrid + toolbar + pure sort/filter tests; App integrates open book by source_id.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-local-library/04-CONTEXT.md
@.planning/phases/02-epub-reading-core/02-UI-SPEC.md
@src/App.tsx
@src/App.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pure sort/filter + LibraryToolbar</name>
  <files>src/library/library-sort.ts, src/library/library-sort.test.ts, src/library/LibraryToolbar.tsx</files>
  <read_first>
    - 04-CONTEXT D-59..D-61
    - 04-RESEARCH validation table
  </read_first>
  <action>
    Pure functions: sort by title, author, last_read_at, progress_fraction; filter by reading status with documented thresholds (e.g. unread=0 or null, finished≥0.99, reading otherwise).
    Toolbar: chips 全部/在读/未读/已读完 + sort control; 简体中文 labels; a11y labels.
    Unit tests for sort stability and each filter bucket.
  </action>
  <verify>
    <automated>pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - library-sort tests cover all four sorts and four filters
    - Toolbar contains exact chip labels 全部 在读 未读 已读完
  </acceptance_criteria>
  <done>LIB-04 pure logic ready.</done>
</task>

<task type="auto">
  <name>Task 2: Cover grid + empty state + App shell</name>
  <files>src/library/LibraryGrid.tsx, src/library/LibraryCard.tsx, src/App.tsx, src/App.css, src/library/library-store.ts</files>
  <read_first>
    - src/App.tsx current library list
    - 02-UI-SPEC paper tokens
    - D-55..D-58, D-61, D-62
  </read_first>
  <action>
    LibraryCard: cover image or placeholder; title+author under; thin progress bar.
    LibraryGrid: responsive CSS grid; open on card primary click.
    Empty: guided copy + 导入书籍 + 扫描文件夹.
    App: load listLibraryItems; wire toolbar; open FoliateView with registry source_id; refresh after import/scan.
    Styles match paper-feel (no Geist on book titles if applicable).
  </action>
  <verify>
    <automated>pnpm test && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - Strings: 未知书名 or extract titles path; empty CTAs present
    - CSS grid class for library cover grid exists
    - pnpm build exits 0
  </acceptance_criteria>
  <done>LIB-02..04 UI shell complete (visual UAT later).</done>
</task>

</tasks>

<threat_model>
| Threat | Mitigation |
|--------|------------|
| T-04-xss | Titles/authors as text nodes only |
</threat_model>

<verification>
pnpm test; pnpm build
</verification>

<success_criteria>
Cover grid shelf with sort/filter/empty/progress
</success_criteria>

<output>
Create `.planning/phases/04-local-library/04-02-SUMMARY.md` when done.
</output>
