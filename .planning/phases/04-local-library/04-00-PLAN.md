---
phase: 04-local-library
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src-tauri/src/migrations.rs
  - src-tauri/tests/migration.rs
  - core/src/publication/mod.rs
  - core/src/publication/epub_meta.rs
  - core/tests/publication_meta.rs
  - src-tauri/src/commands.rs
  - src-tauri/src/covers.rs
  - src-tauri/src/lib.rs
  - src/library/types.ts
  - src/library/library-store.ts
  - src/library/library-store.test.ts
autonomous: true
requirements: [LIB-01, LIB-03]
must_haves:
  truths:
    - "SCHEMA_V4 library_item (or equivalent) links work_id + source_id + title/author + cover path + timestamps (D-54, D-65 fields)"
    - "EpubPublication extracts title/author and cover bytes from real EPUB OPF without WebView"
    - "content_hash remains blake3; UNIQUE shelf row per work_id (D-51)"
    - "Cover files materialize under app_data/covers; no book bytes over IPC"
  artifacts:
    - path: "src-tauri/src/migrations.rs"
      provides: "SCHEMA_V4 library catalog"
      contains: "SCHEMA_V4"
    - path: "core/src/publication"
      provides: "EPUB metadata/cover extract API"
  key_links:
    - from: "library_item.work_id"
      to: "work.work_id"
      via: "FK + ensure_work"
      pattern: "work_id"
    - from: "library_item.source_id"
      to: "SourceRegistry"
      via: "import id"
      pattern: "source_id|import-"
---

<objective>
Land SCHEMA_V4 library catalog, real EPUB metadata/cover extraction on the Publication seam, and typed frontend library-store mappers so import/UI/position plans have a durable identity + shelf model (LIB-01 foundation, LIB-03 metadata, D-51/D-52/D-54).

Purpose: Stop treating `imported_books` registry list as the library; bind shelf rows to work_id + content_hash.

Output: Migration green; core extract tests green; cover write helper; TS types + soft-fail loaders.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-local-library/04-CONTEXT.md
@.planning/phases/04-local-library/04-RESEARCH.md
@docs/MAJOR-READER-POS.md
@core/src/publication/mod.rs
@src-tauri/src/migrations.rs
@src-tauri/src/commands.rs
@src/reader/locator-store.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: SCHEMA_V4 library_item + migration tests</name>
  <files>src-tauri/src/migrations.rs, src-tauri/tests/migration.rs</files>
  <read_first>
    - src-tauri/src/migrations.rs (V1–V3 append pattern)
    - src-tauri/tests/migration.rs
    - 04-RESEARCH.md schema shape
    - 04-CONTEXT.md D-51, D-54, D-65
  </read_first>
  <action>
    Append SCHEMA_V4 only (never rewrite V1–V3):
    - `library_item` table: item_id PK, work_id FK UNIQUE, source_id NOT NULL, title NOT NULL, author nullable, cover_file nullable, imported_at, last_opened_at, last_read_at
    - Indexes for last_read_at DESC and title
    - Register migration version 4 description `local_library_catalog`
    - Tests: fresh_db_v4; columns exist; UNIQUE(work_id); migrations().len()==4
  </action>
  <verify>
    <automated>cargo test --workspace</automated>
  </verify>
  <acceptance_criteria>
    - SCHEMA_V4 in migrations.rs with library_item and work_id
    - migration tests assert v4 columns and unique work_id
    - cargo test --workspace exits 0
  </acceptance_criteria>
  <done>Catalog schema ready for import pipeline.</done>
</task>

<task type="auto">
  <name>Task 2: EPUB metadata/cover extract + covers materialize + TS library-store</name>
  <files>core/src/publication/mod.rs, core/src/publication/epub_meta.rs, core/tests/publication_meta.rs, src-tauri/src/covers.rs, src-tauri/src/lib.rs, src-tauri/src/commands.rs, src/library/types.ts, src/library/library-store.ts, src/library/library-store.test.ts</files>
  <read_first>
    - core/src/publication/mod.rs
    - core/src/protection (zip read patterns if any)
    - 04-CONTEXT D-52 cover cache
    - src/reader/locator-store.ts soft-fail SQL style
  </read_first>
  <action>
    **Core:** Add pure EPUB OPF metadata extract (title, creators/authors) + cover image bytes from package document / manifest item (cover-image or properties=cover-image). Unit test with fixture EPUB from core/tests/fixtures or sample.epub. Soft-fail missing fields → empty title fallback "未知书名".

    **Rust app:** `covers` module writes `app_data/covers/{work_id}.{ext}` with size cap (e.g. 5MB); safe id path confinement like fonts. Optional invoke only returns paths/ids — never cover bytes over IPC if large; prefer write-then-path.

    **ensure_work / commands:** Expose enough for later import (may add `library_upsert` stub or pure SQL helpers documented).

    **TS:** `LibraryItem` type; `listLibraryItems` SELECT join locator progress optional; soft-fail []; bound params only.
  </action>
  <verify>
    <automated>cargo test --workspace && pnpm test && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - Core test proves title or author extract on fixture EPUB
    - covers path confined under app_data/covers
    - library-store uses bound $n SQL
    - cargo test, pnpm test, pnpm build exit 0
  </acceptance_criteria>
  <done>Identity + metadata + catalog read path ready for 04-01 import.</done>
</task>

</tasks>

<threat_model>
| Threat | Mitigation |
|--------|------------|
| T-04-sql | Bound params only for library SQL |
| T-04-path | Cover paths sanitize work_id; no traversal |
| T-04-ipc | No book/cover bulk bytes over IPC |
</threat_model>

<verification>
cargo test --workspace; pnpm test; pnpm build
</verification>

<success_criteria>
SCHEMA_V4 + extract + library-store foundations for LIB-01/03
</success_criteria>

<output>
Create `.planning/phases/04-local-library/04-00-SUMMARY.md` when done.
</output>
