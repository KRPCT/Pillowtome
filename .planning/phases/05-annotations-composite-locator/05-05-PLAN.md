---
phase: 05-annotations-composite-locator
plan: 05
type: execute
wave: 4
depends_on: [05-04]
files_modified: []
autonomous: false
requirements: [ANNO-01, ANNO-02, ANNO-03, ANNO-04]
must_haves:
  truths:
    - "On AVD Medium_Phone_API_36.1, the selection bubble appears correctly positioned over the selection in BOTH paginate and scroll modes"
    - "Highlights / underlines / notes / bookmarks persist and redraw correctly after reopen and after paginate↔scroll switch on device"
    - "A highlight created under one 简繁/词不拆行 state stays anchored after toggling that state (self-heal), not drifted or lost"
    - "Finger-swipe scrolls both the reading view and the annotation sheets — no pointer-capture regression"
    - "200+ annotations open and scroll without visible jank (lazy per-section draw holds under load)"
  artifacts: []
  key_links: []
---

<objective>
Run the mandatory Android emulator acceptance gate for Phase 5. Per CLAUDE.md, any change to the reader shell / `pillow://` / typography / protocol MUST be verified on AVD `Medium_Phone_API_36.1` (or a physical device) before it can be called done — desktop-green + unit-green is NOT enough. The closed-shadow paginate coordinate mapping for the bubble and the CSS-Custom-Highlight per-section redraw are the two riskiest surfaces and are the acceptance hard-gate. Also run the multi-annotation performance stress test the reader-perf memory requires.

Purpose: The prior phases repeatedly shipped desktop-green code that broke on Android (CORS, foliate pager centering, sheet scroll). Annotations add closed-shadow coordinate mapping and per-iframe highlight registries — both device-variance sensitive. This gate catches those before verify-work.
Output: Confirmed on-device behavior across both modes, reopen, mode-switch, transform-toggle, finger-swipe, and a 200+ annotation stress run. No code changes expected (if defects surface, they are fixed in a `--gaps` follow-up).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-annotations-composite-locator/05-CONTEXT.md
@.planning/phases/05-annotations-composite-locator/05-RESEARCH.md
@.planning/phases/05-annotations-composite-locator/05-VALIDATION.md
@docs/ANDROID-BUILD.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full desktop suite + build green before device run</name>
  <files>(no source changes — verification)</files>
  <read_first>
    - .planning/phases/05-annotations-composite-locator/05-RESEARCH.md ("Validation Architecture" — Sampling Rate: phase gate = full suite + tsc + build + Android AVD)
    - docs/ANDROID-BUILD.md (§ Device gate — the emulator run recipe; MEMORY note: export Android env vars before `cargo tauri android *` because this process tree reads a stale env block)
  </read_first>
  <action>
    Run the full desktop verification as the pre-flight for the device gate: `pnpm test` (whole vitest suite), `pnpm exec tsc --noEmit`, and `pnpm build` must all be green. Then run the Rust migration suite `cargo test --test migration` (MSVC toolchain required). If any is red, stop and fix before the device run — do not burn an emulator cycle on known-red code.
  </action>
  <verify>
    <automated>cd /d/Github/Pillowtome && pnpm test && pnpm exec tsc --noEmit && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test` reports 0 failures (includes annotation-store, anchor-resolver, css-highlight, locator-store, scroll-cfi suites).
    - `pnpm exec tsc --noEmit` clean; `pnpm build` succeeds.
    - `cargo test --test migration` green (V7 applied).
  </acceptance_criteria>
  <done>Whole desktop + migration suite green — cleared for the device gate.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Android AVD acceptance — bubble coords, persist/redraw, self-heal, finger-swipe</name>
  <files>(no source changes — on-device human acceptance)</files>
  <what-built>
    Full Phase 5 annotation stack on device: selection bubble (create + edit), 4-color highlights + underline, note editor, annotations sheet with chapter grouping + jump + swipe-delete, bookmark toggle, and silent composite-locator restore — in both paginate and scroll, via foliate draw-annotation (paginate, closed shadow) and CSS Custom Highlight (scroll).
  </what-built>
  <how-to-verify>
    Export the Android env vars first (MEMORY: this process tree has a stale env block — `setx` values read empty in-process), then launch `pnpm tauri android dev` on AVD `Medium_Phone_API_36.1` (or a physical device) per docs/ANDROID-BUILD.md § Device gate. Force-stop + cold-start after the first HMR to verify (not a half-updated HMR view). Verify, in BOTH paginate and scroll:
    1. Long-press to enter native selection → bubble appears ABOVE the selection (flips below when near the top), arrow pointing at the anchor. Confirm the bubble is correctly positioned — this is the closed-shadow coordinate-mapping hard-gate for paginate.
    2. Tap each of the 4 swatches → highlight draws in that color, legible over the page in day / night / sepia. Tap 下划线 → underline draws. Tap 复制 → text copied.
    3. Tap an existing highlight → the SAME bubble reopens in edit context with 删除; change color recolors; 删除 removes it.
    4. 笔记 → note editor sheet opens; type a note, 完成; reopen the highlight → note persists. Finger-swipe inside the note sheet and the annotations sheet → they scroll (touch-gate; no swallowed pan).
    5. Toolbar bookmark toggle → adds a bookmark at the current position (accent state); open the 批注 sheet → the bookmark + highlights + notes are listed grouped by chapter; tap a row → jumps to it and the sheet closes; swipe a row → 删除 (note rows require the two-step confirm).
    6. Close and reopen the book → all annotations redraw at the right places (paginate AND scroll). Switch paginate↔scroll → annotations re-apply from the store.
    7. Toggle 简繁 (and 词不拆行) → an existing highlight stays anchored to its text (self-heal), not drifted a character or vanished.
    8. Perf stress (memory `pillowtome-reader-perf-stress-test`): with a book carrying 200+ annotations, open and scroll through several chapters → no visible jank, no open-time stall (confirms lazy per-section draw, not bulk-draw-on-open).
    Capture screenshots of the bubble in paginate + scroll and the annotations sheet as evidence.
  </how-to-verify>
  <action>
    Launch the app on AVD `Medium_Phone_API_36.1` (or a physical device) via `pnpm tauri android dev` (export Android env vars first — this process tree reads a stale env block per MEMORY) and walk the 8 verification steps in <how-to-verify> across BOTH paginate and scroll. This is the CLAUDE.md mandatory device gate; do not claim Phase 5 done on desktop-green alone. Record pass/fail per step and capture the paginate + scroll bubble screenshots + the annotations sheet. Any failure routes to a `/gsd-plan-phase 05 --gaps` follow-up rather than an inline patch here.
  </action>
  <verify>
    <human-check>All 8 device steps pass on AVD Medium_Phone_API_36.1 in both paginate and scroll; screenshots captured.</human-check>
  </verify>
  <done>Every ANNO-01..04 behavior verified on device in both modes (bubble coords, persist/redraw, self-heal on transform toggle, finger-swipe, 200+ annotation perf); screenshots attached.</done>
  <resume-signal>Type "approved" (attach/note the paginate + scroll bubble screenshots), or describe the defects to route into a `/gsd-plan-phase 05 --gaps` follow-up.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Device WebView version variance | Old/AOSP WebView < 105 lacks CSS Custom Highlight → scroll highlights must fall back |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-13 | Denial of Service | WebView < 105 missing CSS Custom Highlight → blank scroll highlights | mitigate | Feature-detect + foliate Overlayer fallback (plan 05-03); device gate confirms highlights render (fallback path exercised if the AVD/WebView is old) |
| T-05-14 | Tampering | closed-shadow bubble coordinate drift on high-DPI / split-column | mitigate | Device gate step 1 is the explicit coordinate-mapping acceptance check across both modes; dynamic host-rect correction if drift observed |
| T-05-SC | Tampering | npm/cargo installs | accept | No new dependencies this phase; verification only |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (plan 05-05)

- No source symbols. Produces the on-device acceptance evidence (screenshots + approval) that gates `/gsd-verify-work` for Phase 5, plus a perf-stress confirmation for the 200+ annotation case.
</artifacts_produced>

<verification>
- Desktop full suite + tsc + build + migration test green (Task 1).
- Human-verified Android AVD acceptance across both modes, reopen, mode-switch, transform-toggle self-heal, finger-swipe, and 200+ annotation perf (Task 2).
</verification>

<success_criteria>
- Every ANNO-01..04 behavior is confirmed working on device in both paginate and scroll.
- The closed-shadow bubble coordinate mapping and the CSS-Highlight per-section redraw hold on the emulator.
- No touch-gate regression; no open-time stall under 200+ annotations.
</success_criteria>

<output>
Create `.planning/phases/05-annotations-composite-locator/05-05-SUMMARY.md` when done.
</output>
