---
phase: 02-epub-reading-core
plan: 04
type: execute
wave: 4
depends_on: ["02-02", "02-03"]
files_modified:
  - src-tauri/src/fonts.rs
  - src-tauri/src/commands.rs
  - src-tauri/src/lib.rs
  - src-tauri/src/protocol.rs
  - src-tauri/src/storage.rs
  - src/reader/fonts.ts
  - src/reader/SearchSheet.tsx
  - src/reader/SettingsSheet.tsx
  - src/reader/FoliateView.tsx
  - src/reader/apply-reading-styles.ts
  - core/tests/fixtures
autonomous: true
requirements: [READ-06, READ-07]
must_haves:
  truths:
    - "User can import TTF/OTF/WOFF into app_data/fonts with max 20 fonts and 20MB each (READ-06, D-27, D-28)"
    - "Selected custom font injects @font-face via pillow fonts path and applies through setStyles (D-30)"
    - "Remove deletes app-data copy + metadata and falls back to system stack if active (D-29)"
    - "In-book search uses view.search() whole book, grapheme/CJK-friendly, 200–300ms debounce (READ-07, D-31, D-32, D-34)"
    - "Search results show snippet + chapter caption; jump via goTo(cfi)"
    - "Torture/protection fixtures soft-fail without panic; FXL does not crash process"
  artifacts:
    - path: "src-tauri/src/fonts.rs"
      provides: "copy/list/remove + limit enforcement unit-tested"
      contains: "MAX_FONT"
    - path: "src/reader/SearchSheet.tsx"
      provides: "搜索 sheet with debounce and result list"
      contains: "搜索书中内容"
    - path: "src/reader/fonts.ts"
      provides: "frontend invoke + SQL list + font face URL helper"
    - path: "src-tauri/src/protocol.rs"
      provides: "fonts/ path serve under pillow scheme"
      contains: "fonts"
  key_links:
    - from: "src/reader/SearchSheet.tsx"
      to: "view.search"
      via: "for-await generator; no matchWholeWords"
      pattern: "search\\(|buildSearchOpts"
    - from: "src-tauri/src/fonts.rs"
      to: "app_data_dir/fonts"
      via: "copy confined path"
      pattern: "app_data_dir|fonts"
    - from: "src-tauri/src/protocol.rs"
      to: "fonts allowlist"
      via: "sanitize + app_data/fonts only"
      pattern: "fonts/"
---

<objective>
Complete custom font import/apply/remove (READ-06, D-27..D-30), CJK-friendly in-book search (READ-07, D-31..D-34), font serving via extended pillow path, and torture-corpus soft-fail CI coverage for malformed/FXL/obfuscated cases.

Purpose: Final Lithium-parity reading-core capabilities + safety net for bad EPUBs.
Output: fonts Rust module + tests; SearchSheet; Settings font list live; protocol fonts path; CI soft-fail assertions.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-epub-reading-core/02-CONTEXT.md
@.planning/phases/02-epub-reading-core/02-UI-SPEC.md
@.planning/phases/02-epub-reading-core/02-RESEARCH.md
@.planning/phases/02-epub-reading-core/02-VALIDATION.md
@src-tauri/src/protocol.rs
@src-tauri/src/lib.rs
@src-tauri/src/commands.rs
@src/reader/SettingsSheet.tsx
@src/reader/search-opts.ts
@core/tests/fixtures
</context>

<tasks>

<task type="auto">
  <name>Task 1: Font filesystem commands + pillow fonts/ protocol + unit tests (READ-06)</name>
  <files>src-tauri/src/fonts.rs, src-tauri/src/commands.rs, src-tauri/src/lib.rs, src-tauri/src/protocol.rs, src-tauri/src/storage.rs, src/reader/fonts.ts, src/reader/SettingsSheet.tsx, src/reader/FoliateView.tsx, src/reader/apply-reading-styles.ts</files>
  <read_first>
    - src-tauri/src/protocol.rs (serve + sanitize_id + cors)
    - src-tauri/src/lib.rs (protocol registration + sample materialize pattern)
    - 02-CONTEXT.md D-27..D-30
    - 02-UI-SPEC.md font copy + remove confirm
    - 02-RESEARCH.md Pattern 6 fonts; planner decision: pillow path fonts/<id>
  </read_first>
  <action>
    **fonts.rs (Rust, D-27..D-29, T-02-font, T-02-path):**
    - Constants: `MAX_CUSTOM_FONTS = 20`, `MAX_FONT_BYTES = 20 * 1024 * 1024`.
    - Allowed extensions: ttf, otf, woff (UI-SPEC; woff2 optional refuse unless listed — stick to TTF/OTF/WOFF).
    - `fonts_dir(app) -> app.path().app_data_dir()/fonts` create_dir_all.
    - `import_font(app, source_path) -> Result<FontMeta, String>`: validate ext; read length ≤20MB; count existing files/metadata ≤20; generate id (uuid or random hex sanitize-safe); copy to `fonts/{id}.{ext}`; return { id, familyName, fileName, byteSize }. Do not trust client-supplied size alone — check filesystem metadata after copy or before.
    - `remove_font_file(app, id)`: sanitize id (reuse sanitize_id rules — no traversal); delete only under fonts_dir; never delete original user path.
    - Pure helpers unit-tested with tempdirs: reject >20MB, reject 21st font, reject bad ext, accept small ttf path, path traversal id rejected.

    **Commands:** `import_font`, `list` is SQL-side on frontend OR `list_fonts` reading dir — prefer: Rust import/remove file; frontend writes/deletes `custom_font` SQL rows with bound params. Commands must not return font file bytes (D-30 / T-02-ipc).

    **Protocol extension (D-30 planner decision):**
    - In pillow handler, if sanitized path starts with `fonts/` or path form is `/fonts/{id}`: resolve file under app_data/fonts only (join + canonicalize starts_with fonts_dir); serve with cors() + correct Content-Type (font/ttf, font/otf, font/woff). Do NOT use SourceRegistry for fonts. Reject `..` and absolute escapes (T-02-path).
    - Frontend builds URL via `convertFileSrc` only if it maps to pillow correctly; preferred: reuse `pillowUrl` pattern or export `pillowFontUrl(fontId: string)` in fonts.ts that uses `@tauri-apps/api/core` convertFileSrc with pillow scheme and path `fonts/{id}` — mirror pillow.ts, do not hand-roll per-platform host (Phase 1 lesson).

    **fonts.ts + SettingsSheet:**
    - List custom fonts from SQL custom_font; import: dialog.open filters + invoke import_font + INSERT SQL; show 已导入「{name}」 inline.
    - Select font → prefs.activeFontId + font_family_key; build `@font-face { font-family: "PillowCustom-{id}"; src: url("{pillowFontUrl}"); font-display: swap; }` passed into buildReadingCss; font-family CSS uses `"PillowCustom-{id}", ` + SYSTEM_CJK_STACK.
    - Remove: confirm copy 确认移除「{name}」？此操作不会删除设备上的原文件。 → remove file + SQL DELETE → if was active fall back to system (D-29).
    - Wire 导入字体 button; refuse over-limit with 简体中文 helper (reuse 仅支持… plus limit message — discretionary short copy is OK if clear).

    Register commands in lib.rs. Keep DRM/import book commands intact.
  </action>
  <verify>
    <automated>cargo test --workspace && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - fonts.rs contains MAX_CUSTOM_FONTS or 20 and MAX_FONT or 20 * 1024 * 1024
    - protocol.rs serves fonts path under app_data (grep fonts)
    - No new command returns Vec<u8> font or book bytes to JS
    - SettingsSheet or fonts.ts contains 导入字体 and @font-face or PillowCustom
    - cargo test --workspace includes fonts limit tests green; pnpm build exits 0
  </acceptance_criteria>
  <done>Custom fonts import/serve/apply/remove with server-side limits.</done>
</task>

<task type="auto">
  <name>Task 2: SearchSheet + keyboard shortcut + torture soft-fail CI (READ-07)</name>
  <files>src/reader/SearchSheet.tsx, src/reader/FoliateView.tsx, core/tests/fixtures, src-tauri/src/commands.rs</files>
  <read_first>
    - src/reader/search-opts.ts (buildSearchOpts, SEARCH_DEBOUNCE_MS)
    - 02-UI-SPEC.md Search sheet + empty copy
    - 02-CONTEXT.md D-31..D-34
    - 02-RESEARCH.md Pattern 4 search; Pitfall 10 matchWholeWords
    - core/tests/fixtures (clean, corrupt, adept, font-obfuscated)
    - 02-VALIDATION.md torture soft-fail
  </read_first>
  <action>
    **SearchSheet.tsx (READ-07, D-31, D-32, D-34):**
    - Sheet titled 搜索; sticky Input autofocus placeholder 搜索书中内容.
    - Debounce SEARCH_DEBOUNCE_MS (250) after input idle; call `for await (const result of view.search(buildSearchOpts(query)))` whole book (omit index).
    - Map section hits: label as chapter caption; subitems excerpt pre/match/post as snippet.
    - Results ScrollArea: body 14 snippet + caption 12 muted; tap → goTo(cfi) → close sheet.
    - Empty: 未找到匹配内容 / 试试更短的关键词.
    - On clear query: view.clearSearch?.().
    - Never set matchWholeWords true (D-31). No parallel search index.

    **FoliateView / keyboard (D-33 complete):**
    - Toolbar 搜索 opens SearchSheet.
    - Key `/` or Ctrl+F opens search (preventDefault); Esc closes search/settings/toc first.

    **Torture / soft-fail CI (roadmap 02-04):**
    - Keep existing protection decide tests green for clean/font-obfuscated render, adept/corrupt refuse.
    - Ensure fixtures still present under core/tests/fixtures.
    - Add or extend a test that: (1) corrupt → can_render false soft message; (2) content DRM refuse; (3) font-obfuscated still can_render true; (4) if an FXL fixture is added, open classification does not panic — product rule: open FXL if engine can; disable reflow knobs in UI when pre-paginated (already noted 02-01). If no FXL fixture available, document and assert protection path never panics on random bytes / corrupt (existing).
    - Optional: tiny FXL-like zip fixture only if cheap; do not block on hand-crafting complex FXL — soft-fail coverage on corrupt/DRM is mandatory.

    Do not introduce book-byte IPC. Do not copy Readest.
  </action>
  <verify>
    <automated>cargo test --workspace && pnpm test && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - SearchSheet contains 搜索书中内容 and 未找到匹配内容 and uses buildSearchOpts or search(
    - search path does not set matchWholeWords true
    - FoliateView wires Ctrl+F or key === "/" for search
    - cargo protection/decide tests still green; workspace tests exit 0
    - pnpm test + pnpm build exit 0
  </acceptance_criteria>
  <done>CJK search + fonts complete; torture soft-fail remains CI-green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User-picked font file → app_data/fonts | Untrusted binary enters app storage |
| WebView → pillow://fonts/* | Font bytes served to render documents |
| Search query → foliate engine | Untrusted string; stays in-engine |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-font | Denial | fonts.rs import | mitigate | Enforce max 20 fonts and ≤20MB server-side before/after copy |
| T-02-path | Tampering | fonts path + protocol | mitigate | sanitize_id; canonicalize under app_data/fonts only; no registry escape |
| T-02-ipc | Information Disclosure | font/search commands | mitigate | Never return font or book bytes over invoke; search stays in WebView engine |
| T-02-sql | Tampering | custom_font SQL | mitigate | Bound params for INSERT/DELETE metadata |
| T-02-zip | Denial / Tampering | torture fixtures | mitigate | Existing zip guards + decide soft-fail; no panic on corrupt |
| T-02-agpl | Tampering (legal) | Search/font UI | mitigate | Clean-room; MIT foliate search API only |
</threat_model>

<verification>
- cargo test --workspace (fonts limits + protection)
- pnpm test + pnpm build
- Grep: MAX font limits, view.search, no matchWholeWords true, fonts protocol path
</verification>

<success_criteria>
- READ-06 custom fonts end-to-end (import, apply @font-face, remove, limits)
- READ-07 CJK substring search via foliate view.search + jump
- D-27..D-34 honored
- Soft-fail CI still green for DRM/corrupt; font-obfuscation renders
</success_criteria>

<output>
Create `.planning/phases/02-epub-reading-core/02-04-SUMMARY.md` when done.
</output>
