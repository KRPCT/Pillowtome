---
phase: 02-epub-reading-core
plan: 02
type: execute
wave: 2
depends_on: ["02-00", "02-01"]
files_modified:
  - src-tauri/src/migrations.rs
  - src-tauri/tests/migration.rs
  - src/reader/reading-prefs.ts
  - src/reader/SettingsSheet.tsx
  - src/reader/FoliateView.tsx
  - src/reader/apply-reading-styles.ts
  - src/index.css
autonomous: true
requirements: [READ-02, READ-03]
must_haves:
  truths:
    - "Global reading prefs load from SQLite and auto-save on change with debounce (D-20, D-21, D-22)"
    - "Font size, line-height, margins, and font family apply live via setStyles / margin attribute (READ-02)"
    - "Day/night/sepia update reader data-theme chrome and page bg/fg via setStyles (READ-03)"
    - "Schema v2 creates reading_prefs + custom_font tables without rewriting v1"
    - "UNIQUE index on locator(work_id) enables one progress row per work"
  artifacts:
    - path: "src-tauri/src/migrations.rs"
      provides: "SCHEMA_V2 + Migration version 2"
      contains: "reading_prefs"
    - path: "src/reader/reading-prefs.ts"
      provides: "load/save global prefs via plugin-sql bound params"
      contains: "sqlite:pillow.db"
    - path: "src/reader/SettingsSheet.tsx"
      provides: "Aa bottom sheet: mode, theme, font list stub, sliders"
      contains: "字号"
    - path: "src-tauri/tests/migration.rs"
      provides: "v2 table/column assertions"
      contains: "custom_font"
  key_links:
    - from: "src/reader/SettingsSheet.tsx"
      to: "src/reader/apply-reading-styles.ts"
      via: "buildReadingCss + live prefs"
      pattern: "buildReadingCss|setStyles"
    - from: "src/reader/reading-prefs.ts"
      to: "sqlite:pillow.db"
      via: "Database.load + execute/select with $1"
      pattern: "\\$1|sqlite:pillow\\.db"
---

<objective>
Deliver typography controls, day/night/sepia dual-layer themes, and global preference persistence (READ-02, READ-03, D-20..D-22). Append schema v2 for reading_prefs + custom_font (+ locator UNIQUE for later progress), build CSS injection via setStyles, and complete the Aa bottom sheet with live apply + debounced SQLite save.

Purpose: Lithium-parity comfort controls with durable global prefs (not localStorage).
Output: SCHEMA_V2 migrated; SettingsSheet fully drives mode/theme/type; FoliateView applies and persists prefs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-epub-reading-core/02-CONTEXT.md
@.planning/phases/02-epub-reading-core/02-UI-SPEC.md
@.planning/phases/02-epub-reading-core/02-RESEARCH.md
@src-tauri/src/migrations.rs
@src-tauri/tests/migration.rs
@src/reader/FoliateView.tsx
@src/reader/apply-reading-styles.ts
@src/components/ui/sheet.tsx
@src/components/ui/slider.tsx
@src/components/ui/toggle-group.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: SCHEMA_V2 migration + migration tests (prefs, fonts, locator unique)</name>
  <files>src-tauri/src/migrations.rs, src-tauri/tests/migration.rs</files>
  <read_first>
    - src-tauri/src/migrations.rs (SCHEMA_V1 — never rewrite)
    - src-tauri/tests/migration.rs
    - 02-CONTEXT.md D-20, D-21, D-27 fields
    - 02-RESEARCH.md Recommended SCHEMA_V2 + locator UNIQUE decision
  </read_first>
  <action>
    Append schema v2 — do not modify SCHEMA_V1 string or version 1 migration.

    Add `pub const SCHEMA_V2: &str` with:

    1. `reading_prefs` table (D-20/D-21 global only):
       - id TEXT PRIMARY KEY (row id `'global'`)
       - mode TEXT NOT NULL  -- paginate|scroll
       - theme TEXT NOT NULL  -- day|night|sepia
       - font_family_key TEXT NOT NULL  -- system | custom id
       - font_size_px REAL NOT NULL
       - line_height REAL NOT NULL
       - margin_px REAL NOT NULL
       - active_font_id TEXT  -- nullable
       - updated_at INTEGER NOT NULL
       - Seed INSERT for `'global'` with defaults: paginate, day, system, 18, 1.75, 24, NULL, 0 (D-22 / UI-SPEC).

    2. `custom_font` table (metadata stub for 02-04; create now so one migration):
       - id TEXT PRIMARY KEY
       - family_name TEXT NOT NULL
       - file_name TEXT NOT NULL
       - byte_size INTEGER NOT NULL
       - created_at INTEGER NOT NULL

    3. Locator upsert strategy (planner decision):  
       `CREATE UNIQUE INDEX IF NOT EXISTS idx_locator_work_id ON locator(work_id);`  
       Rationale: v1 locator has no PK; P2 progress is one row per work; P5 annotations will use separate tables — do not overload locator for highlights.

    Update `migrations()` to return vec![v1, v2] with version 2, description `reading_prefs_and_custom_fonts`, kind Up, sql SCHEMA_V2.

    Extend `src-tauri/tests/migration.rs`:
    - Apply SCHEMA_V1 then SCHEMA_V2 on memory DB (or apply both constants).
    - Assert tables reading_prefs, custom_font exist; columns mode/theme/font_size_px/active_font_id; seed row id global.
    - Assert unique index exists (query sqlite_master or pragma index_list).
    - Update `migration_set` test: len==2, versions 1 and 2, SCHEMA_V2 contains reading_prefs and idx_locator_work_id.
    - Keep single SQLite binding (sqlx) — Pitfall 6.
  </action>
  <verify>
    <automated>cargo test --test migration</automated>
  </verify>
  <acceptance_criteria>
    - migrations.rs contains SCHEMA_V2 and version: 2 and reading_prefs and custom_font and idx_locator_work_id
    - SCHEMA_V1 block still present unchanged in spirit (work/locator/change_log)
    - migration tests assert reading_prefs + custom_font + migration set length 2
    - `cargo test --test migration` exits 0 (Windows: under MSVC toolchain per HANDOFF)
  </acceptance_criteria>
  <done>Schema v2 + tests green; locator UNIQUE ready for 02-03.</done>
</task>

<task type="auto">
  <name>Task 2: reading-prefs store + SettingsSheet + live setStyles theming</name>
  <files>src/reader/reading-prefs.ts, src/reader/SettingsSheet.tsx, src/reader/FoliateView.tsx, src/reader/apply-reading-styles.ts, src/index.css</files>
  <read_first>
    - src/reader/apply-reading-styles.ts
    - src/reader/FoliateView.tsx (post-02-01)
    - 02-UI-SPEC.md Aa bottom sheet sections + colors + copy
    - 02-CONTEXT.md D-20..D-22, D-28 fields minimum
    - 02-RESEARCH.md Pattern 2 setStyles, Pattern 5 Database.load
  </read_first>
  <action>
    **reading-prefs.ts (D-20, D-21, D-22):**
    - Use `Database.load("sqlite:pillow.db")` from `@tauri-apps/plugin-sql`.
    - `loadReadingPrefs(): Promise<ReadingPrefs>` — SELECT by id `global`; on miss return DEFAULT_PREFS.
    - `saveReadingPrefs(prefs: ReadingPrefs): Promise<void>` — INSERT ... ON CONFLICT(id) DO UPDATE with bound `$1..$n` parameters only (T-02-sql). Never string-interpolate user values into SQL.
    - Export `PREFS_SAVE_DEBOUNCE_MS = 400` (within reasonable auto-save; research allows discretion).
    - Map DB columns ↔ ReadingPrefs fields.

    **SettingsSheet.tsx (UI-SPEC Aa):**
    - shadcn Sheet side=bottom, title 显示设置, max 70vh, ScrollArea body.
    - Sections with 简体中文 copy exactly: 阅读模式 (分页/滚动 ToggleGroup), 主题 (日间/夜间/Sepia), 字体 (list: 系统默认 selected when system; custom fonts list prop for 02-04 — show empty list + 导入字体 button disabled or no-op stub with helper 仅支持 TTF、OTF、WOFF), 字号 Slider 12–32 step 1, 行距 1.2–2.4 step 0.05, 边距 8–48 step 4; show current value captions.
    - Props: open, onOpenChange, prefs, onPrefsChange(partial|full), optional fonts list, onImportFont stub.
    - No separate 应用 button (D-22).

    **FoliateView integration:**
    - On mount after open: loadReadingPrefs → apply to state → set data-theme on `.reader` root → `renderer.setAttribute("flow", flowAttr(mode))` → `renderer.setAttribute("margin", String(marginPx))` → `renderer.setStyles?.(buildReadingCss(prefs, fontFaceCss, fontFamilyCss))` where fontFamilyCss is SYSTEM_CJK_STACK when system (D-22).
    - fontFaceCss empty until 02-04.
    - On any prefs change from SettingsSheet: apply immediately to renderer + data-theme; debounce saveReadingPrefs (D-22).
    - Replace temporary mode-only sheet from 02-01 with SettingsSheet.
    - Theme page colors MUST come from PAGE_COLORS in buildReadingCss (READ-03).
    - Do not use localStorage (D-20).

    Ensure dual-layer: home shell unchanged; only `.reader[data-theme]` tokens for chrome.
  </action>
  <verify>
    <automated>pnpm test && pnpm build && cargo test --test migration</automated>
  </verify>
  <acceptance_criteria>
    - reading-prefs.ts contains sqlite:pillow.db and ON CONFLICT and $1 (bound params)
    - SettingsSheet contains 字号 and 行距 and 边距 and 日间 and 夜间 and Sepia
    - FoliateView (or helpers path) calls setStyles and setAttribute margin
    - No localStorage usage in src/reader for prefs (grep localStorage in src/reader must be 0)
    - `pnpm test`, `pnpm build`, `cargo test --test migration` exit 0
  </acceptance_criteria>
  <done>Typography + themes live-apply and persist globally via SQLite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebView JS → SQLite (plugin-sql) | Prefs read/write with user-influenced numbers/strings |
| Migration SQL → database file | Schema evolution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-sql | Tampering | reading-prefs.ts / migrations | mitigate | Parameterized `$n` queries only; fixed id `global`; migration SQL is const not user input |
| T-02-ipc | Information Disclosure | IPC | mitigate | No new commands returning book bytes; prefs are small structs in SQL not IPC |
| T-02-agpl | Tampering (legal) | SettingsSheet | mitigate | Clean-room UI-SPEC implementation |
| T-02-path | Tampering | n/a this plan | accept | Font path jail deferred to 02-04; no filesystem font writes here |
</threat_model>

<verification>
- cargo test --test migration
- pnpm test + pnpm build
- Grep: no localStorage in reader prefs path; setStyles present
</verification>

<success_criteria>
- READ-02 and READ-03 satisfied at code level (sliders/themes → setStyles + data-theme)
- D-20..D-22 prefs persistence path complete
- SCHEMA_V2 + locator UNIQUE landed for 02-03/04
</success_criteria>

<output>
Create `.planning/phases/02-epub-reading-core/02-02-SUMMARY.md` when done.
</output>
