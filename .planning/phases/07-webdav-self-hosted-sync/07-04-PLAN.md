---
phase: 07-webdav-self-hosted-sync
plan: 04
type: execute
wave: 3
depends_on: ["07-02", "07-03"]
files_modified:
  - src/sync/sync-api.ts
  - src/sync/sync-status.ts
  - src/sync/sync-status.test.ts
  - src/sync/scheduler.ts
  - src/sync/scheduler.test.ts
  - src/sync/sync-form.ts
  - src/sync/sync-form.test.ts
  - src/sync/SyncStatusButton.tsx
  - src/sync/SyncSettingsSheet.tsx
  - src/library/sync-card-state.ts
  - src/library/sync-card-state.test.ts
  - src/library/types.ts
  - src/library/library-store.ts
  - src/library/LibraryCard.tsx
  - src/library/LibraryGrid.tsx
  - src/library/LibrarySettingsSheet.tsx
  - src/reader/SettingsSheet.tsx
  - src/reader/sync-jump.ts
  - src/reader/sync-jump.test.ts
  - src/reader/ReaderBottomBar.tsx
  - src/reader/SyncUndoDialog.tsx
  - src/reader/FoliateView.tsx
  - src/App.tsx
  - src/App.css
autonomous: false
requirements: [SYNC-01, SYNC-02, SYNC-03, SYNC-04, SYNC-05]
must_haves:
  truths:
    - "No modal ever appears for a sync failure (D-93) — failures surface only as the 8px status dot + the Snackbar toast channel; the only Dialogs are user-initiated (撤回弹窗, 断开连接确认)"
    - "Every user-facing string matches the 07-UI-SPEC Copywriting Contract verbatim (简体中文, D-30)"
    - "The sync settings sheet is finger-scrollable on the AVD (touch gate #3 structure) and 测试并保存 is reachable by vertical swipe"
    - "撤回原位 returns to the exact pre-jump local position — the trace pill is driven by replacedLocal from sync_book_opened (merge byproduct) and the revert jump replays the locator RETURNED by sync_revert_jump; the UI never guesses a position"
    - "A downloaded placeholder becomes a real local book across restarts — onDownload explicitly UPDATEs the placeholder library_item row (source_id, cover_file) after ingest; ingest alone skips the existing row as skipped_duplicate (import-actions.ts:59) and would leave the placeholder unopenable"
    - "未开启同步时所有同步 UI 入口收敛到 SyncSettingsSheet — an unconfigured tap on the sync button or the settings CTA opens the sheet and triggers nothing else"
    - "No timers, polling, or background sync exist anywhere in the frontend (D-91) — only open-book pull, close-book/background push, and the manual button (D-90)"
  artifacts:
    - path: "src/sync/sync-status.ts"
      provides: "Framework-free sync-status store (sync_status invoke + \"sync-status\" event) + pure dot/aria/toast mapping + relative-time formatting"
      exports: ["SyncStatusEvent", "SyncStatusSnapshot", "SyncViewState", "createSyncStatusStore", "dotFor", "ariaLabelFor", "shouldToastFailure", "formatRelativeSyncTime"]
    - path: "src/sync/scheduler.ts"
      provides: "Pure close-gate guaranteeing sync_book_closed fires at most once per open (D-90 dedup of unmount + background double-fire)"
      exports: ["createCloseGate"]
    - path: "src/sync/sync-api.ts"
      provides: "Typed thin invoke wrappers for the ten contract IPC commands; single point for command names; password only ever flows IN to sync_test_and_save"
      exports: ["syncGetConfig", "syncTestAndSave", "syncDisconnect", "syncNow", "syncStatus", "syncSetFileSync", "syncDownloadBook", "syncBookOpened", "syncBookClosed", "syncRevertJump"]
    - path: "src/sync/SyncStatusButton.tsx"
      provides: "AppBar sync button (lucide RefreshCw) + 8px status dot per UI-SPEC §1/§C"
    - path: "src/sync/SyncSettingsSheet.tsx"
      provides: "MUI Drawer config form + status details + 断开连接 confirm per UI-SPEC §2"
    - path: "src/sync/sync-form.ts"
      provides: "Pure form logic: URL validation, remote-path normalization, client-side validation-failure copy (backend D-97 classes arrive verbatim as the Err string)"
      exports: ["validateServerUrl", "normalizeRemotePath", "copyForTestClass"]
    - path: "src/library/library-store.ts"
      provides: "V8-aware list query (file_sync_enabled) + placeholder-adoption UPDATE helper"
      exports: ["adoptSyncedFile"]
    - path: "src/library/sync-card-state.ts"
      provides: "Pure placeholder-card state derivation per UI-SPEC §3"
      exports: ["SyncCardState", "deriveCardState"]
    - path: "src/reader/sync-jump.ts"
      provides: "Pure trace-state derivation from sync_book_opened result + dialog body builder (D-92)"
      exports: ["SyncTrace", "traceFromOpenResult", "syncUndoBody"]
    - path: "src/reader/SyncUndoDialog.tsx"
      provides: "MUI Dialog 撤回原位/保留进度 (user-initiated, the one sync dialog)"
    - path: "src/library/LibraryCard.tsx"
      provides: "Placeholder variant (cloud badge / 下载中 bar / 未同步 grey) + 同步此书 MenuItem — same component family, extended in place"
    - path: "src/sync/sync-status.test.ts + src/sync/scheduler.test.ts + src/sync/sync-form.test.ts + src/library/sync-card-state.test.ts + src/reader/sync-jump.test.ts"
      provides: "vitest coverage for all new pure UI logic (Nyquist continuity)"
  key_links:
    - from: "src/sync/SyncStatusButton.tsx"
      to: "\"sync-status\" frontend event"
      via: "createSyncStatusStore(listen) — dot state driven by engine events, never by polling"
      pattern: "sync-status"
    - from: "src/reader/FoliateView.tsx"
      to: "sync_book_opened / sync_book_closed IPC"
      via: "open effect after ensure_work + unmount/handleBack/visibilitychange through createCloseGate"
      pattern: "sync_book_opened"
    - from: "src/library/LibraryCard.tsx"
      to: "sync_set_file_sync IPC"
      via: "long-press Menu 同步此书 MenuItem (above 删除)"
      pattern: "sync_set_file_sync"
    - from: "src/reader/SyncUndoDialog.tsx"
      to: "sync_revert_jump IPC"
      via: "撤回原位 action → UI jumps the view to the locator RETURNED by sync_revert_jump; open-time replacedLocal drives the trace pill"
      pattern: "sync_revert_jump"
---

<objective>
Land the entire user-visible sync surface and the lifecycle scheduler for Phase 7: open-book pull / close-book+background push / manual button (D-90/D-91, no timers anywhere), the AppBar SyncStatusButton with its 8px dot (D-93), the SyncSettingsSheet config form (SYNC-01, D-95/D-96/D-97/D-104), LibraryCard placeholder variants + per-book 同步此书 toggle (SYNC-04, D-98/D-99/D-102), and the reader trace pill + 撤回 dialog (D-92, SYNC-05's only conflict visualization). Ends with the non-autonomous gate: AVD production-APK acceptance + the D-94 real-server matrix.

Purpose: Plans 07-01..07-03 built the engine (transport, credentials, state-plane reconcile, file plane, merge). This plan makes it reachable and honest in the UI — every trigger silent, every failure a toast, every string verbatim from the UI-SPEC Copywriting Contract.
Output: Six new `src/sync/` modules, library/reader extensions with vitest-covered pure logic, and a recorded device+server acceptance matrix in 07-04-SUMMARY.md.

Cross-plan contract consumed (settled phase-wide; do not re-open): IPC commands `sync_get_config`, `sync_test_and_save`, `sync_disconnect`, `sync_now`, `sync_status`, `sync_set_file_sync`, `sync_download_book`, `sync_book_opened`, `sync_book_closed`, `sync_revert_jump`; frontend event `"sync-status"`; password NEVER crosses IPC/serde/logs/toasts (RESEARCH Pattern 4). **All sync IPC results/events are camelCase on the wire** (house convention: `src-tauri/src/commands.rs` uses `#[serde(rename_all = "camelCase")]`; `FoliateView.tsx:233` invokes camelCase). Consumed shapes: `sync_get_config()` → `{ configured, serverUrl, username, remotePath, allowHttp, trustSelfSigned, deviceName, keyringAvailable }`; `sync_test_and_save(input)` → `Ok(())` | `Err(String)` where the Err string IS the final classified Chinese copy (07-01's locked taxonomy, the D-97 classes) — rendered verbatim, never re-mapped; `sync_status()` → `{ configured, serverUrl, username, syncing, lastSyncAt, lastError }`; `sync_book_opened(workId)` → `{ jumped, deviceName, progressFraction, replacedLocal: { cfi, progressFraction } | null }` (merge `MergeOutcome.replaced_local`, RESEARCH Pattern 2); `sync_revert_jump(workId)` → `{ cfi, progressFraction } | null` (the UI jumps the view to the RESPONSE's locator); `sync_download_book(workId)` → `{ localPath, sourceId, workId }`; event `"sync-status"` payload `{ configured, syncing, lastError, downloads: [{ workId, percent }], uploads: [{ workId, percent }] }` (sole emitter: the engine, 07-02; 07-03 reports progress into it). Research corrections adopted upstream and binding here: Q1 (generic servers = streaming whole-file PUT, only Nextcloud chunks — the matrix exercises both), Q2 (`library_item.deleted` tombstone), Q3 (downloading card = progress state, tap disabled); chunk threshold/size 10MB; device name user-editable; scheduler = open/close/manual only.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-webdav-self-hosted-sync/07-CONTEXT.md
@.planning/phases/07-webdav-self-hosted-sync/07-RESEARCH.md
@.planning/phases/07-webdav-self-hosted-sync/07-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scheduler hooks — open-book pull, close/background push, zero timers (D-90/D-91)</name>
  <files>src/sync/sync-api.ts, src/sync/scheduler.ts, src/sync/scheduler.test.ts, src/reader/FoliateView.tsx</files>
  <read_first>
    - src/reader/FoliateView.tsx (workId resolution: `invoke("ensure_work", { id })` → `workIdRef.current = ensured.workId` at lines 1780-1795; main open-effect teardown at 2096-2109; `handleBack` flush-then-close at 2117-2134; props at 249-254)
    - src/App.tsx (`openItem` sets `openId` to `item.sourceId` and `closeReader` clears it, lines 78-90; the reader mounts as `<FoliateView key={openId} …>` at 121-130 — unmount-on-close is guaranteed)
    - src/reader/FoliateView.tsx line 233 (`invoke<string>("save_cover", { workId, bytes, ext })` — the repo's camelCase-args invoke convention this plan follows) and src-tauri/src/commands.rs lines 29/98/165/224/293 (`#[serde(rename_all = "camelCase")]` — why all sync IPC results/events are camelCase on the wire)
    - .planning/phases/07-webdav-self-hosted-sync/07-RESEARCH.md (D-90/D-91 locked triggers; Anti-Patterns: 后台轮询/定时同步 banned; Pitfall battery note)
    - .planning/phases/07-webdav-self-hosted-sync/07-UI-SPEC.md (§ Interaction Contract → Trigger visibility: 开书拉/合书推/切后台推 = 零 UI; failures via dot+toast only)
  </read_first>
  <action>
    Create `src/sync/sync-api.ts`: one typed async wrapper per contract command over `invoke` from `@tauri-apps/api/core` — `syncGetConfig()`, `syncTestAndSave(payload)`, `syncDisconnect()`, `syncNow()`, `syncStatus()`, `syncSetFileSync({ workId, enabled })`, `syncDownloadBook({ workId })`, `syncBookOpened({ workId })`, `syncBookClosed({ workId })`, `syncRevertJump({ workId })`, mapping to the snake_case command names `sync_get_config` … `sync_revert_jump` with camelCase arg keys (repo convention, see save_cover). Export the consumed result interfaces — all camelCase per the phase-wide wire contract: `SyncPublicConfig { configured, serverUrl, username, remotePath, allowHttp, trustSelfSigned, deviceName, keyringAvailable }`, `SyncStatusSnapshot` (the `sync_status()` result `{ configured, serverUrl, username, syncing, lastSyncAt, lastError }`), `SyncStatusEvent` (the `"sync-status"` payload), `SyncOpenResult { jumped, deviceName, progressFraction, replacedLocal }`, `SyncDownloadResult { localPath, sourceId, workId }`, `RevertedLocator { cfi, progressFraction }`. `syncTestAndSave` returns `Promise<void>`: it resolves on `Ok(())` and REJECTS with the engine's `Err(String)` — the already-classified Chinese copy — so callers render it verbatim (there is no `SyncTestResult`/error-code type; do not invent one). Password exists ONLY as an inbound field of `syncTestAndSave`'s payload — no wrapper returns it, no wrapper logs its arguments.

    Create `src/sync/scheduler.ts` with a pure close-gate (no React, no timers):
    ```ts
    export function createCloseGate() {
      let open = false;
      return {
        markOpened() { open = true; },
        /** true at most once per markOpened — fire sync_book_closed only when true. */
        consumeClose(): boolean { if (!open) return false; open = false; return true; },
      };
    }
    ```
    Create `src/sync/scheduler.test.ts`: close fires once until reopened; unmount-after-background double-close yields exactly one fire; close before any open never fires.

    Wire `src/reader/FoliateView.tsx`:
    - Hold one `closeGateRef = useRef(createCloseGate())` and a `syncOpenedRef` guard per mount.
    - In the open effect, after `workIdRef.current = ensured.workId` succeeds (~line 1784): `closeGateRef.current.markOpened()` and fire-and-forget `syncBookOpened({ workId })` — silent: `.catch((err) => console.warn("[sync] book-open pull failed", err))`. Store the resolved `SyncOpenResult` into a new `syncOpenResult` state for Task 4. Fire only on the real-ensure path (skip the `work-${id}` fallback) and only once per mount. Failure NEVER surfaces here — the engine reports it via the `"sync-status"` event → dot + toast (Task 2).
    - Push paths, all through `closeGateRef.current.consumeClose()` so exactly one fire per open: (a) the main open-effect cleanup (~line 2096) → `void syncBookClosed({ workId })`; (b) `handleBack` (~line 2117) → same before `onClose?.()`; (c) a new `visibilitychange` effect — `document.hidden === true` → consume-close push; on return to visible → `markOpened()` again (so a later close fires once) but do NOT re-pull (D-90: pull is open-book only). All catches → `console.warn` only.
    - Absolutely no `setInterval`/`setTimeout`/polling for sync. (The existing 6000ms undo-pill timer at lines 1180-1181 is unrelated reader chrome — untouched.)
  </action>
  <verify>
    <automated>cd /d/Github/Pillowtome && pnpm test src/sync/scheduler.test.ts && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "sync_book_opened" src/sync/sync-api.ts src/reader/FoliateView.tsx` ≥ 2 (wrapper + call site); `grep -c "sync_book_closed" src/sync/sync-api.ts src/reader/FoliateView.tsx` ≥ 2.
    - `grep -c "setInterval" src/sync/scheduler.ts src/sync/sync-api.ts` = 0; `grep -c "visibilitychange" src/reader/FoliateView.tsx` ≥ 1.
    - scheduler.test.ts green: double-close (background then unmount) produces exactly one `consumeClose() === true`.
    - Every sync invoke call site in FoliateView ends in `.catch(() => …)` / `console.warn` — no `setStatus`, no Dialog, no thrown propagation from sync paths (`grep -n "syncBook" src/reader/FoliateView.tsx` shows warn-only handlers).
    - `pnpm test src/sync/scheduler.test.ts` and `pnpm exec tsc --noEmit` green.
  </acceptance_criteria>
  <done>Open-book pull fires once per mount after ensure_work; close/background push fires at most once per open; nothing syncs on a timer; all failures are silent in the reader and flow to the dot+toast channel.</done>
</task>

<task type="auto">
  <name>Task 2: Library chrome — SyncStatusButton with 8px dot + LibrarySettingsSheet 同步 section</name>
  <files>src/sync/sync-status.ts, src/sync/sync-status.test.ts, src/sync/SyncStatusButton.tsx, src/App.tsx, src/library/LibrarySettingsSheet.tsx, src/reader/SettingsSheet.tsx, src/App.css</files>
  <read_first>
    - src/App.tsx (AppBar `Toolbar` cluster at 136-170 — order 示例 → ImportButton → FolderScanButton → settings `IconButton` aria-label="设置"; the new button mounts immediately left of that gear; Snackbar `status` channel at 190-196; `refreshShelf` at 51-58)
    - src/library/LibrarySettingsSheet.tsx (thin wrapper over SettingsSheet, lines 30-82)
    - src/reader/SettingsSheet.tsx (Drawer shell + drag handle at 215-244; `Section` helper at 132-144; `showLibraryPrefs` 书库 section at 408-423 — the syncSection slot goes right after it)
    - src/App.css (`.library-shell` / `.library-chrome__icon` usage in App.tsx; token pattern `var(--reader-accent, #b4472f)` at line 648)
    - .planning/phases/07-webdav-self-hosted-sync/07-UI-SPEC.md (§1 button contract, §C dot semantics, §6 settings section, a11y list)
  </read_first>
  <action>
    Create `src/sync/sync-status.ts` (framework-free, node-testable):
    - `SyncStatusEvent { configured: boolean; syncing: boolean; lastError: string | null; downloads: Array<{ workId: string; percent: number }>; uploads: Array<{ workId: string; percent: number }> }` (the `"sync-status"` payload — camelCase wire contract) and `SyncStatusSnapshot { configured: boolean; serverUrl: string | null; username: string | null; syncing: boolean; lastSyncAt: number | null; lastError: string | null }` (the `sync_status()` command result — note it carries NO transfer arrays). `SyncViewState` unifies them for the UI: `{ configured, serverUrl, syncing, lastSyncAt, lastError, downloads, uploads }`.
    - Pure mappers per UI-SPEC §C: `dotFor(state): "none" | "syncing" | "error"` (unconfigured/idle → none; syncing → syncing; lastError set → error; error clears on next success); `ariaLabelFor(state)`: exactly `"立即同步"` or `"立即同步，上次同步失败"`; `shouldToastFailure(prev, next)`: true only on a transition into `lastError != null` (no repeat toasts for a sticky error); `formatRelativeSyncTime(ts, now)`: `刚刚` / `{n} 分钟前` / `{n} 小时前` / `{n} 天前`.
    - `createSyncStatusStore(load: () => Promise<SyncStatusSnapshot>, listen: (cb) => …)`: dependency-injected tiny store (`getState`/`subscribe`) so the test drives it without Tauri; in App.tsx inject `syncStatus` from sync-api and `listen` from `@tauri-apps/api/event` on `"sync-status"`. The store initializes from the `sync_status()` snapshot with downloads/uploads set to EMPTY arrays (the command result has none); only the unified `"sync-status"` event payload carries transfer progress thereafter.
    Create `src/sync/sync-status.test.ts`: dot mapping for the four §C rows; verbatim aria labels; toast-on-transition-only; relative-time buckets; snapshot→view-state normalization starts with empty downloads/uploads.

    Create `src/sync/SyncStatusButton.tsx`: custom component wrapping MUI `IconButton` (44×44 hit), lucide `RefreshCw` (size 20, spins 1圈/s via CSS only while syncing, static under `prefers-reduced-motion`), and an absolutely-positioned `<span className="sync-status-button__dot">` 8px circle at the icon's top-right corner with a 2px inset (≈top/right 10px for a 20px icon in the 44px target), `data-state={dotFor(state)}`. Props `{ state: SyncViewState; onPress: () => void }`; `aria-label={ariaLabelFor(state)}`, `aria-busy={state.syncing}`; while syncing the tap is ignored (return early in the handler).

    `src/App.css`: add `.sync-status-button { position: relative; }`, `.sync-status-button__dot` (8px, `border-radius: 999px`, absolute top-right per above), `[data-state="syncing"]` → `background: var(--reader-accent, #b4472f)` + 1s opacity pulse keyframes (`prefers-reduced-motion: reduce` → `animation: none`), `[data-state="error"]` → `background: var(--md-error, #ba1a1a)` solid, `.sync-status-button__spin` rotate keyframes for the icon.

    `src/App.tsx`: subscribe via `useSyncExternalStore` to the store; render `<SyncStatusButton>` immediately left of the settings `IconButton` (~line 163). Tap: unconfigured → `setSyncSettingsOpen(true)` (sheet mounts in Task 3); configured → `void syncNow()` → on success `setStatus("同步完成")` + `void refreshShelf()` (merged catalog may have changed). Add an effect: on `shouldToastFailure(prev, next)` → `setStatus(`同步失败：${next.lastError}`)` — the ONLY failure surface (D-93; `lastError` arrives engine-classified in Chinese, e.g. `服务器限流，请稍后重试`).

    `src/reader/SettingsSheet.tsx`: add optional `syncSection?: ReactNode` to `SettingsSheetProps`, rendered with a `Divider` immediately after the `showLibraryPrefs` 书库 Section block (~line 423). `src/library/LibrarySettingsSheet.tsx`: build the 同步 section per UI-SPEC §6 from the store state + an `onOpenSyncSettings: () => void` prop from App — unconfigured: title `同步`, body `未开启同步`, caption `配置 WebDAV 服务器后，阅读进度与批注可在多设备间同步`, CTA `设置 WebDAV 同步`; configured: read-only rows `服务器 {serverUrl}` · `上次同步 {formatRelativeSyncTime(lastSyncAt)}` (error caption `同步失败：{原因}` when set) + row `同步设置`. Pass it into `<SettingsSheet syncSection={…}>`. Add the matching `onOpenSyncSettings` prop to `LibrarySettingsSheetProps` and wire it in App.tsx.
  </action>
  <verify>
    <automated>cd /d/Github/Pillowtome && pnpm test src/sync/sync-status.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - sync-status.test.ts green: four §C dot rows, verbatim aria labels (`立即同步`, `立即同步，上次同步失败`), sticky error toasts once, relative-time buckets, empty-transfers normalization from the `sync_status()` snapshot.
    - `grep -n "SyncStatusButton" src/App.tsx` shows the button rendered before the settings gear IconButton; `grep -c "aria-busy" src/sync/SyncStatusButton.tsx` ≥ 1.
    - `grep -c "sync-status-button__dot" src/App.css src/sync/SyncStatusButton.tsx` ≥ 2; App.css contains `prefers-reduced-motion` for both dot pulse and icon spin.
    - `grep -c "设置 WebDAV 同步\|未开启同步\|同步设置" src/library/LibrarySettingsSheet.tsx` ≥ 3; `grep -c "syncSection" src/reader/SettingsSheet.tsx` ≥ 2.
    - No sync failure path renders a MUI `Dialog`: `grep -c "Dialog" src/sync/SyncStatusButton.tsx src/sync/sync-status.ts` = 0.
    - NOTE: `tsc --noEmit` is deferred to Task 3 — App.tsx mounts `SyncSettingsSheet`, whose module is authored there. Unit tests must still pass at this task.
  </acceptance_criteria>
  <done>AppBar shows the sync button with the three dot states driven by the `"sync-status"` event; unconfigured tap and the settings CTA converge on SyncSettingsSheet; failures toast once via the existing Snackbar channel; settings sheet carries the 同步 section in both configured states.</done>
</task>

<task type="auto">
  <name>Task 3: SyncSettingsSheet — config form, forced test, TLS switches, status details, disconnect (SYNC-01, D-95/D-96/D-97/D-104)</name>
  <files>src/sync/SyncSettingsSheet.tsx, src/sync/sync-form.ts, src/sync/sync-form.test.ts</files>
  <read_first>
    - src/reader/SettingsSheet.tsx (the Drawer pattern to mirror: `anchor="bottom"`, rounded top `borderTopLeftRadius/RightRadius: 28`, drag handle Box, lines 215-244; `Switch` row shape at 348-360; `Section` at 132-144)
    - src/App.tsx (delete-confirm `Dialog`/`DialogTitle`/`DialogContentText`/`DialogActions` with 取消 + error-colored confirm, lines 198-213 — the 断开连接 confirm reuses this shape)
    - .planning/phases/07-webdav-self-hosted-sync/07-UI-SPEC.md (§2 section order, § Spacing `min(85vh, 720px)` exception, Copywriting Contract rows 服务器地址…断开连接, touch gate #3, keyring-unavailable state)
    - .planning/phases/07-webdav-self-hosted-sync/07-RESEARCH.md (Pattern 4: password never round-trips; D-97 four error classes — produced engine-side as the Err string)
  </read_first>
  <action>
    Create `src/sync/sync-form.ts` (pure): `validateServerUrl(input): "invalid" | null` (trim; require `http://`/`https://` scheme + host), `normalizeRemotePath(input): string` (trim; strip leading `/`; ensure exactly one trailing `/`; empty → `pillowtome/`), and `copyForTestClass(cls): string` for CLIENT-SIDE validation failures only — `"invalid" → 无法连接到服务器，请检查地址`. Backend test failures need no mapping: `sync_test_and_save` rejects with `Err(String)` that already IS the classified D-97 copy — the sheet renders it verbatim. Create `src/sync/sync-form.test.ts` covering the client-side invalid-URL copy, path normalization (`/pillowtome` → `pillowtome/`, ` ` → `pillowtome/`), and URL validation.

    Create `src/sync/SyncSettingsSheet.tsx` as an MUI `Drawer anchor="bottom"` sibling of SettingsSheet — rounded top, drag handle, `maxHeight: "min(85vh, 720px)"`, and the touch-gate #3 structure: header block `shrink-0` (handle + `同步设置` title), body `flex-1 min-h-0 overflow-y-auto` with `touchAction: "pan-y"` and `WebkitOverflowScrolling: "touch"`. Sections top→bottom per UI-SPEC §2:
    1. `服务器` — `TextField` 服务器地址 (placeholder `https://dav.jianguoyun.com/dav`), `TextField` 用户名, `TextField` 应用密码 `type="password"` with helper caption `建议使用应用密码，不要使用账户主密码`.
    2. `安全` — two independent `FormControlLabel`+`Switch` rows, default OFF: `允许 HTTP（明文，仅局域网）` (ON → warning caption `明文 HTTP 会暴露你的凭据，请仅在可信局域网内使用`, error color, `role="note"`) and `信任自签名证书` (ON → `仅在确认证书来自你自己的服务器时开启`). Never a single "allow all" switch (D-95).
    3. `远端路径` — `TextField` default `pillowtome/` + helper `多台设备必须填写相同路径`.
    4. `设备名称` — `TextField` (prefilled from config/device default) + helper `同步提示里显示的本机名字`.
    5. 操作行 — primary `测试并保存` (MUI contained) + `取消`; while testing the button shows `正在测试连接…` and is disabled; result is an inline caption: success `连接成功，已保存`, failure renders the rejected Err string verbatim in error color (already the D-97 classified copy). **测试未通过不得保存** — the engine only persists on ok.
    6. `同步状态` (configured only) — read-only rows: 服务器 URL truncated to one line, `上次同步 {formatRelativeSyncTime}`, status `正常` / `同步失败：{原因}`; trailing row action `立即同步`.
    7. `断开连接` (configured only) — destructive text row → MUI `Dialog`: title `断开同步连接`, body `仅移除本机的服务器配置与凭据。服务器上的数据保留，其他设备不受影响。`, actions `取消` (inherit) / `断开` (`color="error"`).

    Data flow: on open, `syncGetConfig()` prefills serverUrl/username/remotePath/switches/deviceName; the password field is ALWAYS empty on reopen, with caption `凭据已保存在系统密钥环` shown when configured. If `keyringAvailable === false` in the config response → disable 测试并保存 and show caption `系统密钥环不可用，无法保存凭据`. `测试并保存` → `validateServerUrl` (invalid → inline `copyForTestClass("invalid")`) → `syncTestAndSave({ serverUrl, username, password, remotePath: normalizeRemotePath(...), allowHttp, trustSelfSigned, deviceName })` → resolves: inline `连接成功，已保存` + refresh the status store; rejects: inline caption with the rejection string rendered verbatim. `断开` → `syncDisconnect()` → close sheet + store refresh. The password lives only in the field's local state; it is never written into any other state, never logged, never included in any toast (`grep`-enforced below).
  </action>
  <verify>
    <automated>cd /d/Github/Pillowtome && pnpm test src/sync/sync-form.test.ts && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Verbatim copy present (each `grep -c … src/sync/SyncSettingsSheet.tsx` ≥ 1): `https://dav.jianguoyun.com/dav`, `建议使用应用密码，不要使用账户主密码`, `凭据已保存在系统密钥环`, `允许 HTTP（明文，仅局域网）`, `明文 HTTP 会暴露你的凭据，请仅在可信局域网内使用`, `信任自签名证书`, `仅在确认证书来自你自己的服务器时开启`, `多台设备必须填写相同路径`, `同步提示里显示的本机名字`, `正在测试连接…`, `连接成功，已保存`, `系统密钥环不可用，无法保存凭据`, `断开同步连接`, `仅移除本机的服务器配置与凭据。服务器上的数据保留，其他设备不受影响。`
    - Client-side mapping only: `grep -c "无法连接到服务器，请检查地址" src/sync/sync-form.ts src/sync/sync-form.test.ts` ≥ 2, and the backend-owned classes are NOT re-mapped in the frontend (`grep -c "认证失败，请检查用户名和应用密码\|证书校验失败，可开启「信任自签名证书」\|没有目录写入权限，请检查路径" src/sync/sync-form.ts` = 0) — the sheet renders the `Err(String)` verbatim.
    - Touch-gate structure greps: `grep -c "min(85vh, 720px)" src/sync/SyncSettingsSheet.tsx` = 1; `grep -c "pan-y" src/sync/SyncSettingsSheet.tsx` ≥ 1; `grep -c 'type="password"' src/sync/SyncSettingsSheet.tsx` = 1.
    - Password hygiene: `grep -c "console.log" src/sync/SyncSettingsSheet.tsx src/sync/sync-api.ts` = 0; `grep -c "dangerouslySetInnerHTML" src/sync/SyncSettingsSheet.tsx` = 0.
    - `pnpm test src/sync/sync-form.test.ts` green; `pnpm exec tsc --noEmit` clean (this is the deferred gate for Task 2's App.tsx wiring).
  </acceptance_criteria>
  <done>The sheet configures, force-tests (the engine's classified Chinese failure string rendered verbatim), saves credentials to keychain only, shows status details, and disconnects behind a confirm dialog — finger-scrollable per touch gate #3, password never echoed anywhere.</done>
</task>

<task type="auto">
  <name>Task 4: Placeholder cards + 同步此书 toggle + reader trace pill and 撤回 dialog (SYNC-02/SYNC-04/SYNC-05, D-92/D-98/D-99/D-102)</name>
  <files>src/library/sync-card-state.ts, src/library/sync-card-state.test.ts, src/library/types.ts, src/library/library-store.ts, src/library/LibraryCard.tsx, src/library/LibraryGrid.tsx, src/App.tsx, src/reader/sync-jump.ts, src/reader/sync-jump.test.ts, src/reader/ReaderBottomBar.tsx, src/reader/SyncUndoDialog.tsx, src/reader/FoliateView.tsx, src/App.css</files>
  <read_first>
    - src/library/LibraryCard.tsx (whole file: badge slot `.library-card__badge` at 109-111, progress bar at 112-119, long-press 500ms/10px at 21-22+48-64, Menu with 删除 at 129-148 — 同步此书 inserts ABOVE 删除)
    - src/library/types.ts (`LibraryItem` lines 6-20, `LibraryItemRow` lines 22-33) and src/library/library-store.ts (`listLibraryItems` locator LEFT JOIN at 34-61, `rowToLibraryItem` at 15-31 — THIS plan extends them; 07-02 touches no frontend files)
    - src/library/import-actions.ts (`ingestPathToLibrary(path, knownHashes)` at 29-92; the `libraryHasWorkId(result.workId)` → `skipped_duplicate` early-return at 59-64 — the grounded reason ingest ALONE never flips a placeholder row) and src/library/import-pipeline.ts (`knownHashesFromItems` at 52-61)
    - src/library/LibraryGrid.tsx (prop threading, lines 69-77) and src/App.tsx (`onDelete`/`onStatus` patterns at 176-186, 151)
    - src/reader/ReaderBottomBar.tsx (`.reader__undo-pill` slot at 94-104 — sync pill reuses it with priority), src/reader/FoliateView.tsx (`captureUndo` at 1163-1182 — ALL manual jumps funnel here; `loadLocator` restore at 1801-1808; `jumpToWholeFraction` at 1430; ReaderBottomBar render at 2308-2318), src/reader/reading-position.ts (`ReadingPosition`, lines 14-23)
    - src/reader/AnnotationsSheet.tsx line 208 (`{a.note}` renders as a React text node — 冲突副本 prefix renders as-is, no change)
    - .planning/phases/07-webdav-self-hosted-sync/07-UI-SPEC.md (§3 card states, §4 menu toggle, §5 trace+dialog, accent-reserved list, Copywriting Contract)
  </read_first>
  <action>
    `src/library/types.ts`: extend `LibraryItem` with `fileLocal?: boolean` and `fileSyncEnabled?: boolean`, and `LibraryItemRow` with `file_sync_enabled?: number | null` (SCHEMA_V8 column; optional so rows predating V8 still type-check).

    `src/library/library-store.ts` (this plan owns the frontend store extension): add `li.file_sync_enabled AS file_sync_enabled` to the `listLibraryItems` SELECT, and derive in `rowToLibraryItem`: `fileLocal: row.source_id !== "sync-remote"` (placeholder rows inserted by the state-plane merge carry the sentinel source_id `sync-remote`), `fileSyncEnabled: !!row.file_sync_enabled`. Add a new exported helper `adoptSyncedFile(workId: string, sourceId: string, coverFile: string | null)`: `UPDATE library_item SET source_id = $1, cover_file = $2 WHERE work_id = $3` — soft-fail style consistent with the file.

    Create `src/library/sync-card-state.ts` (pure): `type SyncCardState = "local" | "downloadable" | "downloading" | "failed" | "unsynced"`; `deriveCardState(item: { fileLocal?: boolean; fileSyncEnabled?: boolean }, download: { percent: number } | "failed" | null): SyncCardState` — local file → `"local"`; remote-only + download in flight → `"downloading"`; remote-only + `"failed"` → `"failed"`; remote-only + fileSyncEnabled → `"downloadable"`; remote-only + not enabled → `"unsynced"`. Test the full UI-SPEC §3 matrix in `sync-card-state.test.ts`.

    `src/library/LibraryCard.tsx` (extend in place, same family):
    - New optional props: `syncState?: SyncCardState` (default `"local"`), `downloadPercent?: number | null`, `uploading?: boolean`, `onDownload?: (item) => void`, `onToggleFileSync?: (item, enabled: boolean) => void`, `onStatus?: (msg: string | null) => void` (the ImportButton toast pattern, threaded from App via LibraryGrid).
    - Placeholder variants per §3: cover area keeps the existing `.library-card__placeholder` initial (cover bytes never travel the state plane, D-100); the badge slot shows a cloud badge INSTEAD of 已读 (never both): `CloudDownload` icon for downloadable/downloading/failed, `CloudOff` icon + text badge `未同步` for unsynced. downloading → 3px `.library-card__progress` bar at `downloadPercent` + caption `下载中 {n}%` under the meta, tap disabled (Q3: 禁用打开, `aria-disabled="true"`); failed → downloadable visuals (the `下载失败，请检查网络后重试` toast is emitted by the App handler at failure time), tap retries via `onDownload`; unsynced → `opacity: 0.55`, tap → `onStatus("该书未开启文件同步，进度与批注仍会同步")`, `aria-disabled="true"` + `aria-label={`${title}，未同步`}`; downloadable → `aria-label="下载此书"`, tap → `onDownload(item)` (no immediate open). Reading-progress bar still renders on placeholder cards when progress exists (D-102).
    - Long-press Menu: insert `同步此书` MenuItem ABOVE 删除, hidden when `fileLocal === false` (开关属于持有文件的一端); icon `Check` when enabled else `CloudUpload` (lucide); while `uploading`, caption becomes `正在上传…` and the item is disabled; otherwise tap → `onToggleFileSync(item, !item.fileSyncEnabled)` → App invokes `syncSetFileSync` → toast `已开启同步《${title}》` / `已关闭同步《${title}》` via the status channel.
    - `src/library/LibraryGrid.tsx`: add pass-through props (`syncView` maps keyed by workId: `downloads`, `uploads`; `onDownload`, `onToggleFileSync`, `onStatus`) and compute per-item card props. `src/App.tsx`: build the maps from the sync-status store and implement the two handlers:
      ```ts
      // onDownload(item) — the placeholder-adoption path (explicit; ingest alone cannot flip the row)
      const res = await syncDownloadBook({ workId: item.workId });          // → { localPath, sourceId, workId }
      const known = knownHashesFromItems(shelf).filter((h) => h !== item.workId); // exclude THIS work or library_ingest dedup-refuses it
      const ingest = await ingestPathToLibrary(res.localPath, known);       // D-100 reparse: metadata/cover; row update skipped (see below)
      await adoptSyncedFile(item.workId, res.sourceId, ingest.coverFile ?? null); // UPDATE library_item SET source_id, cover_file
      void refreshShelf();
      ```
      Grounded necessity: with the placeholder row present, `ingestPathToLibrary`'s `libraryHasWorkId(result.workId)` check (import-actions.ts:59) returns true, so ingest exits as `skipped_duplicate` and never touches `source_id` — without the explicit `adoptSyncedFile` UPDATE the row keeps the `sync-remote` sentinel and the book stays unopenable across restarts. Failure anywhere in the chain → `onStatus("下载失败，请检查网络后重试")` + mark the card `failed` (tap retries). `onToggleFileSync(item, enabled)` → `syncSetFileSync({ workId: item.workId, enabled })` → toast per the contract strings.
    - `src/App.css`: `.library-card--unsynced { opacity: 0.55; }`, `.library-card__badge--cloud` (icon badge reusing `.library-card__badge` metrics: top/right 6px, 4px radius), `.library-card__sync-caption` (12px caption color `var(--md-on-surface-variant, #53433f)`), `.reader__undo-pill--sync svg { color: var(--reader-accent, #b4472f); }` (pill body unchanged — accent only on the icon per the reserved list).

    Reader trace + undo (D-92):
    - Create `src/reader/sync-jump.ts` (pure): `interface SyncTrace { deviceName: string; percent: number; replacedLocal: { cfi: string | null; progressFraction: number | null } }`; `traceFromOpenResult(res: SyncOpenResult): SyncTrace | null` (null unless `res.jumped && res.replacedLocal && res.progressFraction != null`; percent = `Math.round(clamp01(res.progressFraction) * 100)`; deviceName falls back to `另一台设备` when the engine reports none); `syncUndoBody(t): string` → exactly `「${t.deviceName}」上读到了 ${t.percent}%，已自动跳到最远位置。`. Cover in `sync-jump.test.ts` (jumped=false → null; verbatim body; rounding; fallback name).
    - `src/reader/FoliateView.tsx`: new `syncTrace` state. When Task 1's `syncBookOpened` resolves, `traceFromOpenResult` non-null → `loadLocator(workIdRef.current)` and jump the view via the EXISTING restore machinery (real cfi → the pendingLocatorRef relocate path; else `jumpToWholeFraction(fraction)`) WITHOUT calling `captureUndo`, then `setSyncTrace(trace)`. Add `setSyncTrace(null)` as the first line of `captureUndo()` (any manual jump replaces the pill, §5) and clear it in the unmount cleanup. Session-scoped: no auto-hide timer.
    - `src/reader/ReaderBottomBar.tsx`: optional props `syncTraceVisible?: boolean`, `onSyncTrace?: () => void`; when true render in the undo slot (sync pill priority — the undo pill is suppressed): `<button type="button" className="reader__undo-pill reader__undo-pill--sync" onClick={onSyncTrace} aria-label="已从其他设备同步"><CloudDownload aria-hidden />已从其他设备同步</button>`. No hot-zone growth beyond the pill (touch gate).
    - Create `src/reader/SyncUndoDialog.tsx`: MUI `Dialog` — title `已从其他设备同步`; `DialogContentText` body from `syncUndoBody`; actions `撤回原位` (contained, primary) / `保留进度` (inherit). `撤回原位` → `const reverted = await syncRevertJump({ workId })` then jump the view to the RESPONSE's locator (`reverted.cfi` → relocate path; else `jumpToWholeFraction(reverted.progressFraction)`) — robust even if the in-memory trace was lost — and `setSyncTrace(null)`; `保留进度` → `setSyncTrace(null)`. (The open-time `replacedLocal` still drives the trace pill and dialog body; the revert target comes from the command response.) This dialog is user-initiated (pill tap) — D-93 untouched. Wire it in FoliateView next to the existing sheets (~line 2320).
    - 批注合并 stays UI-silent; `冲突副本`-prefixed notes render as-is via AnnotationsSheet's existing text-node path — no code change, assert in criteria.
  </action>
  <verify>
    <automated>cd /d/Github/Pillowtome && pnpm test src/library/sync-card-state.test.ts src/reader/sync-jump.test.ts && pnpm exec tsc --noEmit && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - sync-card-state.test.ts covers all five §3 states; sync-jump.test.ts asserts the verbatim body template `「{设备名称}」上读到了 {n}%，已自动跳到最远位置。` and `jumped: false → null`.
    - `grep -c "file_sync_enabled" src/library/library-store.ts src/library/types.ts` ≥ 2; `grep -c "sync-remote" src/library/library-store.ts` ≥ 1; `grep -c "adoptSyncedFile" src/library/library-store.ts src/App.tsx` ≥ 2.
    - `grep -c "同步此书" src/library/LibraryCard.tsx` ≥ 1; the 同步此书 MenuItem appears before 删除 in file order; `grep -c "正在上传…" src/library/LibraryCard.tsx` ≥ 1.
    - `grep -c "CloudDownload\|CloudOff\|CloudUpload" src/library/LibraryCard.tsx src/reader/ReaderBottomBar.tsx` ≥ 3; `grep -c "已从其他设备同步" src/reader/ReaderBottomBar.tsx src/reader/SyncUndoDialog.tsx` ≥ 2; `grep -c "撤回原位\|保留进度" src/reader/SyncUndoDialog.tsx` ≥ 2.
    - `grep -n "setSyncTrace(null)" src/reader/FoliateView.tsx` shows a hit inside `captureUndo`; no `setTimeout`/`setInterval` added for the trace pill (session-scoped, unlike the 6000ms undo timer).
    - `grep -c "dangerouslySetInnerHTML" src/reader/AnnotationsSheet.tsx src/library/LibraryCard.tsx src/reader/SyncUndoDialog.tsx` = 0 (remote-sourced strings render as text nodes only).
    - Toasts verbatim in App/LibraryCard paths: `已开启同步《`, `已关闭同步《`, `下载失败，请检查网络后重试`, `该书未开启文件同步，进度与批注仍会同步`.
    - `pnpm test … && pnpm exec tsc --noEmit && pnpm build` all green.
  </acceptance_criteria>
  <done>Remote books render as placeholder cards with the four §3 states; 同步此书 toggles per-book file sync with toasts; a downloaded placeholder row is explicitly adopted as a local book; an open-book pull that finds further progress silently jumps, leaves a session-scoped trace pill, and the dialog's 撤回原位 restores the exact pre-jump position returned by sync_revert_jump.</done>
</task>

<task type="checkpoint">
  <name>Task 5: CHECKPOINT — AVD production-APK acceptance + D-94 real-server matrix (non-autonomous)</name>
  <files>.planning/phases/07-webdav-self-hosted-sync/07-04-SUMMARY.md</files>
  <read_first>
    - docs/ANDROID-BUILD.md (the project's AVD production-APK flow)
    - .planning/phases/07-webdav-self-hosted-sync/07-VALIDATION.md (§ Manual-Only Verifications — the three manual gates this checkpoint executes)
    - .planning/phases/07-webdav-self-hosted-sync/07-RESEARCH.md (§ Environment Availability — docker daemon caveat; dufs fallback; 坚果云 free-tier limits: 600 req/30min, 500MB file cap)
    - .planning/phases/07-webdav-self-hosted-sync/07-UI-SPEC.md (§ Device acceptance — the five AVD coverage items)
  </read_first>
  <action>
    Run the full automated gate first: `pnpm test && pnpm exec tsc --noEmit && pnpm build`, then `cargo test --workspace` (repo note: cargo runs in src-tauri need the MSVC toolchain — GNU gcc exits 1 silently).

    AVD acceptance (CLAUDE.md gate — production APK, never `tauri android dev`): `pnpm tauri android build --debug --target x86_64 --apk`, `adb install -r` the APK, force-stop + cold start, then verify by hand:
    1. Sync sheet finger scroll: with an oversize form (small viewport), vertical swipe scrolls the body and 测试并保存 is reachable by finger (not just desktop wheel) — touch gate #3.
    2. Status dot three states: unconfigured/idle (no dot) → syncing (朱砂 pulse, static under reduced-motion) → failure (error dot, sticky until next success); failure surfaces as toast `同步失败：{原因}` — never a modal.
    3. Placeholder flow end-to-end: a book synced from another device appears as a cloud-badge card → tap → `下载中 {n}%` → completes → normal card (placeholder row adopted via `adoptSyncedFile`), opens, progress/annotations in effect.
    4. Undo dialog: with further progress on the other device, open the book → silent jump → trace pill `已从其他设备同步` → tap → dialog → `撤回原位` returns to the exact pre-jump position.
    5. Keychain survives relaunch: save config, force-stop, cold start — configured state persists, password field empty with `凭据已保存在系统密钥环`.

    Real-server matrix (D-94 — at least one full sync round each: configure → 测试并保存 → close-book push → second device/profile pull → progress+annotations land):
    - 坚果云 free account (application password): exercise the rate-limit path — repeat manual syncs to provoke 503/429 and confirm exponential backoff + toast `同步失败：服务器限流，请稍后重试`.
    - Nextcloud behind a real proxy (docker + nginx with `client_max_body_size 100m`, or the user's own instance): upload a >10MB book so chunk v2 engages (10MB threshold/10MB chunks), plus a progress/annotation round. If the docker daemon is down (RESEARCH environment caveat), start Docker Desktop or substitute the user's real Nextcloud instance.
    - Generic RFC-4918 server via `cargo install dufs --locked` (e.g. `dufs -A` on localhost): connect over `http://` with the `允许 HTTP（明文，仅局域网）` switch ON (exercises the D-95 switch), confirm streaming whole-file PUT (research Q1 correction — no chunking on generic servers) and a full state round.

    Record every result (server, version, round outcome, quirks hit — ETag presence, 412 handling, 限流 timing) in the SUMMARY's matrix table. Any failure here blocks the phase gate.
  </action>
  <verify>
    <automated>cd /d/Github/Pillowtome && pnpm test && pnpm exec tsc --noEmit && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - Full frontend suite + `pnpm build` green; `cargo test --workspace` green under the MSVC toolchain.
    - AVD checklist items 1-5 each recorded as pass (with notes) in 07-04-SUMMARY.md — including "no modal appeared for any induced sync failure".
    - Every `sync_*` command the UI invokes WORKS on the production APK with zero capabilities-file edits (phase posture per 07-01's grounded decision: no app ACL manifest exists, existing commands run ungated, adding entries panics at startup) — verified by exercising them, not by inspecting grants.
    - Matrix table in the SUMMARY has one full-round row each for 坚果云 / proxied Nextcloud (chunk v2 with a >10MB book) / dufs-over-HTTP, and the 坚果云 rate-limit row shows the verbatim toast `同步失败：服务器限流，请稍后重试`.
  </acceptance_criteria>
  <done>Device acceptance and all three server-matrix seats pass with results recorded; the phase is ready for /gsd-verify-work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebView form → keychain (via sync_test_and_save) | The password crosses IPC exactly once, inbound; nothing may echo it back |
| Engine → WebView (`"sync-status"` event, IPC results) | Remote-sourced strings (server URL, device names, book titles, error text, 冲突副本 notes) enter the UI |
| Other device → this device's reader position | sync_book_opened's replacedLocal drives the trace pill; sync_revert_jump's response drives the 撤回 jump target |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | Info Disclosure | Password in IPC responses / toasts / logs | mitigate | sync-api wrappers never return or log the password (inbound-only field); sheet keeps it in field-local state; `sync_get_config` response has no password field (RESEARCH Pattern 4 / Security Domain: credentials only in keychain); `console.log` grep = 0 in sync modules |
| T-07-04-02 | Info Disclosure | `lastError` leaking credentials/Authorization headers | mitigate | Engine classifies errors to fixed Chinese strings (07-01 owns); UI renders `lastError` as a plain text node inside `同步失败：{原因}`; the event payload interface carries no credential material |
| T-07-04-03 | Tampering (XSS) | Remote-sourced strings in cards/sheets/dialogs | mitigate | React text-node rendering only; `dangerouslySetInnerHTML` grep = 0 across all new/touched files (extends the T-05-10 pattern at AnnotationsSheet.tsx:9); V5 input validation of remote JSON lives engine-side (RESEARCH § Security Domain) |
| T-07-04-04 | Elevation | New `sync_*` IPC surface | mitigate | Command names centralized in sync-api.ts; NO capabilities/ACL edits anywhere in the phase (07-01's grounded posture: no app ACL manifest exists, existing commands run ungated, adding entries panics at startup) — the Task 5 checkpoint verifies the commands WORK on the production APK, not that grants exist; least-privilege ACL remains a v2 hardening item |
| T-07-04-05 | Tampering | 撤回 restores a UI-guessed position | mitigate | UI never computes the revert target — the trace pill is driven by `replacedLocal` from the sync_book_opened result (merge byproduct, RESEARCH Pattern 2 / Pitfall 7), and the 撤回 jump replays the locator RETURNED by sync_revert_jump |
| T-07-04-06 | DoS (UX) | Failure storms → toast spam / modal abuse | mitigate | Toasts fire only on error *transitions* (`shouldToastFailure`); failures are never modals (D-93); the single Snackbar `status` channel serializes messages; no retry loops or timers in the UI (D-91) |
| T-07-04-07 | Spoofing (MITM) | TLS downgrade via the two safety switches | mitigate | Two INDEPENDENT switches (D-95), default OFF, each with its verbatim Chinese warning caption; the sheet never offers a combined "allow everything" control; path jail under the configured root stays engine-side (remote.rs) |
</threat_model>

<artifacts_produced>
## Artifacts this plan produces (07-04)

- **`src/sync/sync-api.ts`** — typed invoke wrappers for the ten contract commands; the only place command names appear; password is inbound-only; `sync_test_and_save` rejections carry the engine's classified Chinese copy verbatim (`Err(String)`).
- **`src/sync/sync-status.ts`** — `SyncStatusEvent`/`SyncStatusSnapshot`/`SyncViewState` types, `createSyncStatusStore(load, listen)`, and pure mappers `dotFor`, `ariaLabelFor`, `shouldToastFailure`, `formatRelativeSyncTime` (+ `sync-status.test.ts`).
- **`src/sync/scheduler.ts`** — `createCloseGate()` (at-most-one close push per open) (+ `scheduler.test.ts`).
- **`src/sync/sync-form.ts`** — `validateServerUrl`, `normalizeRemotePath`, `copyForTestClass` (client-side invalid-URL copy only; backend D-97 classes render verbatim from the Err string) (+ `sync-form.test.ts`).
- **`src/sync/SyncStatusButton.tsx`** — AppBar button + 8px dot (§C semantics, aria-busy, verbatim aria labels).
- **`src/sync/SyncSettingsSheet.tsx`** — the §2 config form + status details + 断开连接 confirm, touch-gate #3 structure.
- **`src/library/library-store.ts`** — `listLibraryItems` selects `file_sync_enabled`; `rowToLibraryItem` derives `fileLocal`/`fileSyncEnabled` (sentinel `sync-remote`); new `adoptSyncedFile` UPDATE helper flips a placeholder row into a real local book.
- **`src/library/sync-card-state.ts`** — `deriveCardState` five-state matrix (+ test); `src/library/types.ts` gains `fileLocal`/`fileSyncEnabled` + the `file_sync_enabled` row field.
- **`src/library/LibraryCard.tsx` / `LibraryGrid.tsx` / `App.tsx`** — placeholder variants, 同步此书 menu item, store wiring, the explicit onDownload adopt-then-refresh handler, toasts.
- **`src/reader/sync-jump.ts`** — `SyncTrace`, `traceFromOpenResult`, `syncUndoBody` (+ test); `ReaderBottomBar.tsx` sync pill slot; `SyncUndoDialog.tsx`; `FoliateView.tsx` open/close/background hooks + trace state.
- **Consumed contract shapes (settled phase-wide; camelCase on the wire):** `sync_get_config()` → `{configured, serverUrl, username, remotePath, allowHttp, trustSelfSigned, deviceName, keyringAvailable}`; `sync_status()` → `{configured, serverUrl, username, syncing, lastSyncAt, lastError}`; `"sync-status"` → `{configured, syncing, lastError, downloads, uploads}`; `sync_book_opened` → `{jumped, deviceName, progressFraction, replacedLocal}`; `sync_revert_jump` → `{cfi, progressFraction} | null`; `sync_download_book` → `{localPath, sourceId, workId}`; `sync_test_and_save` → `Ok(()) | Err(String)`.
- **07-04-SUMMARY.md** — with the AVD checklist and the D-94 three-seat server matrix results.
</artifacts_produced>

<verification>
- `pnpm test src/sync/scheduler.test.ts src/sync/sync-status.test.ts src/sync/sync-form.test.ts src/library/sync-card-state.test.ts src/reader/sync-jump.test.ts` green; full `pnpm test` green at Task 5.
- `pnpm exec tsc --noEmit` clean (gate at Task 3 and Task 4); `pnpm build` green at Task 4/5.
- `cargo test --workspace` green at Task 5 (MSVC toolchain; GNU gcc exits 1 silently).
- No `setInterval`/sync `setTimeout` in new modules; no `Dialog` rendered for any sync failure; password `console.log` grep = 0; no capabilities-file edits anywhere in the phase.
- AVD production-APK checklist (5 items) + real-server matrix (3 seats, one full round each) recorded in 07-04-SUMMARY.md.
</verification>

<success_criteria>
- Open-book pull / close-book+background push / manual button are the only triggers in the app — no timers, no polling (D-90/D-91).
- Sync failures never produce a modal; they surface exactly once as `同步失败：{原因}` toast + the sticky error dot (D-93).
- The SyncSettingsSheet saves only after a successful classified test, keeps the password in keychain only, and scrolls by finger on Android (SYNC-01).
- Placeholder cards, per-book 同步此书, and the trace pill + 撤回 dialog deliver SYNC-02/04/05's visible behavior with verbatim UI-SPEC copy; a downloaded placeholder is adopted as a real local book; 撤回 returns to the exact pre-jump position returned by sync_revert_jump.
- AVD acceptance and the D-94 matrix (坚果云 rate-limit, proxied Nextcloud chunk v2, dufs HTTP) all pass and are recorded.
</success_criteria>

<output>
Create `.planning/phases/07-webdav-self-hosted-sync/07-04-SUMMARY.md` when done.
</output>
