---
phase: 04-local-library
plan: 03
type: execute
wave: 3
depends_on: ["04-00", "04-01"]
files_modified:
  - src/reader/reading-position.ts
  - src/reader/reading-position.test.ts
  - src/reader/position-bus.ts
  - src/reader/position-bus.test.ts
  - src/reader/FoliateView.tsx
  - src/reader/ContinuousScrollStream.tsx
  - src/reader/locator-store.ts
  - src/library/library-store.ts
  - src/App.tsx
autonomous: true
requirements: [LIB-01, LIB-04]
must_haves:
  truths:
    - "Single ReadingPosition SSOT: spineIndex + offsetFraction + optional cfi + UI fraction (MAJOR READER-POS)"
    - "One apply/jump command path for open-from-library, paginate↔scroll, and scroll TOC (D-64)"
    - "Silent resume from locator; invalid → text start no modal (D-63, D-25)"
    - "last_opened_at on open; last_read_at with debounced locator writes (D-65)"
    - "Paginate→scroll does not land at book start when mid-book position known"
    - "Scroll-mode TOC jump reaches target spine index"
  artifacts:
    - path: "src/reader/position-bus.ts"
      provides: "Imperative position apply helpers / types"
    - path: "src/reader/FoliateView.tsx"
      provides: "Wired open/mode/TOC to bus"
    - path: "docs/MAJOR-READER-POS.md"
      provides: "Acceptance scenarios (device)"
  key_links:
    - from: "library open"
      to: "loadLocator + applyPosition"
      via: "work_id"
      pattern: "loadLocator|applyPosition|jumpTo"
    - from: "mode switch"
      to: "capture position → apply other surface"
      via: "position-bus"
      pattern: "pillow-scroll|goTo"
---

<objective>
Implement READER-POS: library open/resume and dual-surface continuity via one position SSOT + jump command bus (roadmap success criterion 5, D-63..D-65, docs/MAJOR-READER-POS.md).

Purpose: Opening from the shelf and switching modes/TOC must not invent a second progress model or drop jumps.

Output: position-bus module; FoliateView/ContinuousScrollStream wiring; last_opened/last_read updates; unit tests for pure helpers; device acceptance documented for UAT.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@docs/MAJOR-READER-POS.md
@.planning/phases/04-local-library/04-CONTEXT.md
@.planning/phases/04-local-library/04-RESEARCH.md
@src/reader/reading-position.ts
@src/reader/ContinuousScrollStream.tsx
@src/reader/FoliateView.tsx
@src/reader/locator-store.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Position SSOT helpers + jump bus pure API</name>
  <files>src/reader/reading-position.ts, src/reader/reading-position.test.ts, src/reader/position-bus.ts, src/reader/position-bus.test.ts</files>
  <read_first>
    - docs/MAJOR-READER-POS.md (full)
    - src/reader/reading-position.ts
    - ContinuousScrollApi.jumpTo signature
  </read_first>
  <action>
    Formalize ReadingPosition as the only cross-surface resume shape.
    position-bus: pure functions to (1) build apply plan for paginate vs scroll, (2) capture from relocate/scroll progress into ReadingPosition, (3) resolve TOC href→spineIndex when given resolve fn.
    Extend tests: pillow-scroll round-trip; prefer real cfi when present; never use whole-book % alone as resume key.
    Document that React wiring must call imperative jumpTo/goTo after mount, not only setState jumpKey races.
  </action>
  <verify>
    <automated>pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - position-bus.test.ts and reading-position.test.ts pass
    - Comments ban dual progress stores
  </acceptance_criteria>
  <done>Pure SSOT + bus ready for shell wiring.</done>
</task>

<task type="auto">
  <name>Task 2: FoliateView/stream wiring + library open timestamps</name>
  <files>src/reader/FoliateView.tsx, src/reader/ContinuousScrollStream.tsx, src/reader/locator-store.ts, src/library/library-store.ts, src/App.tsx</files>
  <read_first>
    - FoliateView mode switch + TOC + continuous props
    - ContinuousScrollStream onReady jumpTo
    - D-63..D-65
  </read_first>
  <action>
    Wire:
    - Open book: loadLocator(workId) → applyPosition; set last_opened_at
    - Mode switch paginate↔scroll: capture current ReadingPosition → apply other surface (fix MAJOR fail #2)
    - TOC in scroll mode: resolve href → jumpTo(spine, 0) via stream API (fix #3)
    - Debounced locator upsert also updates library_item.last_read_at
    - Silent resume; no continue/start modal
    Do not expand CJK scope. Prefer ContinuousScrollApi over remount jumpKey alone.
  </action>
  <verify>
    <automated>pnpm test && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - FoliateView uses position-bus or equivalent single apply path for mode switch and open
    - last_opened / last_read SQL updates exist in library-store or locator-store
    - pnpm build exits 0
    - SUMMARY lists device UAT scenarios still required (paginate→scroll, TOC scroll, library resume)
  </acceptance_criteria>
  <done>READER-POS implemented; device UAT for full gate.</done>
</task>

</tasks>

<threat_model>
| Threat | Mitigation |
|--------|------------|
| T-04-pos | Never bare % resume; composite locator retained |
| T-04-race | Imperative jump after mount; no dual buses |
</threat_model>

<verification>
pnpm test; pnpm build; manual device gates per MAJOR-READER-POS
</verification>

<success_criteria>
Single SSOT jump path; library timestamps; unit green; device scenarios documented
</success_criteria>

<output>
Create `.planning/phases/04-local-library/04-03-SUMMARY.md` when done.
</output>
