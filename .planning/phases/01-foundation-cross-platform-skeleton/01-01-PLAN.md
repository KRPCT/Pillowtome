---
phase: 01-foundation-cross-platform-skeleton
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - Cargo.lock
  - package.json
  - pnpm-lock.yaml
  - vite.config.ts
  - tsconfig.json
  - .gitignore
  - .gitmodules
  - core/Cargo.toml
  - core/src/lib.rs
  - core/src/error.rs
  - core/src/protection.rs
  - core/src/publication/mod.rs
  - core/src/locator.rs
  - core/src/source.rs
  - src-tauri/Cargo.toml
  - src-tauri/tauri.conf.json
  - src-tauri/build.rs
  - src-tauri/capabilities/default.json
  - src-tauri/src/lib.rs
  - src-tauri/src/main.rs
  - src-tauri/src/protocol.rs
  - src-tauri/src/storage.rs
  - src-tauri/src/commands.rs
  - src-tauri/src/migrations.rs
  - src-tauri/tests/protocol_range.rs
  - src/main.tsx
  - src/App.tsx
  - src/lib/pillow.ts
  - src/vendor/foliate-js
autonomous: true
requirements: [FND-01, FND-02]
must_haves:
  truths:
    - "The app compiles as a Cargo workspace and cargo build --workspace succeeds on the host"
    - "cargo test -p pillowtome-core runs green (empty/stub baseline) proving core is off-device unit-testable"
    - "A pillow:// asynchronous URI-scheme protocol is registered and answers Range requests with 200/206/416"
    - "The bundled sample EPUB is registered in the SourceRegistry under id 'sample' at Builder setup, so pillow://.../sample resolves at runtime (FND-01/FND-02 depend on this)"
    - "The custom scheme is whitelisted in CSP so the WebView can fetch book bytes without silent failure"
    - "foliate-js is vendored at a recorded pinned commit with its MIT LICENSE retained (no npm range)"
    - "Book bytes are reachable only through the custom protocol, never through Tauri IPC (D-06)"
  artifacts:
    - path: "Cargo.toml"
      provides: "[workspace] members = [core, src-tauri]"
      contains: "[workspace]"
    - path: "core/src/lib.rs"
      provides: "Portable core crate root declaring all seam module stubs"
      contains: "pub mod"
    - path: "src-tauri/src/protocol.rs"
      provides: "Range-aware pillow:// byte streamer + parse_range helper"
      min_lines: 40
    - path: "src-tauri/src/storage.rs"
      provides: "SourceRegistry + bundled-sample registration (id 'sample' -> resource path)"
      contains: "sample"
    - path: "src-tauri/tauri.conf.json"
      provides: "CSP allowlisting the pillow scheme + bundle resources + minSdk"
      contains: "pillow"
    - path: "src/lib/pillow.ts"
      provides: "Per-platform pillow:// URL builder"
      exports: ["pillowUrl"]
    - path: "src/vendor/foliate-js"
      provides: "Vendored MIT render engine pinned to a commit"
  key_links:
    - from: "src-tauri/src/lib.rs"
      to: "register_asynchronous_uri_scheme_protocol"
      via: "Builder registration of the pillow scheme"
      pattern: "register_asynchronous_uri_scheme_protocol"
    - from: "src-tauri/src/lib.rs"
      to: "SourceRegistry insert('sample', resource_path)"
      via: "app.path().resolve(..., BaseDirectory::Resource) at setup"
      pattern: "BaseDirectory::Resource|insert\\(.*sample"
    - from: "src-tauri/src/protocol.rs"
      to: "SourceRegistry (managed state)"
      via: "resolve id -> bytes, path-traversal scoped"
      pattern: "range|Content-Range"
---

<objective>
Stand up the cross-platform Tauri v2 skeleton that every later feature runs inside, and wire the one boundary rule that matters: large book bytes reach the WebView through a Range-aware custom protocol, never over IPC (D-06). This plan scaffolds the app, refactors it into a Cargo workspace with a portable `pillowtome-core` crate plus the `src-tauri` glue crate, vendors foliate-js at a pinned commit, pins every dependency exactly with committed lockfiles (supply-chain baseline), pre-declares all `core` and `src-tauri` module stubs so downstream plans fill files without touching shared roots, registers the `pillow://` asynchronous URI-scheme protocol with CSP whitelisting, and registers the bundled sample EPUB id in the `SourceRegistry` at startup so `pillow://.../sample` resolves the moment the reading slice (Plan 04) drops the file.

Purpose: Establishes the shell + byte-streaming boundary that FND-01/FND-02 reading and FND-03 import depend on. Getting the workspace/protocol/IPC boundary right on day 1 is exactly what prevents the Lithium late-refactor trap.
Output: A compiling cross-platform workspace, a registered Range-capable `pillow://` protocol with the bundled sample pre-registered, vendored pinned foliate-js, and empty-but-declared seam modules ready for Plans 02/03 to fill.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-cross-platform-skeleton/01-CONTEXT.md
@.planning/phases/01-foundation-cross-platform-skeleton/01-RESEARCH.md
@.planning/research/ARCHITECTURE.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Workspace + toolchain scaffold with exact-pinned deps, lockfiles, and vendored foliate-js</name>
  <files>Cargo.toml, Cargo.lock, core/Cargo.toml, core/src/lib.rs, src-tauri/Cargo.toml, package.json, pnpm-lock.yaml, vite.config.ts, tsconfig.json, .gitignore, .gitmodules, src/vendor/foliate-js</files>
  <read_first>
    - 01-RESEARCH.md §"Recommended Project Structure", §"Pattern 1: Cargo workspace over the Tauri scaffold", §"Standard Stack" (exact pinned versions + Installation block)
    - 01-CONTEXT.md D-01, D-02, D-03, D-11, D-12 (framework, frontend, layout, license clean-room, minSdk/NDK)
    - CLAUDE.md "Supply-Chain Zero Trust" (exact pins, committed lockfiles, no floating ranges, treat install hooks as code execution)
  </read_first>
  <action>
    Scaffold with `pnpm create tauri-app` selecting React + TypeScript + pnpm (D-01/D-02), then immediately refactor into a Cargo workspace per D-03: convert the repo-root `Cargo.toml` into `[workspace] members = ["core", "src-tauri"]`, create the portable `pillowtome-core` crate (package name `pillowtome-core`, dir `core/`, ZERO tauri/platform dependencies so it cross-compiles and unit-tests off-device) with a MINIMAL `core/src/lib.rs` (crate doc comment only; module stubs are added in Task 2) so the workspace compiles now, and add `pillowtome-core = { path = "../core" }` to `src-tauri/Cargo.toml`. Leave the generated `src-tauri/src/lib.rs` + `main.rs` as the default scaffold for now (Task 3 rewrites the Builder).

    Pin every dependency to the exact versions in 01-RESEARCH.md §Standard Stack (tauri 2.11.5, tauri-plugin-sql 2.4.0 with sqlite feature, tauri-plugin-fs 2, tauri-plugin-dialog 2, serde/serde_json 1, thiserror 2; core: uuid v4, blake3 1, zip 2, thiserror 2, serde 1; JS: @tauri-apps/api 2.11.1, @tauri-apps/plugin-sql 2.4.0, @tauri-apps/plugin-fs 2.5.1, @tauri-apps/plugin-dialog 2.7.1, react/react-dom 19, dev: @tauri-apps/cli, typescript 5, vite). NO `^`/`~`/`latest`/unbounded `>=` anywhere. Set the `packageManager` field to the exact installed pnpm version and commit `pnpm-lock.yaml` AND `Cargo.lock`.

    Vendor foliate-js as a git submodule at `src/vendor/foliate-js` pinned to an explicit commit SHA (do NOT add it as an npm dependency — the author warns the API is unstable; per D-11/D-02 and the supply-chain baseline vendor a pinned commit). Record the exact SHA in `src/vendor/foliate-js/VENDOR.md` and retain the upstream MIT LICENSE file unmodified. Do NOT copy any AGPL Readest source (D-11 clean-room). Configure `.gitignore` to exclude generated build artifacts (`target/`, `dist/`, `node_modules/`, and the generated `src-tauri/gen/android/` except intentional config edits) per 01-RESEARCH.md Runtime State Inventory.
  </action>
  <verify>
    <automated>pnpm install --frozen-lockfile && cargo build --workspace</automated>
  </verify>
  <acceptance_criteria>
    - Root `Cargo.toml` contains `[workspace]` with members `core` and `src-tauri`; `cargo build --workspace` exits 0 (core has a minimal lib.rs; src-tauri uses the default scaffold Builder).
    - `pnpm install --frozen-lockfile` succeeds; `pnpm-lock.yaml` and `Cargo.lock` are committed; grep of `package.json` and both `Cargo.toml` files finds no `^`, `~`, `latest`, or bare `>=` on user-maintained deps.
    - `src/vendor/foliate-js` exists as a pinned submodule with an unmodified MIT `LICENSE` and a `VENDOR.md` recording the SHA; foliate-js is absent from `package.json` dependencies.
    - `.gitignore` excludes `target/`, `dist/`, `node_modules/`, and the generated Android project.
  </acceptance_criteria>
  <done>Compiling workspace with pinned deps, committed lockfiles, and vendored pinned foliate-js (~8-10 files).</done>
</task>

<task type="auto">
  <name>Task 2: Core seam module stubs + app config + Chinese frontend shell</name>
  <files>core/src/lib.rs, core/src/error.rs, core/src/protection.rs, core/src/publication/mod.rs, core/src/locator.rs, core/src/source.rs, src-tauri/tauri.conf.json, src/main.tsx, src/App.tsx</files>
  <read_first>
    - 01-RESEARCH.md §"Recommended Project Structure" (module layout), §"Pattern 3" (CSP baseline notes)
    - 01-CONTEXT.md D-03 (portable core behind traits), D-12 (minSdk 26)
    - CLAUDE.md "UI/UX语言使用简体中文"
  </read_first>
  <action>
    Pre-create ALL seam module stubs so Plans 02 and 03 fill files without editing shared crate roots: in `core/src/lib.rs` declare `pub mod error; pub mod protection; pub mod publication; pub mod locator; pub mod source;` and create each referenced file as a minimal compiling placeholder (a doc comment plus a `// filled by plan 01-02 / 01-03` marker, no real logic — `error.rs`/`protection.rs` are filled by Plan 02; `publication/mod.rs`/`locator.rs`/`source.rs` by Plan 03). Keep `cargo test -p pillowtome-core` green on the empty stubs.

    Configure `src-tauri/tauri.conf.json`: set `bundle.resources` to glob `assets/sample/*` (the sample file is bundled by Plan 04; this globs its location so the resource resolver in Task 3 finds it), set Android `minSdkVersion` 26 (D-12), and add a baseline strict CSP object (default `'self' ipc: http://ipc.localhost`; the pillow scheme entries are appended in Task 3). Set the app UI language default to Simplified Chinese where the scaffold exposes it.

    Wire a minimal `src/App.tsx` + `src/main.tsx` shell that mounts and shows the app chrome in 简体中文 (the "打开示例书籍" button is added by Plan 04; here just prove the WebView renders the React shell). Do NOT add any IPC command that returns book bytes (D-06).
  </action>
  <verify>
    <automated>cargo build --workspace && cargo test -p pillowtome-core && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `core/src/lib.rs` declares modules `error`, `protection`, `publication`, `locator`, `source`, and each file exists and compiles; `cargo test -p pillowtome-core` exits 0 (green stub baseline; proves core has no tauri/platform deps).
    - `src-tauri/tauri.conf.json` sets Android `minSdkVersion` to 26 and globs `assets/sample/*` in bundle resources.
    - `pnpm build` (Vite build of the Chinese React shell) exits 0.
    - No `#[tauri::command]` returns raw book bytes (grep confirms).
  </acceptance_criteria>
  <done>All seam module stubs declared, app config set (minSdk/bundle/baseline CSP), and the Chinese React shell builds — unblocking Plans 02/03.</done>
</task>

<task type="auto">
  <name>Task 3: Range-aware pillow:// protocol + SourceRegistry + bundled-sample registration + CSP + URL helper</name>
  <files>src-tauri/src/protocol.rs, src-tauri/src/storage.rs, src-tauri/src/commands.rs, src-tauri/src/migrations.rs, src-tauri/src/lib.rs, src-tauri/src/main.rs, src-tauri/build.rs, src-tauri/capabilities/default.json, src-tauri/tauri.conf.json, src-tauri/tests/protocol_range.rs, src/lib/pillow.ts</files>
  <read_first>
    - 01-RESEARCH.md §"Pattern 3: Range-aware custom protocol", §"Pitfall 1" (per-platform URL), §"Pitfall 2" (CSP silent failure), §"Security Domain" (over-broad protocol scope / path traversal)
    - 01-CONTEXT.md D-06 (book bytes never cross IPC), D-05 (BookSource, never raw path)
    - Referenced upstream: tauri-apps/tauri examples/streaming/main.rs (200/206/416, 1 MB MAX_LEN, multipart/byteranges) — cited in 01-RESEARCH.md Sources
  </read_first>
  <action>
    Define the `SourceRegistry` in `src-tauri/src/storage.rs` as a `Mutex<HashMap<String, PathBuf>>` held in Tauri managed state (Plan 05 later migrates the value type to `core::source::BookSource`), with helpers to register and resolve an id. Create `commands.rs` and `migrations.rs` as stubs (`migrations()` returning `vec![]`, `pub const SCHEMA_V1: &str = "";`) and wire `add_migrations("sqlite:pillow.db", migrations())` on the sql plugin Builder now so Plan 03 only edits `migrations.rs`.

    In `src-tauri/src/protocol.rs` implement the `pillow` asynchronous URI-scheme handler registered via `register_asynchronous_uri_scheme_protocol("pillow", ...)` on the Builder in `lib.rs`. The handler must: parse the requested id from `request.uri()`; resolve it through the `SourceRegistry`; read the `range` request header and respond `200` (full body), `206 Partial Content` with `Content-Range: bytes s-e/len` and `Content-Length`, or `416` when unsatisfiable, capping each served range at 1 MB per the official streaming example. Extract a pure `parse_range(header: Option<&str>, total_len: u64) -> RangeResolution` helper so the range math is unit-testable without a running WebView. Enforce V12/V5 scope (Security Domain): resolve ONLY registry-known ids to their registered bytes — never an arbitrary caller-supplied path; normalize and reject any `../` traversal and confirm the resolved path stays within the app's scoped library/resource directory before reading.

    Register the bundled sample at Builder `.setup()` (the goal-achievement fix): resolve the bundled path with `app.path().resolve("assets/sample/sample.epub", tauri::path::BaseDirectory::Resource)` and insert it into the `SourceRegistry` under id `"sample"`, BEFORE the frontend ever calls `pillowUrl('sample')`. The file itself is dropped by Plan 04; the resolver computes the path and the registration is unconditional so the registry→protocol plumbing is in place from Wave 1 and `pillow://.../sample` resolves the moment the file exists.

    In `src/lib/pillow.ts` export `pillowUrl(id: string): string` that builds the correct per-platform URL (Windows `http://pillow.localhost/<id>`, Android `https://pillow.localhost/<id>`, macOS/Linux `pillow://localhost/<id>`) by detecting the platform (via `@tauri-apps/plugin-os` or a build-time constant). In `src-tauri/tauri.conf.json` append every per-platform scheme form to CSP `connect-src` AND `img-src`, `media-src`, `style-src`, `font-src` (EPUB internal resources also load through the render pipeline; missing entries surface as silent fetch failures per Pitfall 2), e.g. `http://pillow.localhost https://pillow.localhost pillow:` alongside `'self' ipc: http://ipc.localhost`.

    Add an integration test `src-tauri/tests/protocol_range.rs` that: (1) unit-tests `parse_range` (no header → 200/full; `bytes=0-99` on 1000 bytes → 206 `Content-Range: bytes 0-99/1000`; out-of-range start → 416); and (2) registers a TEMP fixture file under a test id in a `SourceRegistry` and asserts the resolve+serve path returns 200 for a full request and 206 for a Range request — proving the registry→protocol plumbing end to end (independent of the bundled sample file, which arrives in Plan 04). Do NOT ship book bytes over any IPC command (D-06).
  </action>
  <verify>
    <automated>cargo test -p pillowtome --test protocol_range && cargo build --workspace</automated>
  </verify>
  <acceptance_criteria>
    - `src-tauri/src/protocol.rs` registers the `pillow` scheme via `register_asynchronous_uri_scheme_protocol` and `grep -q register_asynchronous_uri_scheme_protocol src-tauri/src/lib.rs` succeeds.
    - The bundled sample is registered: `grep -Eq "BaseDirectory::Resource|insert\\(.*\"sample\"" src-tauri/src/lib.rs src-tauri/src/storage.rs` succeeds (id `"sample"` inserted into the registry at setup) — goal-achievement fix.
    - `cargo test -p pillowtome --test protocol_range` exits 0: `parse_range` asserts 200/206/416 AND a registered temp fixture serves 200 (full) + 206 (Range), proving the registry→protocol path.
    - `src-tauri/tauri.conf.json` CSP `connect-src` contains `http://pillow.localhost`, `https://pillow.localhost`, and `pillow:`; the same forms appear in `img-src`/`media-src`/`style-src`/`font-src`.
    - `src/lib/pillow.ts` exports `pillowUrl` returning the http/https/scheme form matched to the platform; path resolution rejects `..` traversal; no `#[tauri::command]` returns raw book bytes.
  </acceptance_criteria>
  <done>pillow:// Range-aware protocol registered + scope-guarded, bundled sample pre-registered at setup, CSP-whitelisted per platform, with a unit+integration-tested range/registry path and a per-platform URL builder.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebView (foliate-js/React) → src-tauri protocol | Untrusted book bytes + fetch requests cross into the native protocol handler |
| Third-party packages → build | npm/cargo dependencies + vendored foliate-js enter the trusted build at install time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | pillow:// protocol handler | mitigate | Resolve only registry-known ids; normalize + reject `../` traversal; confine reads to the scoped library/resource dir (never an arbitrary path from the request) |
| T-01-02 | Elevation of Privilege | WebView render context / CSP | mitigate | Strict CSP in tauri.conf.json; scheme whitelisted only where needed; no remote origins in connect-src; book-content JS hardening flagged for Phase 2 |
| T-01-03 | Tampering | Book bytes over IPC (D-06) | mitigate | Bytes stream via custom protocol only; no IPC command carries book bytes — verified by grep gate |
| T-01-SC | Tampering | npm + cargo installs, vendored foliate-js | mitigate | Exact-version pins, committed pnpm-lock.yaml + Cargo.lock, foliate-js vendored at a pinned SHA with retained MIT LICENSE; audit table in 01-RESEARCH.md dispositions all P1 packages Approved (the [SUS] tauri-plugin-android-fs is deferred to and gated in Plan 05) |
</threat_model>

<verification>
- Task 1: `cargo build --workspace` + `pnpm install --frozen-lockfile` green.
- Task 2: `cargo test -p pillowtome-core` + `pnpm build` green (core stubs + Chinese shell).
- Task 3: `cargo test -p pillowtome --test protocol_range` green (range math 200/206/416 + registry→protocol serve); sample registration grep-verified.
- No floating version ranges; foliate-js vendored, not an npm dep; no book bytes over IPC.
</verification>

<artifacts_produced>
## Artifacts this plan produces

- **Crates:** `pillowtome-core` (portable, no tauri deps); `pillowtome` (src-tauri glue crate).
- **Workspace:** root `Cargo.toml` `[workspace]`; committed `Cargo.lock` + `pnpm-lock.yaml`.
- **Custom protocol:** scheme name `pillow` (`pillow://` / `http://pillow.localhost` / `https://pillow.localhost`); `src-tauri/src/protocol.rs` handler + `parse_range` helper + `RangeResolution`.
- **Managed state:** `SourceRegistry` (`Mutex<HashMap<String, PathBuf>>`) in `src-tauri/src/storage.rs`, with the bundled sample pre-registered under id `"sample"` (migrated to `BookSource` in Plan 05).
- **Module stubs (declared, filled later):** `core/src/{error,protection,publication/mod,locator,source}.rs`; `src-tauri/src/{storage,commands,migrations}.rs` (`migrations()` + `SCHEMA_V1` stubs).
- **Frontend:** `src/lib/pillow.ts` exporting `pillowUrl`; `src/App.tsx` shell; `src/main.tsx`.
- **Tests:** `src-tauri/tests/protocol_range.rs` (range math + registry→protocol serve).
- **Vendored:** `src/vendor/foliate-js` (pinned SHA, MIT LICENSE, `VENDOR.md`).
</artifacts_produced>

<success_criteria>
- Workspace compiles and core tests pass off-device.
- pillow:// protocol answers Range requests (200/206/416), scope-guarded, CSP-whitelisted per platform.
- Bundled sample id registered at setup so `pillow://.../sample` resolves once Plan 04 drops the file (FND-01/FND-02 unblocked).
- foliate-js vendored at a pinned commit; all deps exact-pinned with committed lockfiles.
- No book bytes traverse IPC (D-06). All seam module stubs declared, unblocking Plans 02/03.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-cross-platform-skeleton/01-01-SUMMARY.md` when done.
</output>
